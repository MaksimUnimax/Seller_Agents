# B3 — ChatGPT Standard H3 corrective evidence

Date: 2026-09-14  
Branch: `feature/server-health-h3-p8-4`  
Rejected predecessor: `d727d372cc01fb0554de98126d16d63cf842d5fb`  
Accepted B2 ancestor: `0474d83e27074f9f61a41efd13e916d2ab75d20c`

## Correction

### Third architectural correction: fail-closed completion semantics

The architect independently identified a remaining production defect in the
second correction: `OBSERVE_COMPLETION` swallowed busy/Stop wait failures,
checked `aria-busy` only below the response element, and did not revalidate
conversation identity or response content. That allowed an incomplete
response, or identity drift, to reach Bridge validation instead of failing at
completion.

The corrected Standard strategy now mirrors the mature read-only authority in
`apps/extension/src/imported/ozon-v0.1.22/shared/ai_adapters.js`:
the associated response remains attached, generation is inactive, and bounded
response content is non-empty. Completion checks the response element's own
`aria-busy` state through the bounded surface-wide `[aria-busy="true"]`
authority, requires every authoritative busy marker to clear, requires every
visible Stop control to clear, and revalidates the exact bound route/canonical
conversation identity on every poll. It returns PASS only when all conditions
are simultaneously true; an unmet condition remains a bounded completion
timeout or a completion observation failure and is never converted to PASS.
Response content is inspected only as an ephemeral boolean and is never
stored, returned, logged, or persisted.

The deterministic fixture now proves response-self busy stuck, Stop-cleared
with another busy marker remaining, busy-cleared with Stop remaining, empty
response after generation, identity change during completion, and valid
completion. Each negative case asserts `failureStep = OBSERVE_COMPLETION`, one
physical Send, and no resend; `COMPLETION_MISSING` remains covered as the
legacy completion-stuck case.

The rejected candidate located every Standard contour through synthetic
`hf-*`/`data-hf-*` markers generated by its own fixture. This correction adds
the immutable local `CHATGPT_STANDARD_H3_V2` profile in
`apps/health-runner/src/standard-h3-profile.ts`. The strategy uses its typed
locator builders and the existing B2 engine; no caller selector, URL, script,
raw prompt, raw response, or remote override is accepted.

### Second architectural correction: fresh-chat identity lifecycle

The original B3 implementation coupled Standard surface identification to
fixture-only `hf-*`/`data-hf-*` markers. The first correction preserved the
real-authority `CHATGPT_STANDARD_H3_V2` profile and removed that coupling.

The exact previous CI candidate was run `34869901458` (job `104062922373`),
for head `0fcc31fe8c31105e6338016e8797df1359af171a`. Its failing Standard E2E
path was reproduced locally before this correction: the strategy required a
conversation identity before Send, while a fresh root had no route/canonical
`/c/<UUID>` identity yet. The resulting valid-path failure was the fresh-chat
lifecycle defect; the conversation-change case was also classified as a
separate pre-correction identity-observation defect. The remote log endpoint
was unavailable in this execution environment, so the local reproduction is
the recorded failure evidence rather than a claim of direct log readback.

The corrected strategy carries a small typed binding state:

- `UNBOUND_FRESH`: an allowed Standard root may identify its surface,
  composer, and insert the packaged prompt without a conversation UUID.
- `BOUND`: an existing authoritative route/canonical identity is bound before
  Send, or a fresh identity is bound once during post-Send observation.

The mature read-only identity authority is
`apps/extension/src/imported/ozon-v0.1.22/shared/conversation_identity.js`.
The packaged profile mirrors its same-origin `/c/<UUID>` route/canonical
semantics and rejects route/canonical conflicts. Response association,
completion, and Bridge validation require a stable bound identity. A never-
binding fresh run or any identity mutation fails within the bounded
observation path, with no resend.

The deterministic fixture now includes fresh-root/no-ID, existing stable
conversation, never-bind, existing-identity-change, fresh-bound-identity-change,
and route/canonical-conflict variants. Fresh variants intentionally do not
pre-seed a canonical conversation link; one valid route/canonical identity is
created only after the single Send. Historical assistant content remains a
baseline and cannot satisfy response association.

The deterministic fixture now models the profile's real ChatGPT semantics:
the bounded document root plus `/c/<UUID>` route/canonical identity, `#prompt-textarea` and the
accepted prompt fallbacks, `button[data-testid="send-button"]`,
`button[data-testid="stop-button"]`, `section[data-turn="assistant"]`,
`data-turn-id`, writing-block/CodeMirror/`#code-block-viewer`/`pre > code`
surfaces, and native Copy buttons labelled `Copy`/`Копировать`. Fixture
bookkeeping is limited to orthogonal `data-fixture-*` attributes and server
counters; the production strategy never queries them.

## Authority ledger

| Contour | Packaged Standard authority | Fixture mirror |
| --- | --- | --- |
| Surface/conversation | Imported adapter host policy; `body` is only the bounded traversal root; imported conversation adapter `/c/<UUID>` with path/canonical conflict rejection and bounded fallback | `main` inside `body`, route-compatible canonical link |
| Composer/input | Imported `chatgptComposerContext()`: `#prompt-textarea`, prompt `data-testid`, bounded prompt fallbacks; forms own the active input | `form` containing `#prompt-textarea` |
| Send/Stop/busy | Imported `chatgptSendCandidates()`: `send-button`, Send/submit semantics, stop/cancel exclusion; imported `stop-button`/`aria-busy` generation signal | Send and Stop test controls with the same provider semantics |
| Assistant/response | Imported `assistantMessages()` and `messageId()`: assistant turn semantics and `data-turn-id`; route identity is rechecked after send | `section[data-turn="assistant"]` and bounded turn IDs |
| Completion | Imported `messageComplete()`: attached response, non-generating state, non-empty response; Standard strategy additionally waits for busy state to clear | response text plus Stop hidden/`aria-busy` cleared |
| Code/Copy | Imported `findCodeBlocks()`/`chatgptCopyOwnedBlock()`: writing-block, CodeMirror, legacy viewer, `pre > code`; Copy/Копировать native controls owned by the local code surface | writing-block region, `pre > code`, local Copy button |
| Conversation identity | Imported `BB2ConversationIdentity`: URL `/c/<UUID>`, same-origin canonical fallback, conflict rejection | dynamic loopback origin with `/c/<UUID>` canonical link |
| Delivery insertion | Imported composer/send ownership: prompt editor and irreversible control share the active form; no marketplace delivery runs in B3 | same form-owned editor/send relationship |
| Blockers | Provider auth route `/auth/login` plus bounded accessible dialog labels; unknown dialog fails closed as verification uncertainty | auth route and labelled checkpoint dialogs |

The imported adapter is read-only reference authority; no extension runtime is
imported or modified. The repository adapter does not provide authenticated
blocker-specific markup beyond the auth route, so other checkpoint labels are
intentionally conservative and classify unknown accessible dialogs as bounded
`VERIFICATION_CHECKPOINT` rather than claiming provider-specific certainty.

## Anti-coupling and acceptance

- `standard-h3-profile.test.ts` scans the production profile and strategy for
  `hf-*`, `data-hf-*`, `fixture-editor`, `fixture-send`, localhost/test
  branches, and arbitrary environment backdoors.
- E2E runs the same packaged strategy through the same B2 engine with fixture
  bookkeeping present and removed.
- Profile revision output is asserted by E2E; removing a real profile semantic
  is exercised by Work/surface, missing composer/send, and the post-send drift
  variants.
- The fixture has no production-only success path and no strategy-visible
  test bookkeeping.

The full deterministic matrix is 36/36 PASS in real Chromium. It covers valid
Standard, wrong/missing surface, blocker states, all composer/send failures,
old-response exclusion, busy/response/completion failures, code/Copy drift,
fresh-root binding, existing-conversation binding, never-bind, identity
mutation, route/canonical conflict, conversation change, fail-closed
completion signals/content, identity drift during completion, and
delivery-path drift. Normal PASS and every post-send failure recorded exactly one fixture
Send activation; all pre-send and blocker cases recorded zero. Stop never
increments the Send counter.

Focused health-runner unit coverage is 73/73 green, including H2, the B2
engine, the packaged action vocabulary, the V2 profile, and explicit
`UNBOUND_FRESH`/`BOUND`/conflict identity resolution cases. The H2 Chromium
regression is 13/13 green.

No Work strategy, B5 persistence, live H3 acceptance, P8.5 scheduling,
database migration, contract change, Stream A edit, extension edit, provider
call, customer session, or security bypass was introduced. The old candidate
run `34875403817` for `cd4f12db167311e82ab35fd2687167bd5aa5684c` was queried
directly through both the workflow-run and workflow-jobs endpoints; GitHub
returned 404 for both, so its terminal conclusion is not readable in this
execution environment. It remains architecturally rejected regardless of
that readback because its deterministic completion matrix did not prove the
fail-closed invariant. Final correction-head CI is recorded below after the
exact new candidate run completes.

### Final exact candidate CI

- Candidate SHA: `PENDING_LOCAL_VALIDATION`
- Server CI run/job: `PENDING_PUSH`
- Terminal result: `PENDING_PUSH`
