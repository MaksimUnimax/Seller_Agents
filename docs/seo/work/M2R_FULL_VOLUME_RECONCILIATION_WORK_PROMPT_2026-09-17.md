# WORK PROMPT — Octoport SEO M2R full-volume reconciliation

Use this prompt in **ChatGPT Work** as one bounded execution task.

---

You are executing a full-volume analytical reconciliation for the existing **Octoport / Октопорт SEO program**.

This is **NOT a new project**.

This is **NOT final semantic-core construction**.

This is **NOT clustering**.

This is **NOT page architecture**.

This is **NOT content writing**.

This is **NOT a site implementation task**.

This is **NOT a provider-acquisition task**.

You must perform one independent, full-volume reconciliation of the complete currently authorized historical Wordstat + M2R correction evidence so that Main Chat can safely resume **M3 ordinary Yandex Search SERP collection** under the owner-corrected product model.

## A. Repository and branch

Repository:

`MaksimUnimax/runtime-fixtures`

Branch:

`seo/wordstat-batch-01-2026-09-16`

Frozen evidence HEAD before the Work handoff files themselves were added:

`5dc772191d264be30f3858bfe2dffe0059cb5af8`

Your FIRST ACTION is mandatory:

1. fetch the current remote branch;
2. record the actual live HEAD;
3. verify the repository and `docs/seo/**` authorities exist;
4. read the full pre-handoff manifest:
   `docs/seo/work/M2R_FULL_VOLUME_RECONCILIATION_PRE_HANDOFF_MANIFEST_2026-09-17.md`;
5. inspect any `docs/seo/**` changes after the frozen evidence HEAD;
6. if governing product truth, evidence authority, methodology or input files changed materially, revalidate the task before proceeding;
7. if authority conflict cannot be resolved from current repository truth, STOP with `WORK_EXECUTION_HOLD_AUTHORITY_DRIFT` rather than guessing.

Do not assume the SHA in this prompt is still current.

## B. Role and authority

Main Chat is the architect/controller. You are the full-volume execution environment.

You may:

- read all authorized inputs completely;
- parse/reconcile/deduplicate/classify them;
- create the required analytical output artifacts;
- run mechanical and semantic QA;
- produce one ZIP handoff.

You may NOT:

- mutate product truth;
- invent a new permanent SEO methodology;
- run Wordstat/Search/Alice/provider requests;
- browse the external web to replace missing repository evidence;
- create final page architecture;
- create final semantic clusters;
- mark M7 Collection Freeze as passed;
- implement site/code changes;
- commit/push/open PR/publish to GitHub.

## C. Governing documents — READ IN FULL BEFORE ANALYSIS

Read all of these completely and treat them as current authority unless a newer repository authority explicitly supersedes them:

- `docs/seo/PRODUCT_TRUTH.md`
- `docs/seo/QUALITY_FIRST_RESOURCE_RULE.md`
- `docs/seo/WORK_HANDOFF_RULE.md`
- `docs/seo/SEO_MASTER_ROADMAP_2026-09-16.md`
- `docs/seo/research/PRODUCT_AUDIENCE_API_AND_KNOWLEDGE_BOUNDARIES_2026-09-16.md`
- `docs/seo/wordstat/M2_WORDSTAT_RETRO_GATE_AUDIT_2026-09-16.md`
- `docs/seo/wordstat/M2_WORDSTAT_VOLUME_ACCOUNTING_2026-09-16.md`
- `docs/seo/wordstat/BATCH_01_SYNTHESIS_2026-09-16.md`
- `docs/seo/wordstat/BATCH_02_SYNTHESIS_2026-09-16.md`
- `docs/seo/wordstat/M2R_TASK_FAMILY_COVERAGE_AUDIT_2026-09-16.md`
- `docs/seo/serp/M3_QUERY_MATRIX_2026-09-16.md`
- current `docs/seo/serp/SERP_PROGRESS.md` if present.

Also read the pre-handoff manifest in full and obey every input/output/boundary rule in it.

## D. Product truth — HARD, DO NOT REWRITE

Canonical product mechanism:

`user-selected supported AI -> Octoport -> authorized Ozon/Wildberries data/tools -> same AI works as seller employee/helper`

Octoport is **not**:

- a proprietary LLM;
- `our AI`;
- itself the AI employee;
- a dashboard-only analytics service;
- a stand-alone card-image/infographic generator;
- a 24/7 real-time advertising bid bot.

Launch is read-only / analysis / explanation / preparation oriented. Do not silently widen current launch claims into autonomous mutation/editing/bidding.

Search demand may still enter through older categories such as AI agents, analytics services, reports, helper language, cards, advertising analytics and search/niche analytics. Product-fit classification must be based on the underlying user job, not exact category naming alone.

## E. Authorized evidence scope — PROCESS COMPLETELY, NO SAMPLING

### E1. Historical M2 raw Wordstat

Read every current file matching:

- `docs/seo/wordstat/raw/B01_*.md`
- `docs/seo/wordstat/raw/B02_*.md`

Preserve the accepted historical limitation: early B01 files are durable structured transcriptions with factual result bodies/provenance, not necessarily exact complete top-level bridge envelopes. Do not downgrade valid result-body evidence merely because wrapper fidelity differs.

### E2. M2R A01-A19

Read every current M2R artifact for A01 through A19:

- raw responses under `docs/seo/wordstat/raw/`;
- analysis files under `docs/seo/wordstat/analysis/`;
- query-specific pre-step/release files under `docs/seo/wordstat/`.

Do NOT skip tiny or totalCount-only steps. They are part of the evidence model.

### E3. Historical accounting / syntheses / audits

Read the M2 accounting, retro gate, B01/B02 syntheses, and M2R family coverage audit listed above.

### E4. Current Search evidence for rebaseline only

Read current `docs/seo/serp/**` evidence needed to understand:

- S01 `ии агенты для маркетплейсов` — accepted valid top-20;
- S02 `ии агент для озон` — accepted valid top-20;
- S03 `ии агент для wildberries` — accepted valid top-20;
- current progress and rebaselined query matrix.

Use S01-S03 only to avoid redundant recommendations and preserve already-resolved intent facts. Do NOT pretend M3/M4 are complete.

## F. Core analytical task

Build one reconciled, auditable picture of the currently observed/tested phrase/evidence universe.

You must independently challenge earlier Main Chat family conclusions. Do not merely restate existing analyses because they already say `PASS` or `10/10`.

For every evidence row/string/seed, preserve source lineage and distinguish:

- direct provider `results[]`;
- provider `associations[]`;
- tested seeds;
- totalCount-only seeds;
- successful exact-empty `{}` seeds;
- externally/source-derived task terminology recorded in analyses, which must NOT be silently converted into observed Wordstat phrases.

The final reconciliation must answer:

1. What raw and exact-unique universe do we actually have?
2. What conservative normalized universe do we have?
3. What evidence is observed Wordstat vs source-derived/inferred?
4. Which user jobs are actually represented?
5. Which rows are core-fit, adjacent, boundary, noise or unresolved?
6. What contamination patterns would falsely inflate Octoport demand?
7. Which families still need Search/competitor/Alice rather than more Wordstat?
8. Which representative Search queries now have the highest decision value?

## G. Family model — preserve F1-F10

Use these families without silently redefining them:

- `F1` Existing AI-agent / AI-assistant category;
- `F2` connect user's own AI/LLM to marketplace data/tools;
- `F3` seller-cabinet analytics / reports;
- `F4` daily seller work / helper;
- `F5` operational product-card help;
- `F6` advertising analysis / help;
- `F7` marketplace / search / niche analytics, capability-bounded;
- `F8` procedural knowledge / cabinet help;
- `F9` comparison / discovery of AI/tools;
- `F10` broad-noise controls.

For each family, independently assign one of:

- `STRONG_FOR_SERP`
- `SUFFICIENT_FOR_SERP`
- `PARTIAL_BUT_SERP_CAN_RESOLVE`
- `WORDSTAT_GAP_REMAINS`
- `HOLD`

Do not use family state as a page decision.

## H. Required contamination/adversarial pass

You MUST independently search the complete reconciled corpus for false-positive leakage from:

- human assistant vacancies/hiring;
- human analyst/profession queries;
- accountant / tax / 1C / commissioner / statutory-reporting intent;
- buyer-side Wildberries/Ozon login/orders/search/navigation;
- AI image/photo/infographic/cards-as-pictures generation;
- continuous real-time bidder/autobidder/bid-management tools;
- generic `market`/brand/entity/navigation noise;
- courses/training/how-to content unrelated to tool purchase/use;
- service-brand navigation mistaken for task demand;
- generic AI-assistant demand without seller specificity;
- Ozon/WB capability transfer by analogy;
- unsupported help-center-through-Seller-API claims;
- unsupported mutation/write-back claims.

Do not delete these rows from the ledger. Classify them and preserve lineage.

## I. Capability/source boundaries — HARD

Preserve at least these current facts:

- WB `Поисковые запросы` seller report public-API availability is confirmed by current official evidence;
- WB `Анализ ниш` is confirmed as a native seller report/task, but do not infer full public-API availability from the separate search-query API statement;
- Ozon seller/product search-query analytics is API-confirmed by accepted project evidence;
- Ozon full external market/niche/competitor intelligence is not proven;
- marketplace API data and official help-center procedural knowledge are separate authority classes unless a dedicated knowledge API is explicitly proven;
- ad statistics/analysis may be relevant while 24/7 automated bid management remains a boundary product category;
- WB Content API may support card mutations at API level, but Octoport launch remains read-only/analysis-first;
- product truth outranks competitor/product wording.

If a row has user-demand relevance but current launch addressability is uncertain, classify it as adjacent/HOLD with the correct capability state instead of erasing demand or inventing capability.

## J. Counting rules — MUST NOT BREAK

Do not sum `totalCount` values from overlapping roots into a market-size total.

Report separately:

- raw direct-result occurrence count;
- exact unique direct-result strings;
- association occurrence count;
- exact unique association strings;
- tested seed count;
- exact tested seeds not echoed in arrays;
- totalCount-only seed count;
- exact-empty successful seed count;
- conservative normalized unique count;
- counts by family and fit state, with clear denominator.

`association != candidate keyword`.

`totalCount-only != zero`.

`result:{}` success != zero`.

`SEED != FINAL KEYWORD != INTENT != CLUSTER != PAGE`.

## K. Required output 1 — reconciliation report

Create:

`M2R_FULL_VOLUME_RECONCILIATION_2026-09-17.md`

It must contain:

- execution/base manifest;
- exact input inventory;
- full accounting before/after conservative normalization;
- family-by-family independent conclusions;
- contamination/boundary findings;
- observed-vs-inferred/source-derived distinctions;
- capability boundary findings;
- what M2R corrected relative to old B01/B02;
- what remains unresolved;
- explicit decision on whether any further Wordstat acquisition is justified now;
- readiness verdict for resuming M3 Search;
- no final page architecture.

## L. Required output 2 — phrase lineage ledger

Create:

`M2R_PHRASE_LINEAGE_LEDGER_2026-09-17.csv`

At minimum include columns:

`raw_phrase, normalized_phrase_conservative, source_stage, source_query_id, source_file, evidence_type, observed_count, seed_total_count, region, device, marketplace_scope, task_family_primary, task_family_secondary, intent_class, fit_class, contamination_flags, capability_state, observed_vs_inferred, lineage_notes, reconciliation_reason`

Allowed `fit_class`:

- `CORE_FIT`
- `ADJACENT`
- `BOUNDARY`
- `NOISE`
- `HOLD`

Do not silently remove noise or duplicates. Multiple source rows may point to the same normalized phrase; preserve row lineage or a lossless source-list representation.

## M. Required output 3 — family coverage matrix

Create:

`M2R_FAMILY_COVERAGE_MATRIX_2026-09-17.csv`

At minimum include:

`family_id, family_name, prior_state, reconciled_state, direct_evidence_count, seed_evidence_count, representative_phrases, main_contaminations, capability_boundary, unresolved_decisions, more_wordstat_needed, serp_needed, competitor_needed, alice_needed, rationale`

Do not fabricate numeric evidence when not directly derivable.

## N. Required output 4 — revised representative M3 Search matrix

Create:

`M3_REVISED_REPRESENTATIVE_QUERY_MATRIX_2026-09-17.md`

This is the most important downstream deliverable.

Rules:

1. Preserve these already executed/accepted queries as `CLOSED / EXISTING EVIDENCE`:
   - S01 `ии агенты для маркетплейсов`
   - S02 `ии агент для озон`
   - S03 `ии агент для wildberries`
2. Do not mechanically continue the old S04-S17 sequence.
3. Select the **minimum representative set** of new Search queries needed to resolve distinct remaining intent/page-type/competitor questions across F2-F9.
4. Every proposed query must have:
   - query text;
   - family;
   - state `RUN | HOLD | COVERED_BY_EXISTING_SERP | NOT_NEEDED`;
   - exact open decision;
   - why Wordstat/source evidence is insufficient;
   - what Search outcome would change;
   - contamination/boundary to watch;
   - relationship to other candidate queries (merge/split/control).
5. Do not choose queries solely because their Wordstat number is larger.
6. Do not create one query per phrase.
7. Prefer representative queries that can resolve several related lexical variants without losing a materially different intent.
8. Include explicit controls only when they protect a named decision, e.g. card-generator contamination or autobidder contamination.
9. Do not make final page/URL decisions.
10. End with an execution order based on information gain and dependency, not arbitrary query numbering.

## O. Required output 5 — QA report

Create:

`M2R_RECONCILIATION_QA_2026-09-17.md`

Include hard checks for:

- all expected input classes read;
- all B01/B02 rows accounted for;
- all A01-A19 rows/seeds accounted for;
- direct/association/seed shape preservation;
- totalCount-only preservation;
- exact-empty success preservation;
- silent row loss = 0;
- duplicate lineage retained;
- no additive overlapping-root market-size arithmetic;
- all F1-F10 represented;
- contamination matrix checked;
- product/API/source boundaries checked;
- M3 S01-S03 retained;
- final page architecture absent;
- provider calls executed = 0;
- repository writes/pushes = 0.

Also provide an independent ten-dimension quality score and list every remaining HOLD/limitation.

## P. Required output 6 — output manifest

Create:

`M2R_RECONCILIATION_OUTPUT_MANIFEST_2026-09-17.json`

Include:

- work_id;
- repository;
- branch;
- start live HEAD;
- end observed remote HEAD;
- input file counts by class;
- output file names;
- SHA-256 for every output file;
- output row counts where applicable;
- QA verdict;
- unresolved HOLD count;
- whether remote authority drift occurred;
- timestamp.

## Q. Work artifact handoff rule — mandatory

Do NOT commit, push, force-push, create PR or publish files individually.

When all outputs and QA are complete:

1. create exactly one ZIP:
   `octoport-seo-m2r-reconciliation-2026-09-17.zip`;
2. ZIP must contain exactly the six required final output files and no temporary artifacts;
3. provide the owner a direct downloadable link to the ZIP;
4. final Work report must list:
   - ZIP name;
   - all files inside;
   - exact recommended repository path for each file;
   - whether each file is new or would replace an authority;
   - QA verdict;
   - work completed;
   - work explicitly out of scope;
   - start/end remote HEAD observations;
   - any authority drift.

Recommended repository destination for all six files:

`docs/seo/work/m2r-reconciliation-2026-09-17/`

The owner will relay/upload as needed; Main Chat will perform remote readback and acceptance. Your local completion is not automatically accepted project authority.

## R. Stop / HOLD rules

STOP and return a HOLD instead of guessing if:

- required repository evidence cannot be read completely;
- product truth conflicts with another current governing file;
- a classification depends on missing SERP/competitor/Alice evidence;
- a capability claim is not supported by accepted current authority;
- authority drift changes governing evidence during execution;
- producing a requested table would require sampling or silent row loss.

Do not repair missing evidence with model memory or external general knowledge.

## S. Final execution verdict required from Work

Your final report must end with exactly one of:

`WORK_M2R_RECONCILIATION = PASS / HANDOFF_READY`

or

`WORK_M2R_RECONCILIATION = HOLD / <specific reason>`

PASS means only that the reconciliation artifacts are complete and QA-passed for Main Chat return review. PASS does **not** mean M7 Collection Freeze, M8 semantic master, final clusters or page architecture are approved.

---

Execute the complete task now at full volume. Do not sample. Do not call providers. Do not publish to GitHub.
