# R04 lifecycle analysis — local start

Date: 2026-09-17.  
Query: `аналитика маркетплейсов для селлеров`.  
Job: `octoport-serp-r04-20260917`.  
Status: **START PASS / RAW PERSISTED / REMOTE READBACK PASS / EXACTLY ONE SUBMITN RELEASED**.

## Observed Bridge facts

```text
action = start
ok = true
request_executed = false
provider_calls = 0
control = RUNNING
total = 1
PENDING = 1
SUBMITTING = 0
WAITING = 0
COLLECTING = 0
RESULT_SAVED = 0
SUCCEEDED = 0
PARSE_FAILED = 0
FAILED = 0
UNKNOWN = 0
CANCELLED = 0
requests_started = 0
operations_accepted = 0
polls_started = 0
unresolved = 1
all_successful = false
busy = false
revision = 0
```

## Persistence/readback result

Exact owner-returned envelope is persisted at:

`../raw/R04_01_START_2026-09-17.md`.

Remote branch readback returned the same complete envelope and confirmed the critical fields without drift.

```text
RAW_PERSISTENCE = PASS
REMOTE_READBACK = PASS
JOB_ID_MATCH = PASS
ACTION_MATCH = PASS
REQUEST_EXECUTED_FALSE = PASS
PROVIDER_CALLS_ZERO = PASS
PENDING_ONE = PASS
REQUESTS_STARTED_ZERO = PASS
OPERATIONS_ACCEPTED_ZERO = PASS
POLLS_STARTED_ZERO = PASS
REVISION_ZERO = PASS
FAILURE_UNKNOWN_COUNTS_ZERO = PASS
```

## Gate interpretation

The R04 start matches the accepted local-start pattern exactly:

- one pending item exists;
- no provider request has occurred;
- no operation has been accepted;
- no poll has occurred;
- no failure/unknown/cancelled state exists;
- no retry or second start is justified.

## Released next action

Exactly one submission of the already-created pending item is now released:

```text
SEARCH_ASYNC_BATCH_API_V1 {"action":"submitN","jobId":"octoport-serp-r04-20260917","count":1}
```

The actual returned Bridge envelope remains authority.

## Still blocked

Until the submit response is persisted and remotely read back:

```text
SECOND_START_ALLOWED = false
SECOND_SUBMIT_ALLOWED = false
R04_COLLECTN_ALLOWED = false
R04_EXPORT_ALLOWED = false
R05_ALLOWED = false
```

After submit response:

`persist exact envelope -> remote readback -> verify provider call / operation id / WAITING / revision -> decide collect gate separately`.
