# R03 lifecycle analysis — submit accepted

Date: 2026-09-17.  
Query: `chatgpt для wildberries`.  
Job: `octoport-serp-r03-20260917`.  
Status: **SUBMIT ACCEPTED / RAW PERSISTED / REMOTE READBACK PASS / EXACTLY ONE COLLECTN RELEASED**.

## Observed Bridge facts

```text
action = submitN
ok = true
request_executed = true
provider_calls = 1
processed = 1
normalized = 0
bounded_stop = false
last.outcome = accepted
last.code = null
last.index = 0
operation_id = spr8vij9p1s7cijt2chi
control = RUNNING
total = 1
PENDING = 0
SUBMITTING = 0
WAITING = 1
COLLECTING = 0
RESULT_SAVED = 0
SUCCEEDED = 0
PARSE_FAILED = 0
FAILED = 0
UNKNOWN = 0
CANCELLED = 0
requests_started = 1
operations_accepted = 1
polls_started = 0
unresolved = 1
all_successful = false
busy = false
revision = 2
```

## Persistence/readback result

Exact owner-returned submit envelope is persisted at:

`../raw/R03_02_SUBMIT_2026-09-17.md`.

Remote branch readback returned the same complete envelope and confirmed:

```text
RAW_PERSISTENCE = PASS
REMOTE_READBACK = PASS
JOB_ID_MATCH = PASS
ACTION_MATCH = PASS
REQUEST_EXECUTED_TRUE = PASS
PROVIDER_CALLS_ONE = PASS
OPERATION_ID_MATCH = PASS
WAITING_ONE = PASS
REQUESTS_STARTED_ONE = PASS
OPERATIONS_ACCEPTED_ONE = PASS
POLLS_STARTED_ZERO = PASS
REVISION_TWO = PASS
FAILURE_UNKNOWN_COUNTS_ZERO = PASS
```

## Gate interpretation

The one released R03 submission executed exactly once and was accepted by the provider. The authoritative operation is `spr8vij9p1s7cijt2chi`. The only item is now `WAITING=1`; no provider-backed collection/poll has yet occurred. A second start or submit is forbidden.

## Released next action

Exactly one bounded collect of the accepted operation is now authorized:

```text
SEARCH_ASYNC_BATCH_API_V1 {"action":"collectN","jobId":"octoport-serp-r03-20260917","count":1}
```

If this returns local `NO_DUE_OPERATIONS` with `request_executed:false/provider_calls:0`, preserve it as a timing guard; do not treat it as failure or zero demand. If it performs a provider-backed poll, preserve the exact returned state before any export decision.

## Still blocked

```text
SECOND_START_ALLOWED = false
SECOND_SUBMIT_ALLOWED = false
R03_EXPORT_ALLOWED = false
R04_ALLOWED = false
```

After the collect response:

`persist exact envelope -> remote readback -> verify operation/state/revision/provider counters -> decide next lifecycle action separately`.
