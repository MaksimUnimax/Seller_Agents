# R03 lifecycle analysis — submit accepted

Date: 2026-09-17.  
Query: `chatgpt для wildberries`.  
Job: `octoport-serp-r03-20260917`.  
Status: **SUBMIT ACCEPTED / RAW PERSISTED / REMOTE READBACK REQUIRED BEFORE COLLECT RELEASE**.

## Observed Bridge facts

```text
action = submitN
ok = true
request_executed = true
provider_calls = 1
processed = 1
normalized = 0
bounded_stop = false
last.outcome = accepted
last.code = null
last.index = 0
operation_id = spr8vij9p1s7cijt2chi
control = RUNNING
total = 1
PENDING = 0
SUBMITTING = 0
WAITING = 1
COLLECTING = 0
RESULT_SAVED = 0
SUCCEEDED = 0
PARSE_FAILED = 0
FAILED = 0
UNKNOWN = 0
CANCELLED = 0
requests_started = 1
operations_accepted = 1
polls_started = 0
unresolved = 1
all_successful = false
busy = false
revision = 2
```

## Gate interpretation

The one released R03 submission executed exactly once and was accepted by the provider.

- one provider request was executed;
- exactly one operation was accepted;
- authoritative operation identity is `spr8vij9p1s7cijt2chi`;
- the only item moved from `PENDING` to `WAITING`;
- no collection/poll has occurred yet;
- no failure, parse failure, unknown or cancellation state exists;
- revision advanced from `0` to `2`;
- a second `submitN` or another `start` is forbidden.

## Next gate

Before any collect attempt:

```text
persist exact submit envelope
-> remote readback
-> verify job/action/provider_calls/operation_id/WAITING/revision
-> only then release exactly one bounded collectN count=1
```

Until remote readback passes:

```text
SECOND_START_ALLOWED = false
SECOND_SUBMIT_ALLOWED = false
R03_COLLECTN_ALLOWED = false
R03_EXPORT_ALLOWED = false
R04_ALLOWED = false
```

A later `collectN` may legitimately return local `NO_DUE_OPERATIONS` with `request_executed:false/provider_calls:0`; that is a timing guard, not a semantic failure or zero-result conclusion.
