# SERP analysis — S01-03 start accepted

Дата: 2026-09-16.
Query: `ии агенты для маркетплейсов`.
Job: `octoport-serp-s01-20260916`.
Evidence: `../raw/S01_03_START_2026-09-16.md`.

## Observed facts

- action: `start`;
- `ok: true`;
- `request_executed: false`;
- `provider_calls: 0`;
- job control: `RUNNING`;
- total items: `1`;
- `PENDING: 1`;
- all other item states: `0`;
- `requests_started: 0`;
- `operations_accepted: 0`;
- `polls_started: 0`;
- `unresolved: 1`;
- `all_successful: false`;
- `busy: false`;
- revision: `0`.

## Interpretation

The Search service is now active enough for local Deferred Search admission. The job was created successfully, but no Yandex provider request has yet been performed.

This response is lifecycle evidence only; it contains no SERP results and consumes no Search provider call/cost.

## Next lifecycle action

Issue exactly one bounded `submitN` for this job with `count: 1`.

Exact command shape already used by this bridge lifecycle:

`SEARCH_ASYNC_BATCH_API_V1 {"action":"submitN","jobId":"octoport-serp-s01-20260916","count":1}`

This is the first action expected to perform the provider POST and, on acceptance, bind an operation id and transition the item out of `PENDING` (typically to `WAITING`). Persist and verify its complete returned envelope before any `collectN`.
