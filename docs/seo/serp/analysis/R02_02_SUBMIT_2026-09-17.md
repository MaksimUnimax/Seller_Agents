# R02 lifecycle analysis — submit accepted

Date: 2026-09-17.  
Query: `chatgpt для ozon`.  
Job: `octoport-serp-r02-20260917`.  
Status: **SUBMIT ACCEPTED / RAW PERSISTED / REMOTE READBACK PASS / EXACTLY ONE COLLECTN RELEASED**.

## Observed Bridge facts

```text
action = submitN
ok = true
request_executed = true
provider_calls = 1
processed = 1
normalized = 0
bounded_stop = false
last.outcome = accepted
last.code = null
last.index = 0
operation_id = sprg1vmblbk160ogsha3
control = RUNNING
total = 1
PENDING = 0
SUBMITTING = 0
WAITING = 1
COLLECTING = 0
RESULT_SAVED = 0
SUCCEEDED = 0
PARSE_FAILED = 0
FAILED = 0
UNKNOWN = 0
CANCELLED = 0
requests_started = 1
operations_accepted = 1
polls_started = 0
unresolved = 1
all_successful = false
busy = false
revision = 2
```

## Persistence/readback result

Exact owner-returned submit envelope was persisted at:

`../raw/R02_02_SUBMIT_2026-09-17.md`.

Remote branch readback returned the same complete envelope and confirmed:

```text
RAW_PERSISTENCE = PASS
REMOTE_READBACK = PASS
JOB_ID_MATCH = PASS
ACTION_MATCH = PASS
REQUEST_EXECUTED_TRUE = PASS
PROVIDER_CALLS_ONE = PASS
PROCESSED_ONE = PASS
OPERATION_ID = sprg1vmblbk160ogsha3
WAITING_ONE = PASS
REQUESTS_STARTED_ONE = PASS
OPERATIONS_ACCEPTED_ONE = PASS
POLLS_STARTED_ZERO = PASS
REVISION_TWO = PASS
FAILURE_UNKNOWN_COUNTS_ZERO = PASS
```

## Gate interpretation

The one released submission executed exactly once and was accepted by the provider.

- one provider request was executed;
- exactly one operation was accepted;
- authoritative operation identity is `sprg1vmblbk160ogsha3`;
- the item moved from `PENDING` to `WAITING`;
- no collection/poll has occurred yet;
- no failure, parse failure, unknown or cancellation state exists;
- revision advanced from `0` to `2`;
- another `submitN` or another `start` is forbidden.

## Released next action

Exactly one bounded collection attempt against this existing job is now released:

```text
SEARCH_ASYNC_BATCH_API_V1 {"action":"collectN","jobId":"octoport-serp-r02-20260917","count":1}
```

If the Bridge returns `NO_DUE_OPERATIONS` with `request_executed:false/provider_calls:0`, preserve it as a local timing guard; do not treat it as provider failure, zero results or permission to resubmit.

## Still blocked

Until the collect response is persisted and remotely read back:

```text
SECOND_START_ALLOWED = false
SECOND_SUBMIT_ALLOWED = false
ADDITIONAL_COLLECT_ALLOWED = false
R02_EXPORT_ALLOWED = false
R03_ALLOWED = false
```

After the complete collect response:

`persist exact envelope -> remote readback -> inspect WAITING/SUCCEEDED/provider_calls/revision -> decide next bounded lifecycle action separately`.
