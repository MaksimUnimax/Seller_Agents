# Wordstat analysis — B02-03

Дата: 2026-09-16.
Seed: `ии для продаж на маркетплейсах`.
Observed `totalCount`: `23`.

## Interpretation

The phrase is directly product-adjacent: it expresses use of AI in marketplace selling rather than generic AI content generation. However, the observed volume is low and the provider returned only the seed phrase itself; no narrower product-fit child vocabulary was exposed.

The association list is not useful for semantic expansion: it is dominated by unrelated marketplace/market brand and generic buy/sell noise. None of those association counts should be attributed to the Octoport-relevant intent.

## SEO consequence

- Keep `ии для продаж на маркетплейсах` as a secondary product-fit phrase.
- Do not create a standalone target page from this count alone.
- Compare it later against the stronger agent/assistant and analytics clusters.
- Require SERP verification before assigning any final page role.
- Do not treat `23` as evidence for a broader `продажи на маркетплейсах` market size.

Status: `KEEP_SECONDARY / SERP_REQUIRED`.
