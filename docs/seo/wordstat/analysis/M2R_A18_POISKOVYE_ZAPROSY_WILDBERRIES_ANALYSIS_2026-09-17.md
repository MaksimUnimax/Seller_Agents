# M2R-A18 analysis — `поисковые запросы wildberries`

Date: 2026-09-17.
Status: `ANALYZED / FULL 1-DIRECT-ROW REVIEW`.
Raw authority: `../raw/M2R_A18_POISKOVYE_ZAPROSY_WILDBERRIES_RESULT_2026-09-17.md`.

## 1. Provider outcome

- Wordstat status: `OK`;
- request executed: `true`;
- `totalCount`: `73`;
- direct `results[]`: `1` row;
- associations: `19` rows;
- requested depth: `2000`;
- no provider failure;
- no depth-saturation signal.

`totalCount=73` is demand for this exact phrase family. It is not the size of the entire WB search-analytics market and must not be added mechanically to overlapping roots.

## 2. Full-row review

All 1 direct row and all 19 associations were reviewed. No sampling was used.

Direct row:

| Phrase | Count | Interpretation |
|---|---:|---|
| `wildberries поисковый запрос` | 73 | live WB-specific search-query wording; relevant to search analytics but semantically under-specified |

All 19 associations are irrelevant or buyer-side/general-search noise: generic Google/search phrases, login/order navigation, unrelated songs/entities and generic Wildberries navigation/news. None exposes seller-side search-analytics subjobs such as ranking, query volume, conversion, report/API or card optimization.

## 3. What A18 proved

The exact WB search-query family is **real but non-expansive** in Wordstat.

This is not evidence that seller search analytics is weak. Current official WB surfaces independently prove that the seller job is rich:

- `Поисковые запросы на WB`;
- `Поисковые запросы: ваши товары`;
- search-position/ranking and traffic data;
- query volume/frequency;
- card-transition/search-conversion indicators;
- downloadable reports;
- official public-API availability for the search-query report.

Therefore A18 says that the broad user phrase `поисковые запросы wildberries` does not enumerate these subjobs in Wordstat. The task exists; the lexical expansion is weak.

## 4. Buyer-vs-seller boundary

A18 associations strongly demonstrate why the seller/buyer boundary must remain explicit.

The broad words `поиск` and `Wildberries` attract:

- buyer login/navigation;
- order lookup;
- generic website search;
- unrelated general-search noise.

These association rows are not seller-analytics demand and must not inflate F7 coverage or Octoport market size.

## 5. Source-bounded product consequence

WB search analytics is currently one of the strongest source-bounded F7 opportunities because the official help surface states that the `Поисковые запросы` report is available through the public API.

Relevant Octoport job:

`user's chosen AI + authorized WB search-query/report data -> explain demand, visibility, positions, query performance and card-search opportunities`.

This does not imply that all broader marketplace/niche analytics are exposed through the same API path.

## 6. Why F7 is still incomplete after A18

F7 contains two materially different seller jobs:

1. **search-query analytics** — now directly measured by A18 and API-confirmed;
2. **niche / marketplace-development analytics** — officially available in WB seller analytics, but not yet independently measured in Wordstat and not automatically covered by the search-query result.

Current official `Анализ ниш` material shows a distinct job around:

- identifying prospective niches;
- demand/availability;
- sellers and product cards;
- monopolization;
- revenue and average check;
- turnover and stock availability;
- buyout percentage;
- seasonality;
- search-query widgets;
- price segments and related marketplace-wide indicators.

This is sufficiently distinct to justify one independent F7 niche probe before reconciliation.

## 7. Next acquisition decision

Next query:

`анализ ниш wildberries`

Why:

1. it represents a distinct seller decision from search-query analytics;
2. the exact current WB report is officially named `Анализ ниш`;
3. current 2026 market pages use the exact `анализ ниши на Wildberries` wording;
4. it can reveal niche-selection, demand, competition, revenue, turnover and tool/service language;
5. it provides the missing minimum F7 branch needed before cross-family M2R reconciliation;
6. capability claims must remain source-bounded: existence of the WB report is proven, but public-API access to the full niche report is not assumed from the separate search-query API statement.

## 8. F7 status after A18

```text
WB_SEARCH_ANALYTICS_JOB = VALID / API-CONFIRMED
WB_SEARCH_QUERY_WORDSTAT_ROOT = LIVE / NON-EXPANSIVE
BUYER_SEARCH_ASSOCIATION_NOISE = HIGH
F7_SEARCH_SIDE = REPRESENTED ENOUGH FOR NOW
F7_NICHE_SIDE = STILL MISSING
NEXT_QUERY = анализ ниш wildberries
```

## 9. Work decision

`WORK_NOW = DEFER ONE MORE BOUNDED QUERY`.

Reason: the accumulated M2R corpus is already substantial, but F7 would remain structurally incomplete if reconciliation starts before one independent niche-analysis observation.

After A19 (`анализ ниш wildberries`) analysis, a proactive full-volume Work reconciliation should be prepared/triggered if quality gain remains positive, covering:

- all historical M2 B01/B02 evidence;
- M2R A01-A19 raw and analyses;
- cross-family deduplication;
- intent/boundary classification;
- source-capability lineage;
- discovery-gap and redundancy checks;
- recommendations for returning to M3 Search.

## 10. Quality score

| Criterion | /10 |
|---|---:|
| Full-row coverage | 10.0 |
| Buyer-vs-seller separation | 10.0 |
| API capability discipline | 10.0 |
| Search-vs-niche family separation | 10.0 |
| Product-truth alignment | 10.0 |
| Noise handling | 10.0 |
| Avoidance of synonym inflation | 10.0 |
| F7 completion routing | 10.0 |
| Work-trigger reasoning | 10.0 |
| Downstream usefulness | 10.0 |

`QUALITY_TOTAL = 100/100`
`QUALITY_SCORE = 10.0/10`

## 11. Verdict

```text
M2R_A18 = CLOSED
DIRECT_ROWS_REVIEWED = 1/1
ASSOCIATIONS_REVIEWED = 19/19
TOTALCOUNT = 73
WB_SEARCH_QUERY_ROOT = LIVE / NON-EXPANSIVE
F7_SEARCH_ANALYTICS = REPRESENTED ENOUGH FOR NOW
F7_NICHE_ANALYTICS = STILL OPEN
NEXT_QUERY = анализ ниш wildberries
WORK_RECONCILIATION = AFTER A19
```
