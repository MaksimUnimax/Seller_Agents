# Analysis — B01-04 `аналитика маркетплейсов с ии`

Дата: 2026-09-16.
Raw source: `../raw/B01_04_2026-09-16.md`.

## Observation

Yandex Wordstat `getTop` returned `totalCount=13` for the exact seed `аналитика маркетплейсов с ии` in region `225`, `DEVICE_ALL`.

The supplied result did not include a `results` array, therefore this call gives no child-query vocabulary to expand from. No child rows are inferred.

## SEO interpretation

- Exact phrase is low-volume.
- This weakens the hypothesis that users commonly formulate the product category specifically as `аналитика маркетплейсов с ии`.
- It does **not** weaken the broader analytics intent itself. The previous B01-01 result contained `ии для аналитики маркетплейсов` with count `15`, which indicates the same concept may be expressed with different word order.
- Analytics remains product-adjacent and should be investigated via marketplace-specific and task-specific queries rather than promoted to a primary category phrase on current evidence.

## Routing impact

No page route is assigned from this call alone.

Status:

- category-head candidate: `WEAK / HOLD`;
- analytics intent family: `KEEP_FOR_DISCOVERY`;
- separate landing page: `NOT_DECIDED`.

## Next action

Proceed to B01-05 `ии для ozon` to test marketplace-specific language, which may reveal substantially cleaner demand than generic marketplace + AI terms.
