# Wordstat analysis — B02-06

Дата: 2026-09-16.
Seed: `нейросети для менеджеров маркетплейсов`.
Provider totalCount: `19`.

## Observation

Wordstat returned only the seed phrase itself in `results`: `нейросети для менеджеров маркетплейсов` — `19`. No product-specific child query was exposed.

The `associations` set is dominated by generic neural-network/free-tool/course vocabulary and unrelated marketplace noise. In particular, `курсы по ии и нейросетям`, `курсы по нейросетям`, `ии нейросеть бесплатно` and other generic AI phrases indicate that the role phrase can bleed toward education/tool-discovery intent rather than a clean commercial SaaS intent.

## Product-fit interpretation

The phrase is semantically relevant because Octoport is aimed at seller/marketplace work, but the observed demand is small and the surrounding association vocabulary is noisy. Treat it as a secondary role-oriented phrase, not as evidence for a dedicated landing page.

Do not infer that `19` represents all demand from marketplace managers; it is only the observed Wordstat count for this wording in the requested region/device scope.

## Next-step consequence

Keep the manager-role vocabulary in the semantic universe, but defer any target-page assignment until the full B02 normalization and SERP verification. Continue with marketplace-specific agent wording, beginning with `ии агент для озон`, which was a stronger product-fit child phrase observed in Batch 01.
