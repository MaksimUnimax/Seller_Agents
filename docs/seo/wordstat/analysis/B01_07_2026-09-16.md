# Wordstat analysis — B01-07

Seed: `ии для wildberries`.
Status: `ANALYZED AFTER RAW PERSIST`.

## Observed facts

- totalCount: `225`;
- exact seed string itself is not returned as a visible `results` row in the supplied envelope;
- returned rows are:
  - `ии для создания карточек товара на wildberries` — `153`;
  - `ии агент для wildberries` — `20`;
  - `инфографика для wildberries ии` — `10`.

## Interpretation

The English marketplace spelling `wildberries` shows lower volume than the Russian Ozon spelling pass and is still heavily contaminated by content-card creation. However, `ии агент для wildberries` appears as a product-adjacent phrase with count `20`, which is materially closer to Octoport's product model than content-generation phrases.

This is evidence for keeping an `AI agent/assistant × marketplace` vocabulary family alive for Batch 02. It is not yet evidence for a dedicated landing page, because the current seed is sparse and needs Russian-spelling comparison plus SERP verification later.

## Routing impact

- do not assign HOME to `ии для wildberries` yet;
- keep `ии агент для wildberries` as `WORDSTAT_OBSERVED / REVIEW`;
- continue with Russian spelling `ии для вайлдберриз` as planned B01-08;
- later compare Wildberries spellings and agent/assistant variants before creating any marketplace-specific target.

## Parallel-development safety

This analysis belongs only to `docs/seo/**` in branch `seo/wordstat-batch-01-2026-09-16`. It does not authorize changes to server, extension, contracts, migrations, or site runtime while those lines are being developed in parallel.
