# Wordstat analysis — B02-01

Дата: 2026-09-16.
Seed: `ии агенты для маркетплейсов`.
Raw evidence: `../raw/B02_01_2026-09-16.md`.

## Observed facts

- provider status: `OK`;
- HTTP: `200`;
- `totalCount`: `134`;
- `results` contains exactly one row: `ии агенты для маркетплейсов` — `134`;
- no child phrase expansion was returned beyond the seed itself;
- returned `associations` are largely unrelated lexical noise around `agent` / `market` and are not suitable as product semantic expansion.

## Interpretation

This confirms that the phrase `ии агенты для маркетплейсов` has observable Wordstat demand and is materially closer to Octoport's actual product model than the broad roots `ии для маркетплейсов` / `нейросеть для маркетплейсов`, which were dominated by card, image and infographic generation intent.

However, this one call does **not** prove that the phrase should become a primary landing-page target. The result is narrow and has no useful child vocabulary. It remains a strong product-fit category hypothesis that must be compared with the other B02 task/role/marketplace formulations and later checked against live SERP intent.

## Classification

- evidence class: `WORDSTAT_OBSERVED`;
- relevance: `HIGH_PRODUCT_FIT`;
- intent hypothesis: `CATEGORY / COMMERCIAL-INVESTIGATIVE`;
- page-role decision: `HOLD_FOR_B02_SYNTHESIS_AND_SERP`;
- do not use unrelated `associations` as semantic-core candidates.
