# Wordstat analysis — B02-14

Дата: 2026-09-16.
Call: `B02-14`.
Seed: `какой ии для маркетплейсов`.
Source: `../raw/B02_14_2026-09-16.md`.

## Provider facts

- status: `OK`;
- HTTP: `200`;
- `totalCount`: `128`;
- exact result row: `какой ии для маркетплейсов` — `128`;
- child result rows: none;
- associations returned: `16`;
- estimated call cost: `0.02 ₽`.

## Interpretation

The seed has materially more volume than most narrow product-adjacent Batch 02 phrases, but `getTop` did not expose any child query vocabulary that clarifies what users mean by “какой ИИ”. The association list is mostly unrelated marketplace/name noise and therefore cannot be used as evidence that the observed `128` searches specifically seek an AI agent, seller assistant, analytics tool, or Octoport-like product.

This is a broad comparison/discovery-style phrase. It is relevant enough to retain for later SERP verification because the wording can plausibly surface users choosing between AI tools for marketplace work, but Wordstat alone does not establish product-fit intent.

## SEO handling

- keep in semantic universe: **yes**;
- current role: broad informational/comparison candidate;
- product-fit confidence from Wordstat alone: **unresolved / mixed**;
- standalone landing page decision: **defer**;
- required next validation: SERP intent inspection after Batch 02 completion;
- do not treat `128` as direct Octoport demand without SERP evidence.
