# Wordstat analysis — B01-05

Seed: `ии для ozon`.
Raw evidence: `../raw/B01_05_2026-09-16.md`.

## Observed facts

- exact/top phrase `ии для ozon`: `128`;
- единственный дочерний result: `ии видео для ozon`: `20`;
- provider `totalCount`: `128`;
- associations почти полностью состоят из общего Ozon-навигационного/брендового шума и не используются как product demand evidence.

## Interpretation

`SEO_FINDING`: marketplace-specific формулировка `ии для ozon` заметно чище broad-запросов `ии/нейросеть для маркетплейсов`: Wordstat не развернул её в большой массив карточек/инфографики. Это делает фразу потенциально релевантной для marketplace landing или secondary coverage.

Однако `128` нельзя автоматически считать спросом именно на продукт класса Октопорта. Без SERP/соседних запросов не доказано, что пользователи имеют в виду ассистента для работы с кабинетом, а не генерацию контента, рекомендации по продажам или другой AI use case.

Дочерний `ии видео для ozon` (`20`) относится к content-generation intent и не является целевой capability Октопорта на старте.

## Decision

- сохранить `ии для ozon` в `WORKING / COMMERCIAL_MARKETPLACE` как предварительный status;
- не назначать URL/Title/H1 до проверки русского написания `ии для озон` и SERP;
- `ии видео для ozon` пометить как `EXCLUDED_UNSUPPORTED_CONTENT_GENERATION` для продуктовой посадки, сохраняя как рыночный факт;
- следующий B01-06 обязателен: `ии для озон`, чтобы проверить языковую форму и возможный более широкий русский словарь.
