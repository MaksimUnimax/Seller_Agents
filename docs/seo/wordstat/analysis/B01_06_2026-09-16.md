# Wordstat analysis — B01-06

Call: `B01-06`.
Seed: `ии для озон`.
Raw evidence: `../raw/B01_06_2026-09-16.md`.

## Observed facts

- provider `totalCount`: `972`;
- top returned phrase: `ии для озона` — `972`;
- content-creation intent dominates the returned rows:
  - `ии для карточек озон` — `548`;
  - `карточка товара для озон ии` — `229`;
  - `ии для создания карточек на озон` — `137`;
  - `ии для создания карточек товара на озон` — `90`;
  - `ии фото для озона` — `70`;
  - `ии для инфографики озон` — `28`;
- product-adjacent rows:
  - `ии агент для озон` — `40`;
  - `ии ассистент для озон` — `10`.

## Interpretation

`SEO_FINDING`: кириллический Ozon-вариант открывает существенно больший observable demand, чем B01-05 (`ии для ozon`, totalCount `128`), но этот объём нельзя считать спросом на Октопорт: большая часть выдачи относится к созданию карточек, изображений и контента.

Важное отличие от generic-marketplace seeds: внутри Ozon-словаря появился явный `agent/assistant` vocabulary (`40` + `10` как отдельные observed phrases). Это ближе к продуктовому позиционированию Октопорта, чем content-generation intent, и должно попасть в Batch 02 как отдельная targeted family.

## Routing consequence

- не назначать `972` как volume для будущей Ozon landing page;
- сохранить Ozon marketplace page как сильную гипотезу, но её primary query ещё не выбран;
- добавить в targeted follow-up candidates:
  - `ии агент для озон`;
  - `ии ассистент для озон`;
  - возможные грамматические/брендовые варианты после объединения Wordstat vocabulary;
- до SERP verification не утверждать, что `ии для озона` имеет коммерческий intent именно на seller-work assistant.

## Comparison with B01-05

B01-05 (`ии для ozon`) вернул totalCount `128`, тогда как B01-06 (`ии для озон`) привёл к `ии для озона` с `972`. Это подтверждает необходимость отдельно учитывать русский пользовательский словарь и морфологию, а не считать латинское `Ozon` единственным каноническим SEO-вариантом.
