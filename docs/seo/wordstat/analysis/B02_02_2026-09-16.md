# Wordstat analysis — B02-02

Дата: 2026-09-16.
Seed: `ии для работы с маркетплейсами`.
Observed totalCount: `39`.

## Observed structure

- `ии для работы с маркетплейсами` — `39`;
- `ии для работы с маркетплейсами обучение` — `10`.

The returned associations are predominantly unrelated/noisy and are not treated as product vocabulary.

## Interpretation

This phrase is product-adjacent and materially cleaner than the broad `ии для маркетплейсов` root, but it is not a pure commercial-software query. The child `... обучение` shows that part of the intent can be educational: users may be looking for courses or instructions on using AI in marketplace work rather than for a product that connects AI to marketplace data.

For Octoport this wording remains relevant as a secondary category/task phrase because it directly describes using AI in marketplace work, but the observed volume (`39`) is modest and does not by itself justify a standalone landing page.

## Decision

- keep in the product-fit semantic universe;
- mark intent as `MIXED: PRODUCT/TASK + EDUCATIONAL`;
- do not assign a final target page yet;
- require later SERP verification before choosing whether it belongs on the main product page, an explanatory page, or informational content;
- continue targeted expansion rather than treating `39` as clean transactional demand.
