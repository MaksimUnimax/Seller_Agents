# Wordstat analysis — B02-07

Дата: 2026-09-16.
Seed: `ии агент для озон`.
Observed totalCount: `40`.

## Interpretation

Это наиболее прямой Ozon-specific product-fit запрос из текущей выборки: формулировка `ии агент` соответствует модели продукта заметно лучше, чем широкий корень `ии для озон`, который в B01 был сильно загрязнён карточками, фото и инфографикой.

При этом `getTop` не дал дочернего тематического расширения: единственная строка `results` — исходная фраза с count `40`. `associations` в основном состоят из общего Ozon-навигационного и информационного шума и не используются как доказательство продуктового интента.

## SEO status

- relevance: `HIGH_PRODUCT_FIT`;
- demand size: `MODEST`;
- marketplace: `OZON`;
- intent hypothesis: коммерческое/solution-seeking, но требует SERP-проверки;
- page decision: `HOLD_FOR_SERP`, не создавать отдельную страницу только по Wordstat count;
- comparison note: фраза выглядит качественнее по соответствию продукту, чем широкий `ии для озон`, несмотря на меньший объём.

## Next use

После завершения B02 включить `ии агент для озон` в приоритетную marketplace-specific SERP-проверку вместе с WB-аналогом и общим `ии агенты для маркетплейсов`.
