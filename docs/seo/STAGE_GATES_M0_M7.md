# Octoport SEO — transferred KW-002 gates for M0–M7

Status: **ACTIVE / CURRENT STEP-GATE AUTHORITY**.
Date: 2026-09-16.

Purpose: make the already-earned KW-002 Step00–06 controls local and executable for Octoport. This document does not copy Kwork commercial-delivery mechanics. It transfers the method gates that protect evidence quality.

## Gate 0 — scope / source / product-truth freeze

Maps to Octoport `M0`.

Before dependent research, freeze:

```text
product definition
launch capability boundary
marketplaces
language/geography
commercial/product goal
known exclusions
site state
allowed source authorities
parallel-work isolation boundary
```

Rules:

- later search demand cannot invent product capability;
- material product/source corrections are versioned;
- affected downstream decisions are explicitly reopened;
- superseded product/source facts remain history, not current authority.

Current Octoport state: `PASS` via `PRODUCT_TRUTH.md` and current product authorities, subject to future explicit product revisions.

## Gate 1 — factual software capability / user-task model

Maps to factual-model portion of `M0/M1`.

For Octoport, the business model is not SKU taxonomy. Maintain a neutral model of:

```text
marketplace = Ozon | Wildberries
product interaction = external web AI/LLM <-> extension <-> marketplace API
launch action boundary = read-only / reports / data retrieval
user control = explicit execution
credential/storage boundary = product truth
multi-store/product mechanics = only where accepted
user roles = evidence-supported, not invented personas
supported jobs = only product-supported or explicitly future/HOLD
```

Unsupported action intent can remain market evidence but cannot become a commercial promise.

Current state: `PASS BASELINE`, expandable only by explicit product truth.

## Gate 2 — seed / acquisition probe quality

Maps to Octoport initial `M2` Wordstat discovery planning.

Hard rules:

```text
SEED != FINAL KEYWORD
SEED != FINAL INTENT
SEED != PAGE
BUSINESS/CAPABILITY COVERAGE != SEARCH-LANGUAGE COVERAGE
```

Each material probe must have:

- purpose / question to resolve;
- source lineage;
- expected information gain;
- noise/ambiguity expectation;
- role: broad control / qualified refinement / marketplace-specific / task / integration / comparison / noise control;
- region/device/provider mode;
- claim boundary.

High-noise broad phrases cannot alone prove category coverage. Use bounded refinements/controls rather than combinatorial query multiplication.

### Retrospective Octoport status

The B01/B02 seed logic already covered independent category, marketplace, task, LLM-pairing, analytics, integration and problem axes, and B02 was derived from observed product-adjacent B01 vocabulary. However the original seed tables were created before this stricter local gate existed.

Therefore:

```text
B01_B02_PROVIDER_EVIDENCE = PRESERVED
SEED_QUALITY_RETRO_AUDIT = REQUIRED BEFORE M7 FREEZE
NO PROVIDER REPLAY JUST FOR MISSING DOCUMENTATION
```

The retro audit must verify purpose/info-gain/noise/refinement coverage and record any genuine acquisition gaps.

## Gate 3 — Wordstat depth justification

Applies to original and any reopened Wordstat acquisition.

Before any **new** material Wordstat call:

```text
exact information question
current official provider depth contract
candidate depth options
selected depth + rationale
what shallower option may miss
boundary/truncation interpretation
expansion trigger
request/cost effect
```

Forbidden:

```text
provider maximum = automatic correct depth
chat convenience = depth rationale
previous job value = current justification
```

If justified evidence is large, use Work; do not reduce depth to fit chat.

### Retrospective B01/B02 status

Historical B01/B02 used `numPhrases=2000`. The calls remain valid observations; they are not replayed merely because a formal depth artifact was absent. Before M7, run a retrospective depth/coverage audit:

- identify whether any result hit a material request boundary;
- determine whether B01/B02 + later Search/competitor/Alice evidence expose missing lexical families;
- reopen only named gaps with a fresh depth contract.

## Gate 3R — full Wordstat RAW persistence

Hard order after every Wordstat response:

```text
receive complete envelope
-> save complete factual body
-> save provenance
-> remote readback
-> verify IDs/fields/rows
-> only then next Wordstat provider call
```

Preserve all returned result/association rows and error/empty payload truth. Raw duplicates are evidence and are not deleted during storage.

### Retrospective Octoport persistence audit

Current known history:

- B02 raw evidence was collected under the strict full-envelope rule;
- B01-15 was strict full-envelope;
- earlier B01 artifacts require a dedicated persistence audit because some were created before the strict envelope rule and may be structured transcriptions rather than byte/field-complete envelopes.

Therefore:

```text
M2 WORDSTAT EVIDENCE STATUS = USABLE BUT PERSISTENCE RETRO-AUDIT OPEN
DO NOT CLAIM ALL B01 RAW IS EXACT-ENVELOPE UNTIL VERIFIED
```

Recovery priority:

1. verify every existing B01 raw artifact against available source evidence;
2. backfill from durable/chat/file evidence where complete source survives;
3. if exact historical envelope cannot be reconstructed, record the specific evidence limitation;
4. do not blindly replay provider calls solely to make documentation prettier;
5. replay only if the missing evidence is material to a current unresolved decision and a new information-gain/provider release authorizes it.

## Gate 3A — normalization / safe deduplication

For consolidated semantic processing:

```text
preserve raw text
conservatively normalize case/whitespace
stable normalized identity
collapse exact duplicates analytically
preserve every source/occurrence lineage
implicit merge only at high confidence
uncertain equivalence -> separate/HOLD
```

Hard gates:

```text
RAW_LINEAGE_LOSS = 0
SILENT_ROW_LOSS = 0
ORIGINAL_TEXT_RECOVERABLE = true
```

### Octoport execution

Full cross-source normalization is intentionally deferred to Work after M7 Collection Freeze so the whole evidence set can be processed consistently. New targeted Wordstat evidence must still be minimally lineage-tagged and cannot be injected directly as accepted semantic truth.

## Gate 3B — conservative sanitation

Asymmetric rules:

```text
CLEAR OFF-TOPIC -> EXCLUDE + reason
DIRECT PRODUCT-SUPPORTED -> KEEP CANDIDATE
MATERIAL AMBIGUITY -> HOLD
LOW FREQUENCY ONLY -> never exclude
HIGH FREQUENCY ONLY -> never keep
```

Mandatory collision controls for Octoport include:

- buyer/consumer marketplace intent vs seller intent;
- card/image/infographic generation vs operational/data agent intent;
- courses/jobs/training vs software/tool intent;
- marketplace built-in AI vs third-party product;
- external market analytics vs seller-owned/internal data analytics;
- developer-only API intent vs user-facing integration intent;
- unsupported mutation/automation vs current read-only product promise;
- generic AI/agent meanings unrelated to marketplaces.

Mechanical counts do not substitute for independent semantic QA.

### Octoport execution

Final full-volume sanitation occurs in Work W1 after M7. During M2–M6 collection, only bounded provisional noise/relevance classifications may guide which evidence gaps deserve further collection; they are not final semantic states.

## Gate 4 — preliminary family/task triage

Purpose: reveal current demand/task structure and acquisition gaps; **not** decide pages.

Hard boundary:

```text
PRELIMINARY FAMILY != FINAL INTENT
PRELIMINARY FAMILY != SERP CLUSTER
PRELIMINARY FAMILY != PAGE
PRELIMINARY FAMILY != IA
```

For materially large/rule-heavy data, Work must run an independent family-coherence diagnostic that does not simply replay the production taxonomy.

Large generic/residual families require heterogeneity review for hidden coherent tasks.

Coverage gaps are hypotheses, not provider authorization.

### Octoport state

B01/B02 synthesis provides preliminary families only. Final family authority is not frozen before M7/M8.

## Gate 5 — targeted expansion / information-gain control

Applies to B02 and every future M6 acquisition gap.

Before a new provider call:

```text
name exact unresolved question
-> check all current durable evidence
-> separate owner/product fact from search-demand fact
-> prove new information gain
-> define meanings of success/valid-zero/failure/unknown
-> justify operator/depth/scope
-> define stop/reopen rule
-> execute
-> persist/readback
-> normalize/sanitize before accepted semantic union
```

Hard distinctions:

```text
VALID ZERO != TECHNICAL FAILURE
NO RETRY != NEGATIVE EVIDENCE
DEPTH BOUNDARY != MARKET COMPLETENESS
BROAD EVIDENCE != ANSWER TO NARROW QUESTION
TEMPORAL SNAPSHOT != PERMANENT TRUTH
```

### B02 retrospective status

B02 was purpose-built from B01 observed product-adjacent phrases and therefore has a clear incremental-information rationale. It still receives a formal retrospective Gate-5 audit before M7; no B02 replay is implied.

## Gate 6 — current ordinary Yandex SERP / search-competitor discovery

Maps to `M3` and feeds `M4`.

Purpose:

```text
find current result/page types
resolve intent ambiguity
find recurring real search competitors
collect page evidence for later semantic expansion/page ownership
```

Rules:

```text
BUSINESS RIVAL != SEARCH COMPETITOR
TOP20 DISCOVERY != FULL MARKET COVERAGE
ONE APPEARANCE != ACCEPTED RECURRING COMPETITOR
RECURRENCE != AUTOMATIC PAGE DECISION
```

For Deferred Search:

```text
release bounded query/job
-> local start
-> exact one submit unless a separately accepted batch contract exists
-> persist operation identity
-> respect due-time guard
-> collect existing operation, never blind resubmit
-> export complete required normalized rows
-> persist/hash/readback
-> close query
-> only then release next provider action
```

Current YMB popup/state UI bugs do not override lifecycle evidence; command/provider state and durable export evidence remain authority.

### M3 Work boundary

Ordinary chat may orchestrate bounded individual Search jobs while the result volume is small and fully preserved. Once cross-query comparison/recurrence/overlap requires large complete multi-file analysis, the `WORK_HANDOFF_RULE` triggers.

## Gate 6C — competitor page corpus

Maps to M4.

Competitor pages are collected only from current SERP-derived candidates or justified adjacent evidence. For every page preserve URL/query/rank lineage.

Competitor content may provide:

- terminology;
- page type/format;
- feature/task coverage;
- proof/trust patterns;
- missing topic hypotheses.

It does **not** directly prove Wordstat demand or authorize copying content.

Large page corpora go to Work for full-volume extraction/systematization rather than first-N sampling.

## Gate 6A — Alice/AI-search preparation boundary

M5 may create only a provisional AI diagnostic hypothesis register. **Alice/GenSearch provider acquisition is forbidden before a frozen Search-only page/IA baseline exists.**

Correct sequence:

```text
SEARCH-SIDE FREEZE
-> SEARCH-ONLY SEMANTIC MASTER
-> SEARCH-ONLY CLUSTERING
-> SEARCH-ONLY QUERY->PAGE / IA BASELINE
-> FINAL AI CASE SELECTION
-> ALICE EVIDENCE
-> CHANGE | ENRICH | DE-RISK | NO-CHANGE | HOLD
```

Alice evidence never retroactively rewrites the Search-only baseline. It produces an explicit later delta or NO_CHANGE.

## M7 — Collection Freeze acceptance

Before final semantic master / clustering / page ownership begins, verify:

```text
PRODUCT_SCOPE_CURRENT = PASS
WORDSTAT_PERSISTENCE_RETRO_AUDIT = PASS | EXPLICIT_LIMITATION_ACCEPTED
WORDSTAT_DEPTH/COVERAGE_RETRO_AUDIT = PASS
SEED/INFO_GAIN_RETRO_AUDIT = PASS
REPRESENTATIVE_SERP_MATRIX = COMPLETE FOR DECISIONS
M3_CONTROL_DEBT = CLOSED
M3_FULL_SERP / DEVICE / REGION / TEMPORAL_CONTROLS = ACCEPTED OR EXPLICIT HOLD
M3_CROSS_QUERY_OVERLAP_QA = PASS
SEARCH_COMPETITOR_REGISTRY = STABLE ENOUGH
RELEVANT_COMPETITOR_CORPUS = CAPTURED
AI_DIAGNOSTIC_HYPOTHESIS_REGISTER = PREPARED
ALICE_PROVIDER_EVIDENCE_USED_IN_SEARCH_FREEZE = 0
OPEN_HIGH_VALUE_ACQUISITION_GAPS = 0
OUTCOME_UNKNOWN_PROVIDER_ACTIONS = 0
REQUIRED EVIDENCE DURABLE/READBACK = PASS
WORK_TRIGGER_FOR_POST_FREEZE_FULL-VOLUME_ANALYSIS = EVALUATED
```

Only then:

```text
EVIDENCE_COLLECTION_COMPLETE = true
M8_SEMANTIC_MASTER_ALLOWED = true
```

## Required retrospective migration before M7

Because these gates were adopted after some Octoport acquisition already ran, preserve all historical evidence but perform a migration audit rather than pretending the stricter rules had always existed.

Required artifacts before Collection Freeze:

1. `M2_WORDSTAT_RETRO_GATE_AUDIT_2026-09-16.md`;
2. B01 raw-persistence inventory;
3. seed/information-gain/depth review;
4. explicit list of any evidence limitations that cannot be reconstructed;
5. decision whether any limitation actually requires new provider acquisition.

No historical provider replay is automatic.


## 2026-09-18 — M3 retrospective control migration

M3 primary organic acquisition was accepted before the current full-SERP control requirements were made explicit.

This does not invalidate the accepted organic top-20 corpus. It creates mandatory control debt:

- full-SERP HTML feature sample;
- device/browser sensitivity where supported;
- bounded regional sensitivity;
- bounded temporal repeat;
- complete cross-query URL/domain overlap QA;
- explicit closure artifact explaining whether any original M3 conclusion changed.

The cause is an incomplete historical M3 Definition of Done: it verified organic intent, lifecycle, persistence and readback, but did not require those control dimensions.

Therefore:

```text
M4/M5 MAY PROCEED
M6 MUST REOPEN M3 FOR CONTROL PATCH
M7 MUST NOT FREEZE WHILE M3_CONTROL_DEBT != CLOSED
```
