# C2.1 R3 evidence

Task `SA-I1-C2-1-R3-20260916-01`; status `PENDING ARCHITECT REVIEW`.

The narrow correction replaces the invalid restored pending/starting `queueMutation` + `commit` branch with terminal `invalidateKnown(contextForState(), error("ACTIVATION_CONTEXT_MISMATCH"), true)`. The denial is therefore authoritative in memory before persistence, clears pending/rotation/credentials/authority/cacheClock, resets runtime clock ownership, preserves marketplace/catalog/IDB data, and uses the existing denied-state write followed by AUTH removal on write failure. If both persistence operations fail, initialization completes denied with `AUTH_DENIAL_PERSISTENCE_FAILED` and the restart limitation remains truthful.

R3-A and R3-B use real `startActivation` records and worker recreation. R3-A is RED at the exact-base start `06e0eb79badc5e65ea972035a2968f72fdaae6a` (raw failed AUTH set escapes initialization), and GREEN on the candidate: one failed denied-state write, one successful AUTH removal, no restore request/portal/exchange, deleted-attempt non-return after rolled-back restart, fresh idempotency key/device code, and preserved catalog marker. R3-B covers invalid-origin and malformed-starting records with both set and removal failing: initialization resolves denied with `AUTH_DENIAL_PERSISTENCE_FAILED`, the old record remains explicitly unprotected, and storage recovery permits a fresh attempt without old keys. T1/T2 valid pending/starting positive controls remain passing.

R3-C completes the omitted T6 orderings. The held real checkpoint write blocks reset publication; reset then B activation/signing complete in recorded order and B's generation/account/envelope/clock remain authoritative after the old result and a late event. A separately signed, real-verified envelope replacement in the same device/session/requested-AI/account context is observed as a different envelope; an old decision is denied when stale or remains only in its pre-replacement ordering, and current status/getAuthority reflect the replacement. R3-D completes T7 with a fresh denied `OZ_EXECUTE_COMMAND` per marketplace, new manual request IDs, unchanged provider and control-plane counts, no new delivery advertisement, and `manual_recovery` absent alongside recovery/outgoing text absence. Existing positive Ozon text, Wildberries binary bytes, and real artifact-key assertions remain.

All focused source and extracted-package routes pass. Full `extension_i1.py` passed 98 processes and `extension_core.py` passed 111 processes with Node `v24.20.0`. The browser verifier and native Chromium application passed source and extracted routes on Chromium `151.0.7922.34`; each native route used the same fresh per-run private key and packaged public trust configuration. Installed-local API/portal/PostgreSQL passed on task-owned database `seller_agents_i1_sync_r3_20260916`, with two isolated accounts/device sessions and zero live provider calls. `docs:check`, `bridge:guard`, and `openapi:check` passed. No required local gate is skipped or failed; remote observability is `UNKNOWN` where the executor cannot authenticate to CI.

The final development ZIP is `build/c2-r3-final-i1/package/SELLER_AGENTS_I1_C1_v0.2.4_LOCAL_DEVELOPMENT.zip`, size `1823235` bytes, SHA-256 `03ed7c22618cbb9b49c83e0c2b6a4735b314c0a30480258550b02492cc8dab65`. The deterministic repeat archive matches, source/extracted file bytes match, and both service-worker bytes hash to `9a9728bac54f6ba014c5ebe470925f5fcea06936650ad0b68c29c4074bfc23b0`.

Refs: task start `06e0eb79badc5e65ea972035a2968f72fdaae6a`, start tree `e090b748a6a1aa8d35869f215c7846984e4f73f3`, start parent `ead575816dde5df2613a61cb8aa599e216c8066a`, canonical main `5d7c8853cc69dd95bc6e713cac3fb2aa0a63383c`, code commit `6dc770e` (full hash in `results.json`). Current-head native CI `35060667796` / `job104680101447` passed both routes; installed CI `35060667782` / `job104680101417` passed; client job `104680101221` was successful; candidate-identical checkout `c8652fb156b4a14e67ecbf9de8c016176163e1bb`. Run `35057935403` is retained as R1 evidence, not R2 CI. Domain migration, offline grace/C2.2, profile consumption, Health, merge, deploy and release remain out of scope.
