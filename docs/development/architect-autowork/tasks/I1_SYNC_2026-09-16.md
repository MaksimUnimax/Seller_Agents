TASK ID: SA-I1-SYNC-20260916-01
STAGE: I1 synchronization — one combined C1 + I1-SRV.5 candidate before C2.
ROLE: One implementation executor. Apply the exact integration procedure below; no independent architecture, new features, extra agents or parallel tasks.

REPOSITORY / EXACT INPUTS
MaksimUnimax/Seller_Agents.
Canonical main: 5d7c8853cc69dd95bc6e713cac3fb2aa0a63383c.
Accepted server branch: feature/server-i1-srv5-acceptance-2026-09-16.
Server head: 086ae20c2858849ec13b1ab67c2f7661259022c3.
Server tree: ea17da20acb01ae3c03a14d5b18128c8040e27e7.
Accepted client branch: feature/extension-i1-client-2026-09-15.
Client head: 56c81a3521c02502b65fd713aec890e5a30f038d.
Client tree: 5c14497e239922c3712d0c3be12af7ee59e665f5.
Common ancestor: bc0cd0088ca50ba06021ea602a46bdd90de91378.
New integration branch: integration/i1-c1-srv5-2026-09-16, based on the accepted server head.
PR7 and PR8 remain draft/unmerged; do not move either source branch or main.
The preceding R2 task is complete. Architect has accepted I1-SRV.5 in the bounded server/reference scope, separately from accepted C1.

PROVEN DEPENDENCY / ARCHITECT DECISION
The accepted client branch predates four commits now in main. Client and server acceptance currently belong to different trees; their separate green results do not establish one combined installed candidate.
Architect compared each head with the exact common ancestor: client54 changed paths, server19, zero intersecting paths.
Create one ordinary history-preserving merge and validate that combined tree. No C2 implementation or production behavior changes in this task.

EXACT PROCEDURE
1. Fetch and read remote refs. Verify all full40-character inputs and trees above. Check the target branch and an existing PR for it before creating anything. Architect observed neither at preparation. If a target already exists, do not overwrite or duplicate it; report its exact identity unless it is this task's own recoverable work.
2. Create an isolated worktree and the target branch from server086ae20. Merge client56c81a3 with --no-ff, preserving server as first parent and client as second. No squash, cherry-pick, rebase, ours/theirs override or force push. A conflict contradicts the verified disjoint inputs: preserve the facts and return without inventing conflict resolution.
3. Verify both accepted heads and canonical main are ancestors of the merge. Record merge SHA/tree and both parents.
4. Mechanically verify file provenance against the common ancestor:
   - For each of the54 client-only changed paths, merged blob/mode equals client56c81a3.
   - For each of the19 server-only changed paths, merged blob/mode equals server086ae20.
   - Every other path/blob/mode equals the common ancestor.
   - Account for additions/deletions explicitly. No untracked output enters the commit.
   Record this inventory under the new evidence directory.
5. After that merge, make only the factual documentation updates listed below. All non-documentation blobs must remain byte-identical to the merge tree. Keep package version/composition0.2.4 unchanged. Retain the two Playwright reference assertions and health schema reset/migrate exactly.

ALLOWLIST FOR NEW EDITS AFTER MERGE
- docs/STATUS.md
- docs/ROADMAP.md
- docs/development/client-i1/README.md
- docs/server/I1_SRV_5_REFERENCE_ACCEPTANCE_2026-09-16.md
- docs/server/I1_SERVER_EXTENSION_SYNC_2026-09-16.md
- docs/server/evidence/i1-sync-2026-09-16/README.md (new)
- docs/server/evidence/i1-sync-2026-09-16/runtime-blobs.json (new)
- docs/server/evidence/i1-sync-2026-09-16/package-receipt.json (new)
The merge imports the exact already accepted commits; that import is distinct from new edits.
Do not modify implementation, test code, workflows, dependencies, lockfile, migrations, contracts, historical evidence or architect STATE. No cosmetic cleanup.
If a combined gate exposes a new defect, preserve the exact failed command/log and return for architect correction; no self-designed repair or bypass.

DOCUMENTATION
Record C1 ACCEPTED at56c81a3 only for its source/package/native/installed-local development scope.
Record I1-SRV.5 ACCEPTED at086ae20 only for server/reference lifecycle and fixture gates, using references/I1_SRV5_R2_REVIEW.md and references/I1_SRV5_R2_CI.json under docs/development/architect-autowork on docs/architect-autowork-handoff-2026-09-15 (read that branch; do not merge it).
Describe this new combined candidate as IMPLEMENTED_CANDIDATE / ARCHITECT_REVIEW_PENDING until reviewed.
Keep main's actual old SHA and draft/unmerged status explicit; acceptance is not merge or deployment.
Preserve previous disk-exhaustion BLOCKED, R1 integration FAIL, R2 setup-interlock BLOCKED and deterministic RED/GREEN history. Historical evidence is not rewritten as PASS.
Explain the synchronization dependency; do not reorder the wider roadmap.
C2 offline/profile/joint command-result acceptance remains pending. Health/P8.4 B5 remains NOT ACCEPTED; B6–B8 queued. S1.2 real email/preprod, D3, full D2/I1, beta and release remain open.

VALIDATION ON THE COMBINED CANDIDATE
Use Node24, pnpm10.34.5, frozen lockfile and the existing Python/browser requirements. No version/dependency changes.
One local combined cycle, using the existing commands without modifying fixtures:
A. pnpm install --frozen-lockfile; pnpm lint; pnpm format:check; pnpm typecheck; pnpm exec vitest run tests/e2e/server/playwright-config-regression.test.ts; pnpm test.
B. On a fresh, task-owned disposable PostgreSQL database whose name includes test or e2e: pnpm test:integration; pnpm db:migrate; pnpm openapi:check; pnpm bridge:guard; pnpm build; pnpm test:e2e.
Require integration39files/1527tests with all20 health tests executed; E2E88/88 including both I1 reference tests. A skipped conditional frozen-import workflow step is not a test result. No new skips.
C. python tooling/checks/extension_core.py --output build/i1-sync/core
D. python tooling/checks/extension_i1.py --output build/i1-sync/client
Both source and extracted routes must pass; preserve the actual per-process results.
E. Run tests/regression/extension-core/client-i1/browser_verifier.py on both build/i1-sync/client/package/runtime and package/extracted, with separate outputs.
F. Run the existing native application fixture as specified in .github/workflows/extension-ci.yml: create the temporary key/config with make-browser-config.mjs; build with SA_PACKAGED_CONFIG_JSON through tooling/build/extension_composed.py; run browser_application.py on runtime and extracted with --fixture-private-key and separate outputs. Private fixture material stays outside published artifacts.
G. Run python tests/regression/extension-core/client-i1/installed_local_integration.py --output build/i1-sync/installed-local with PRODUCT_CONTROL_PLANE_E2E=1 and a separate fresh task-owned local DATABASE_URL, using the setup from .github/workflows/extension-i1-ci.yml. Require both existing account scenarios, actual installed Chromium extension, API/portal/PostgreSQL, approval/exchange/bootstrap, account isolation and fail-closed assertions. No storage injection or substitute API-only proof. Record Chromium version and zero live-provider calls.
H. pnpm docs:check after factual documentation is complete.
The unchanged baseline Ozon/WB suites remain mandatory applicable remote Extension CI gates; do not repeat accepted-parent standalone audits.
Do not run the two database harnesses concurrently or point cleanup/reset at shared databases. If local resources fail, record the actual block once; do not prune shared containers or falsify PASS.

PACKAGE / EVIDENCE
Use the existing deterministic composed builder/checker. Record the produced development ZIP name, SHA-256, size, repeated-build equality, source/extracted parity and all packaged-file byte readbacks. Use separate receipts for default-development and ephemeral-key browser packages; never conflate them.
runtime-blobs.json must contain the immutable input SHAs, merge parents/tree and provenance checks plus a check that post-merge non-documentation blobs are unchanged.
Record sanitized command results and explicit failed/skipped/not-run scopes. No tokens, private keys, raw auth envelopes, screenshots/video/traces containing secrets. No production or live-provider credentials.

PUBLICATION / TERMINAL
Commit only the merge plus allowlisted factual documentation/evidence. Do not amend historical accepted commits. Publish the new branch without force.
Create or reuse exactly one draft PR against main for this integration branch after checking for duplication. PR7/PR8 stay unchanged and unmerged.
Existing workflows must remain unchanged: Server CI, Documentation CI, Extension CI, and both jobs of Extension I1 are required for the combined candidate. Extension I1's push filter excludes the new branch, but its pull_request-to-main trigger applies; use that normal draft-PR event rather than editing filters or unnecessary dispatch.
Record known run/job URLs and checkout SHA only when actually observed. If authenticated visibility is unavailable, state it once; architect checks through the connector. Never infer all-green from local checks or rerun CI just to wait.
Terminal report: task ID, canonical main and exact source heads, integration branch/head/tree/parents, merge commit versus evidence commit, changed paths/provenance, all local outcomes, ZIP receipt, PR URL and virtual merge if observable, remote evidence or explicit UNKNOWN.
Stop after terminal report for architect review. Do not launch C2, Health, merge, deploy or release.
