SA-I1-C2-2A-R3-20260916-01 — C2.2-A, исправление доказательности Q2/Q3/Q4/Q5, без изменений production.

REPOSITORY / BASE
Canonical: MaksimUnimax/runtime-fixtures, repository ID 1369117174. Это переименованный прежний репозиторий; PR9 и история сохранены.
Branch: integration/i1-c1-srv5-2026-09-16; existing draft PR9.
Start/base: edc70704cec965b9e7bc58ca4d3c961834573517.
Start tree: 3aaad9a0fd29649f492ebb0cd31522366f2475dcc.
Parent: d927b2498262798f03d2fa9f0accf9c62fbaaad0.
Observed main: bc718cc5c677ad0eb4598e7de3ad766473ff0847.
Fetch current canonical remote and compare these refs before edits. Do not reset advanced refs or recreate branches/PRs. If integration advanced, return exact divergence rather than overwrite.
Main integration is NOT part of this task. PR9 currently conflicts only in docs/README.md; that path is owned by the parallel repository-presentation stream. Do not resolve it, merge/rebase main, or alter public presentation.
Owner's rename/publication, domain migration and SEO are accepted parallel work. Do not edit the separately owned private control repository.

PROVEN PROBLEM / DECISION
Architect independently ran exact R2 policy source/extracted: 36 ledger rows PASS. Rebuilt default ZIP exactly matches 1834654 bytes / SHA256 2c1f5765b0eb382d9387e3ea1a59e71bd549344f10c107a39069d92719f341de.
Nevertheless Q3 idbPutGet writes the artifact again before every retention assertion. Forced deletion before its postconditions still produces PASS. Read-only postconditions correctly fail. The seed also lacks expires_at_ms; normal startup cleanup rightly removes it as expired. This is a fixture/proof defect, not a reason to weaken cleanup.
Q4-D currently accepts a valid signed online account replacement; that is a legitimate positive case, not rejection of stale account-A fallback.
Q5-B assert.notEqual(record, clonedRecord) only compares object references; it does not prove exact B preservation.
Q2-D consumes its first floor through canWork and manually rewrites durable time. Make the policy checkpoint itself prove persistence without fixture rewriting.
Architect's disposable corrected probes passed on unchanged production. No runtime fix is authorized.

ALLOWLIST
- tests/regression/extension-core/client-i1/client-offline-policy.mjs
- docs/migration/evidence/extension-i1-c2-2a-2026-09-16/r3/README.md
- docs/migration/evidence/extension-i1-c2-2a-2026-09-16/r3/results.json
- evidence logs/probes/receipts under that same r3 directory, no ZIP or secrets
- append-only correction note in docs/migration/evidence/extension-i1-c2-2a-2026-09-16/README.md
All other paths unchanged, including worker-harness.mjs. Read existing source helpers as needed, but do not choose new scope or architecture.

R3-1 — Q3 actual retention and honest storage-failure boundary
Split seed/write and read-only get. Put the fixture artifact exactly once; subsequent assertions use a readonly object-store get, never put.
Seed explicit created_at_ms=T0 and expires_at_ms=T0+3600000, with the existing stable artifact key/value. Keep all scenario clocks within this TTL. Preserve normal cleanup; do not disable timers.
Capture IDB write count after seed. After both AUTH set/remove fail, after recovery and after restart assert exact retained record and unchanged IDB write count. Preserve catalog marker assertions and exact AUTH failure assertions.
At first increased floor T0+100: set and remove each fail once, policy rejects AUTH_DENIAL_PERSISTENCE_FAILED and durable AUTH remains byte-equivalent to the original.
Immediately clone the failed durable backing for the separate crash/restart branch.
On the original worker restore storage, roll wall back to T0-100 while leaving monotonic=1100. Invoke policy again: CACHE/FRESH; successfully persisted floor remains T0+100, proving in-memory time did not roll back.
Close original worker. Recreate from the separate failed-backing snapshot with seedAuthority:false, wall=T0, mono=1000 and the same IDB. It can only restore the old durable floor; explicitly retain that limitation. Advance to T0+100/1100, invoke policy, and assert successful normal checkpoint.
Keep restarted.AUTH assertions pointed at the failed-backing branch, not at the original worker's later recovered backing.
Add a disposable negative control in scratch: clear fixture IDB immediately before a post-failure readonly assertion. Corrected test must FAIL because get returns undefined. Unmodified scenario must PASS. Do not publish the destructive mutation as the normal test.
Record architect's exact-start false-positive reproduction separately; do not claim production was RED or modify runtime to manufacture RED.

R3-2 — Q2 policy time consumption
In Q2-D replace the two initial canWork checkpoint calls with bootstrapWithPolicy({detectedAi:CHATGPT}) and assert CACHE/FRESH at T0+500/mono1500, then CACHE/STALE_BUT_OFFLINE_GRACE_ELIGIBLE at rolled wall T0-500/mono2500.
Expected durable consumed floor T0+1500. Remove direct assignment to backing.AUTH.cacheClock after acquisition.
Restart with rolled wall/reset monotonic: floor does not decrease, policy remains stale eligible. At grace equality policy rejects CACHE_EXPIRED and persists floor>=T0+3000; a second rollback/restart still rejects.
Supply explicit fetch returning actual bootstrap503 BOOTSTRAP_UNAVAILABLE to every restarted policy worker; assert exact /v1/bootstrap paths, never rely on a harness assertion exception being classified as transport.
Retain independent fresh-only canWork/status/getAuthority guard assertions.

R3-3 — account fallback ownership and exact B preservation
Preserve current correctly signed online account replacement as a positive case, name it accordingly; do not change production to reject legitimate server authority.
Add Q4-D-account-replacement-obsoletes-held-cache using existing beforeCryptoVerify hook:
(a) valid restored signed account A; positive same-account acquisition establishes the fixture;
(b) arm hook only after that positive operation; make policy A receive transport failure and hold its real cached-envelope verification;
(c) while held, complete raw bootstrap B with a correctly signed payload using same device/session, distinct account id 99999999-9999-4999-8999-999999999999, configVersion2 and serverTime=T0+1;
(d) capture a deep snapshot of B's durable AUTH after B commits; verify B account differs from A and device/session are unchanged;
(e) release A; expect CACHE_ACQUISITION_OBSOLETED, no CACHE return; assert exact B AUTH deep equality, including generation, credentials, account, envelope, binding and clock.
Use real WebCrypto verification and public APIs. No private closure/state injection; fixture signing is allowed.
In both existing Q5-B variants capture durable AUTH after B success/403 and before releasing A; after A settles assert deep equality with that snapshot. For 403 authority remains null and owned credentials remain. Replace the cloned-object reference inequality.
For Q5-C retain queue-order assertion; after both operations settle assert complete expected B account/envelope/credentials/generation/clock or exact denied authority, using values captured from the intended signed B response and committed B state, not configVersion alone.
Retain Q5-A real activation, Q6 final-return fence, Q1 classification and all earlier controls. No new production hooks.

EVIDENCE / VALIDATION
Append factual R2 correction: old Q3 retention proof invalid, old Q4-D was online positive, Q5 comparison insufficient; do not rewrite old results.json or erase reported passes.
Generate per-case source/extracted results from actual assertions. Missing/failed/skipped cases prevent group PASS.
Run python3 tooling/checks/extension_i1.py --output <new-task-owned-output>; it executes updated policy tests on both composed source/extracted routes. Expect every prescribed case PASS, no skipped cases, zero live provider calls.
Run pnpm docs:check, pnpm bridge:guard, pnpm openapi:check with existing pinned tooling; no dependency/version changes.
No repeat of unchanged core/native/installed local suites is required: shared harness and production are immutable. Preserve their exact historical provenance; do not call them current-task runs.
Rebuild repeat archive and compare source/extracted runtime inventory with exact start. All production inputs/runtime bytes must match; client Git blob stays 5f6a195048e257304c16fb92a25c4ff772d98fe0. Default ZIP must retain the exact receipt above; do not transfer it to an ephemeral-key native composition.
For Q6 retain the current positive assertion and cite architect's already executed candidate-versus-single-final-fence-mutant proof explicitly as architect evidence. Do not invent an executor mutant run.
If an expected behavior fails, preserve the exact failure and preconditions; do not weaken assertions or modify production.

REMOTE / PUBLICATION / TERMINAL
Existing R2 push Extension CI run35075574262: core104727070823/native104727071214/Ozon104727071155/WBnodes104727071159/WB-browser104727071069 SUCCESS; run completed SUCCESS. Native completed log verifies checkout edc70704cec965b9e7bc58ca4d3c961834573517, source/extracted fixture PASS, Chromium151.0.7922.34, zero live calls. Separate native log receipt:1835439bytes/SHA2561b0ad27a67946fa2755d2637af9d4fba345b3bc7d425fc939f8c18c315d72468, not downloaded and not the default ZIP. No current R2 PR I1/Server/Docs run was observed while PR9 was dirty. These are separate facts, not all-green.
Do not rerun/cancel CI or modify workflows/triggers. Normal FF publication of test/evidence commits to the same integration branch; reuse draft PR9, no merge. Architect handles remote gate review; executor authentication absence is UNKNOWN.
Terminal: task ID; exact start/final/tree/parents; test/evidence commits; changed paths; R3-1/2/3 and retained Q1–Q7 outcomes; negative-control expected failure versus normal PASS; gates with provenance; unchanged runtime inventory and package receipt; current PR conflict/CI facts.
Stop after terminal for architect review. No C2.2-B, offline Work, profile execution, Health, server runtime, domain/site/SEO/public-presentation work, cleanup, deployment or release.