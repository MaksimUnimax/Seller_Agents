TASK ID: SA-I1-C1-R4-20260915-01
STAGE: Seller Agents / D2-I1 / I1-C1, remaining R3 acceptance corrections.
ROLE: One implementation executor; implement these decisions, run prescribed checks, publish and stop for architect review. No agents, independent architecture/research, self-acceptance or parallel roadmap task.

START / IDENTITY
Repository MaksimUnimax/Seller_Agents.
Branch feature/extension-i1-client-2026-09-15; draft PR#7 unmerged.
Required current starting head e26fcc7dd60617838cb9a49d7c6560018cb45ec3; tree c1beb4927fee66c4065e133c960929e800fabe5e.
Observed main 5d7c8853cc69dd95bc6e713cac3fb2aa0a63383c.
Fetch/check remote head before work and before publication. Unexpected advancement, conflicting local work or a still-running previous job => report exact condition and stop; no reset/force-push/duplicate job.
R3 has been reviewed: REWORK_REQUIRED, not environment-only BLOCKED. Do not rerun R3 or rename G1-G6. Use R4 acceptance IDs below and map back to original G1 polling, G2 refresh, G3 denial/Work guards, G4 versions, G5 installed auth, G6 native restart.

R4-1 — ALL SUCCESSFUL RESPONSE CLASSIFICATION AND SEMVER VERIFIER
Files packages/control-client/src/client.js and crypto.js; tests client-r3.mjs/new client-r4.mjs and verifier.mjs as appropriate.
Independent exact-source probes on starting head:
- Oversized200 now denies correctly; oversized201 and oversized206 still throw CONTROL_RESPONSE_TOO_LARGE with authenticated:true and Work:true. bootstrap catch checks status===200 although request already attaches responseOk.
- Correctly signed top-level minimumVersion 0.2.4-alpha.2+build.7 is rejected BOOTSTRAP_INVALID_PAYLOAD_SCHEMA by crypto.js's older regex, though valid under packages/shared/src/index.ts SemVerV1Schema and below client0.2.4.
Changes:
- In bootstrap, classify CONTROL_RESPONSE_TOO_LARGE for every successful response using failure.responseOk===true (or status>=200 && status<300), not equality200. Preserve final401/403 invalidation and transient503 handling. Keep the bounded body reader and generation fences.
- In crypto.js enforce the exact shared SemVerV1Schema grammar, type and length1..64 for non-null top-level extension minimum. Copy its browser-safe grammar; do not import Node/zod into browser or modify shared/server code. Retain client.js exact decimal comparison.
- Add signed acceptance/rejection tests through actual verifier/client. Oversized statuses200/201/206 must all deny; oversized503 must remain transient; final oversized401/403 retained.
- Valid combined prerelease+build top-level minimum must verify and allow compatible client; equivalent build metadata must not change precedence. Malformed leading-zero numeric prerelease, malformed metadata and over64 characters deny.
- Meaningful precedence matrix must vary packaged client version: alpha.2 versus alpha.10 in both directions, prerelease versus release, hyphenated prerelease, numeric versus text, equal-prefix length. Current R3 alpha rows only compare release0.2.4 with two lower prereleases and do not test their ordering.
- Actual Chromium120.0.0.0 versus minimum120.0.0.1 must deny, equal120.0.0.0 and zero-padded120.0 must allow. Keep leading-zero/out-of-range controls. Recompute profile fingerprints/signatures. No production comparator stubs.
Demonstrate newly proven defects RED on e26fcc7, GREEN after corrections; retain already-green account-only login/restore and oversized200 controls.

R4-2 — COMPLETE PREVIOUSLY REQUIRED OWNER AND DENIAL PROOFS
Files tests/regression/extension-core/client-i1/client-r4.mjs; worker-harness.mjs only for fixture facilities; tooling/checks/extension_i1.py wires BOTH source/extracted.
R3 adds held exchange but not held start with B active, same-worker old refresh/bootstrap across actual B authentication, or active composed denial/delivery checks. client-races.mjs's old refresh/new-account control uses two workers and is not a substitute.
Add deterministic cases using actual client, signer, deferred responses and one worker:
a. Hold A device-start; cancel/reset and complete B activation before releasing A. Release A success and failure separately; no extra portal/polling, no B account/error overwrite.
b. Hold A refresh, reset and COMPLETE B device exchange/signed account bootstrap in same worker. Release A success and AUTH_REFRESH_INVALID separately; B remains authenticated, credentials/generation/error unchanged. Keep existing same-worker concurrent refresh singleflight and lost-response idempotency tests.
c. Hold A bootstrap (including initial expired-token refresh path); reset and authenticate B; release A signed success,403 and malformed response separately. No old request may adopt B bearer or invalidate B.
d. Retry startActivation with credentials/no authority, hold bootstrap, establish B, release A failure; old error must not be committed into B.
Use response-owner barriers and awaited operation settlement; do not replace same-worker cases with seeding another worker or arbitrary sleeps.

For original G3, add actual composed lifecycle denial tests, not merely status().workAllowed:
- Reuse application.mjs and batch-context.mjs fixture patterns: save a real SA Ozon/WB store, SA_WORK_START with actual start acknowledgement/identity, then OZ_EXECUTE_COMMAND. Count marketplace and control-plane requests separately.
- For Ozon and WB, hold the first provider response in a sequential two-command batch; issue bootstrap403/invalid-signature denial, inject failure of cleanup writes to Work/binding/manual-operation records, then release provider. There is no second provider dispatch, no delivery advertisement and no insert/send permission despite stale stored Work state.
- In separate delivering-owner fixtures, trigger denial and exercise OZ_BATCH_DELIVERY_INSERT_COMMIT, OZ_WORK_SEND_COMMIT, file manifest/chunk port reads and recovery using existing application.mjs file fixtures. No text insert, click, binary read/chunk or replay is allowed. saEnabled remains composed; legacy global credentials do not restore standalone routing.
- Auth/status and Finish remain callable; ordinary dispatch/delivery guards add zero control-plane HTTP. Distinguish the explicit bootstrap test request from guard traffic.
- Add authority invalidation cases for bad signature, unknown signing key, malformed/schema-invalid successful payload. Assert generation advance and denial with previously valid positive Work; standalone verifier rejection alone is insufficient.
- Retain actual no-seed restart after set-fails/remove-succeeds and memory-only/bounded-error result when both fail. Retain valid-compatible positive control.
Do not change production guards speculatively. If one of these specified assertions reveals an additional implementation failure beyond R4-1, report the exact assertion, runtime function and trace for architect decision; do not weaken expected behavior.

R4-3 — RESTORE COMMON-CORE ASSERTIONS AND FIXTURE SCOPE
Files tests/regression/extension-core/full-worker-composed.mjs, worker-harness.mjs and tooling/checks/extension_import.py.
R3 retained ten cases and most counts but omitted donor assertions: no MANUAL_BATCH_FAILED, no BATCH_PROCESSOR_UNCAUGHT, and Performance-only versus Seller-only provider identity. It also removed the existing0.2.3 sender adaptation and changed default Performance credentials for every harness consumer.
Changes:
- Add exact zero counts for both failure events and retain all current positive once-only assertions.
- For each Performance case require exactly one Performance business request, zero Seller business requests and one Performance auth request; keep combined total1. For seller case require one Seller business request and zero Performance auth/business requests. Keep fingerprint cd4bce38 and current logical/physical assertions.
- Revert R3 global additions of Performance credentials in worker-harness.settings and adaptFixtureMessage to their starting R2 behavior. In full-worker-composed.mjs explicitly call worker.popup SA_STORE_SAVE with seller AND Performance fixture credentials, personalDataEnabled:true, then worker.start. The existing request code records fixtureStoreId on that response.
- Restore expected_version0.2.3 full-worker sender adaptation exactly as present at0b2a1776, while0.2.4 uses new composed file. Frozen imported source/donor RED unchanged.
- Make adaptation evidence enumerate the actual assertions including zero failures and provider split; behavior_assertions_changed:false must describe reality.
No legacy fallback, skipped case or authority=true stub.

R4-4 — FIX THE WHOLE INSTALLED AUTH FIXTURE, KEEP PRODUCT ADMISSION CLOSED
Files tests/regression/extension-core/client-i1/api-harness.ts and installed_local_integration.py ONLY for fixture setup/observations. No production auth/DB/portal changes.
Exact-head CI: run34969687336/job104382641739, PostgreSQL18 healthy, timeout at line100 waiting activation URL after OTP Verify. Log does not include OTP response code. Source diagnosis: migration0016 initializes beta_admission_state CLOSED/capacity0; harness provides no existing identities; createAuthRepository.verifyOtp returns BETA_CLOSED for a valid new identity. Independent exact repository-function probe confirms this path with SQL replies matching migration defaults.
Decision: this gate tests LOGIN of two existing fixture accounts, not new beta registration. Preserve CLOSED admission and normal OTP/session/device authorization logic.
- Before app.listen in api-harness.ts, prepare exactly two existing ACTIVE users/accounts/OWNER memberships/verified EMAIL identities for i1-client-one@example.test and i1-client-two@example.test in one parameterized database transaction.
- Use random UUIDs, normal tables users, accounts, account_memberships, user_identities and their default fields. Require the two fixture identities absent in the fresh disposable DB; fail clearly on collisions. Do not insert portal sessions, device authorizations, tokens, beta admissions or extension storage.
- Enable this preparation only with PRODUCT_CONTROL_PLANE_E2E=1 and a loopback DATABASE_URL in the installed harness's isolated test DB. Keep fixture setup inside this test entrypoint. Read and retain beta mode/capacity/admitted before/after and assert unchanged; do not OPEN beta or alter capacity. No migrations, bypasses or new production endpoint.
- Existing AuthService/createAuthRepository and fixed development OTP still process both browser logins; api-harness BootstrapService continues signed UNCONFIGURED/accessBasis NONE. No invented RESOLVED/entitlement.

Fix all identified subsequent harness defects together:
1. Await and inspect OTP request/verify HTTP responses from portal BFF before waiting navigation. Expected202 request and200 verify. On failure record only stage/status/allowlisted error code, never raw body/challenge/OTP/cookie. A denied OTP must fail immediately with its actual condition, not generic30s navigation timeout.
2. Keep real login/activation URL, preview, code-pattern and successful approval waits. Capture distinct authorization IDs transiently from the two portal activation URLs; select an ACTIVE enabled account option after it loads. Store only equality/count booleans in artifacts.
3. Correct logout expectation to204: apps/api/src/auth-routes.ts explicitly sends204 on every successful logout path. Verify session-cookie clearing/next login path and retain custom reset confirmation.
4. Correct device-start success to201: apps/api/src/device-authorization-routes.ts explicitly sends201. Exchange/bootstrap success remain200.
5. Response observer must distinguish direct extension API traffic at43100 from portal BFF traffic at43101/api/control-plane. Portal preview GETs never appear as browser requests to43100; do not derive distinct authorizations from that impossible set. Normalize endpoint labels in memory, collect count/status booleans without raw URLs/codes. Use captured activation URLs/approval responses for distinct authorization evidence and direct extension responses for start/exchange/bootstrap.
6. Require successful A and B account-only authentication, Work false, distinct account/device/session, catalog isolation, two successful starts/exchanges/bootstrap and actual same-worker sentinel continuity. Do not replace assertions with literaltrue or inject extension storage/restart browser.
7. Preserve bounded stage/status/count evidence on every failure before context closes. Do not publish raw auth traces, private keys, browser profiles or screenshots containing codes.

The small test-only existing-account fixture is a diagnosed prerequisite of G5. It does not implement I1-SRV.5 or accept beta registration. Local disk exhaustion remains a distinct environment condition. Use fresh task-local PostgreSQL if available and required remote CI; no shared Docker pruning/volume deletion/other-process kills.

VERIFICATION / PUBLICATION / STOP
Retain version0.2.4 and pinned Node24/pnpm/Python dependencies.
Run:
- extension_i1.py with new R4 cases on source/extracted;
- extension_core.py on source/extracted with full restored assertion set;
- browser_verifier.py, native browser_application.py source/extracted using retained G6 restart setup;
- installed_local_integration.py with isolated loopback PostgreSQL;
- pnpm docs:check, pnpm bridge:guard, pnpm openapi:check.
Use exact existing workflow arguments and fresh task output directories. No repeated unrelated full server suites. Full applicable remote Extension CI, both I1 jobs and Documentation CI remain required; do not claim all-green while WB browsers or installed job is pending/failed.
Preserve R1-R3 evidence. Write r4/README.md, criterion-to-executable-assertion mapping, commands/outcomes, remaining limitations and deterministic ZIP receipt (source/extracted parity, repeat archive, hash/size, byte readback). Correct current README broad claims; do not relabel incomplete criteria PASS or claim all2xx from only200.
Push fast-forward to existing branch after rechecking remote. Keep PR#7 draft/unmerged, no main/Health change. Let CI run on final candidate. Record pure push SHA versus PR merge checkout and actual parent SHAs. If GitHub access fails, state exact command/endpoint once; architect independently reads Actions.
Do not edit architect handoff STATE.
NON-GOALS: production server/shared schema/migration/portal changes, BFF retry-header backlog, shared server SemVer backlog, Health/P8, I1-SRV.5, C2/offline, D3, S1.2/email/preprod, deployment/release, provider calls, dependencies or unrelated refactors. Preserve Ozon common lifecycle, explicit sequential read-only work, account/store/generation isolation, no replay, one-hour buffers and no mandatory control HTTP in data-plane paths.
TERMINAL: task ID; start/final full head/tree; R4-1..4 executable assertion results and original G mapping; local and remote gate results/job URLs/checkout SHAs; ZIP evidence; exact blockers. READY_FOR_ARCHITECT_REVIEW only when required checks pass, otherwise precise BLOCKED/REWORK_REQUIRED. Stop after terminal report for architect acceptance; no next task.
