TASK ID: SA-I1-C2-2A-R2-20260916-01
STAGE: C2.2-A — finish the existing acceptance; tests/evidence only.
One executor. Implement the specified tests; do not choose a new architecture.

REPO / REFS
MaksimUnimax/Seller_Agents.
Branch integration/i1-c1-srv5-2026-09-16; reuse draft PR9.
Start 90f3f5ab4787d1ed4b782bff4f97181dd9fe3772.
Tree 835ce56a1fd235196f7010936817485bf7347eb5.
Parent/code 65596c5f1b5189c60de64d72a80773d43f1a203f.
Main observed cfd63e5c2227a52ccff6cbb5bb2da4580ec3bb3a. Owner-authorized Octoport migration AND SEO are running separately. Preserve their work. Do not merge/reset main into this task or edit site/SEO/ingress. Verify refs before work and FF publication; no force push.

ARCHITECT DECISION
R1 REWORK_REQUIRED for incomplete assigned acceptance and unsupported ledger PASS, not a request to redo the production correction.
Independently verified exact client blob 5f6a195048e257304c16fb92a25c4ff772d98fe0:
P1 refresh503 now rejects BOOTSTRAP_UNAVAILABLE, only refresh requested.
P2 superseded successful denial write returns CACHE_ACQUISITION_OBSOLETED, writes2/removes0/AUTH retained.
Final cached public fence independently passes; removing only that final check in a disposable local mutant returns obsolete CACHE.
Keep production client bytes unchanged. If a prescribed test exposes another production defect, preserve the failing evidence and report its exact case for architect design; do not weaken the test or invent a production fix.

PROVEN GAPS
client-offline-policy.mjs has no refresh503 negative regression despite R1-1 PASS; only the positive bootstrap503 and body-failure cases were added.
Rows97–108 use generic rejection; missing oversized/signed compatibility/server-time/refresh terminal rows remain.
Boundary table124 covers equality/after only. The guard fixture240–256 explicitly replaces AI with UNCONFIGURED; it does not prove resolved-AI stale Work denial.
There is no policy grace rollback/restart test, no clocks crossing grace during held write, no catalog/IDB retention assertion, no policy reset→B activation, no held verification.
The test labelled public-return at201–234 starts B immediately after releasing storage, before persist resumes; it does not isolate the final public-return check.
A corrupted signature at313 is not a valid same-session differently signed replacement. Existing C2.1/raw-bootstrap tests do not cover policy fallback merely by being green.
R1 README says OpenAPI not run; terminal says PASS. results.json still says docs/bridge/OpenAPI UNKNOWN. Preserve history and append factual reconciliation.

ALLOWLIST
tests/regression/extension-core/client-i1/client-offline-policy.mjs
tests/regression/extension-core/worker-harness.mjs — optional deterministic test hooks only
docs/development/client-i1/C2_OFFLINE_POLICY_2026-09-16.md — factual status only
docs/migration/evidence/extension-i1-c2-2a-2026-09-16/README.md — append only
docs/migration/evidence/extension-i1-c2-2a-2026-09-16/r1/README.md — append correction only
docs/migration/evidence/extension-i1-c2-2a-2026-09-16/r2/README.md — new
docs/migration/evidence/extension-i1-c2-2a-2026-09-16/r2/results.json — new
Do not alter production, old results.json, checker registration, other tests, dependencies or workflows.

TEST IMPLEMENTATION
Retain working tests. Use named per-case results instead of inferred A–H PASS. Extend prepared() to forward existing clock/IDB options and test-only hooks; do not expose production closure state.

Q1 — classification missing rows.
(a) Valid matching signed cache + expired access: refresh alone returns503 BOOTSTRAP_UNAVAILABLE. Assert exact code/status503, path list only /v1/auth/refresh, zero CACHE. Retain paired actual bootstrap503 positive.
(b) Actual bootstrap Response200 and503 with body exceeding 1MiB: CONTROL_RESPONSE_TOO_LARGE, correct status/responseOk and one bootstrap request, no cached result.
(c) Online bootstrap envelopes: unknown keyId; corrupted signature; correctly signed browser UNSUPPORTED_BROWSER; correctly signed extension UPDATE_REQUIRED with valid required minimum; correctly signed incompatible profile minimumExtensionVersion 99.0.0; correctly signed serverTime below persisted trustedServerTime. Recompute profile contentSha256 whenever compatibility/content changes, then sign. Keep other fields valid and separately verify fixture preconditions. Each must fail for its intended verification/compatibility/time reason, never CACHE.
(d) Preflight refresh401 and403; bootstrap401 followed by forced refresh401 and403. Assert exact ordered endpoints, no cache, durable credentials/authority cleared, restart remains signed out. Preserve successful preflight transport fallback and bootstrap401→refresh-transport denial.
(e) Local AUTH write failure is storage failure, not an eligible HTTP503 even when the injected error carries status503/code BOOTSTRAP_UNAVAILABLE.
Replace generic negative assertions with expected error predicate and observed request/response identity. Malformed JSON control must actually return invalid JSON bytes, not JSON string "not-json".

Q2 — time and fresh-only guards.
Use fixed valid signed times t0, expires=t0+1000, grace=t0+3000; fresh credentials beyond grace. Do not re-sign when advancing clocks.
Separate fixtures at expires-1 => FRESH; expires => STALE_BUT_OFFLINE_GRACE_ELIGIBLE; grace-1 => STALE; grace and grace+1 => CACHE_EXPIRED.
Hold a real increased-floor AUTH write at grace-1, advance wall AND monotonic to grace, release. Same call rejects CACHE_EXPIRED; durable floor>=grace, no success delivered.
Resolved CHATGPT fixture: positive fresh canWork, then successful stale CACHE; canWork/status/getAuthority all deny, and those guards add zero HTTP requests.
Consume time with monotonic advancing while wall rolls backward. Checkpoint still-eligible floor; recreate worker with seedAuthority:false, identical backing/config, rolled-back wall and reset monotonic. Effective floor cannot decrease or reclaim consumed grace. Continue to grace, checkpoint denial, recreate again with rolled-back wall: CACHE_EXPIRED persists. Assert precise durable floor and outcome in both restarts, not only authenticated=true.

Q3 — storage and unrelated data.
Retain actual failed-set/successful-remove and failed-denial-set/failed-remove cases.
Add both-fail case at the FIRST increased-floor write (before any successful new floor). Record old durable AUTH bytes; both writes/removal fail, no result; in-memory time cannot move back. Assert disk remains old and explicitly record restart cannot promise the lost higher floor. Do not claim durability that failed.
Restore storage, invoke policy again at unchanged/advanced effective time, and assert normal allowed/expired outcome plus successful checkpoint.
Seed a catalog marker in local storage and an actual fixture IDB record before failure. Read both afterward/restart and assert exact preservation. Reuse the fakeIDB() transaction/object-store fixture from tests/regression/extension-core/application.mjs (read-only reference; copy the helper into the allowed policy test if needed). Exercise record put/get through that fixture and supply it as indexedDB to makeWorker. This proves VM fixture retention, not native-browser IDB acceptance; do not replace it with an unrelated object marker.

Q4 — missing context distinctions.
Retain existing origin/browser/version/contract/trust/device/session/requested-AI controls.
Add account mismatch with a correctly signed payload and valid paired positive fixture. Label whether rejection happens during restore or during policy processing; do not count restore rejection as an in-flight ownership race.
Add no-authority cache control: valid owned credentials with authority=null/cacheClock valid; actual bootstrap503 BOOTSTRAP_UNAVAILABLE must remain that original error with no CACHE. Separately signed-out state must give AUTH_REQUIRED with zero request.
Retain account-only successful configuration case. Use a valid newly signed same-session payload/configVersion for replacement, not corrupted signature.

Q5 — ownership at real asynchronous boundaries.
(a) Hold A policy's network failure; localReset; perform B through real startActivation/device exchange/bootstrap in the same worker using the real response shapes in tests/regression/extension-core/client-i1/client-r3.mjs (read-only reference). B uses distinct account/device/session. Capture B generation/account/envelope/clock, release A transport rejection, assert old no CACHE and exact B state retained. No manual AUTH injection for B.
(b) Test-only beforeCryptoVerify hook: wrapper awaits the hook then calls the real bound webcrypto.subtle.verify with unchanged arguments. Other crypto methods remain correctly bound. Arm only after restore and only for the selected fallback verification call. Hold A cached-envelope verification; complete later raw B with valid same-session signed replacement; release A, expect CACHE_ACQUISITION_OBSOLETED and B unchanged. Repeat with later bootstrap403; authority remains denied.
(c) Retain held-denial-write race. For held positive AUTH write, later B cannot commit until queue releases; assert that ordering. Cover later valid replacement and later403, old no CACHE, final state exact.

Q6 — isolate final public-return fence.
Use existing injected monotonic clock; no production hook needed.
Arm after the fallback floor storage write has succeeded. At the next monotonic sample (checkpoint completion), schedule queueMicrotask to invoke raw bootstrap B, then return the unchanged monotonic value. The checkpoint's synchronous checks finish before B begins; B advances sequence before bootstrapWithPolicy consumes the completed checkpoint.
Keep B HTTP pending until A settles. A must reject CACHE_ACQUISITION_OBSOLETED. Then finish B with a valid signed replacement and assert ownership.
Architect independently verified this schedule: candidate rejects; disposable mutant deleting ONLY the final check after await cachedBootstrapCheckpoint returns CACHE. Port this schedule and use that exact one-check mutant as a negative test control in scratch only; never publish mutant production.
Retain ONLINE committed-identity check and renewal behavior; no production edit.

Q7 — evidence completeness.
Each subcase gets a stable ID, source/extracted outcome, actual assertion and failure origin. Missing/failed/skipped is explicit and prevents its group PASS.
Retain renewal and zero provider replay/calls. No activation/exchange fallback or offline Work is added.
R1 broad PASS claims are corrected by append-only addendum. Do not rewrite the historical JSON.
Previously supplied exact-start P1/P2 RED is historical proof; new candidate source/extracted regressions are mandatory. No need to fabricate another base RED run.

VERIFICATION / RECEIPTS
Run updated focused tests on source and extracted, I1 and core checkers, docs:check, bridge:guard, openapi:check. Run core because the shared harness may change. Frozen existing toolchain only.
Production runtime must be byte-identical to start, including client blob above; report inventory comparison.
Do not rerun unchanged local native/installed suites just to replace UNKNOWN. Current R1 remote native/installed completed logs were independently verified:
I1 run35069793516 client104708353296 and installed104708353496 SUCCESS.
Extension PR35069793510 native104708353281/core104708353139/Ozon104708353175/WBnodes104708353271 SUCCESS.
Docs35069793460/job104708352933 SUCCESS.
At review Server35069793457/job104708352831 and WB-browser104708353036/104708342236 remain IN_PROGRESS; no all-green claim and no rerun/cancel.
Actual tested PR checkout0c66a4fc0d98a7702d28f3301af2aa97d8b07d2b/treeeb42f95e850fa0dd0b31643b23eb0f4d869da02e has parents current maincfd63e5... and R1head90f3f5a. Extra32 paths are owner site/SEO/ingress/docs, not client changes.
Native CI package receipt is separately reported in completed log:1835439bytes/SHA256d3aa2c954b483d6d7b22ddea5bd2e4d09e1adb91738c7e399c0d6f5afb5e5352; repeat/source-extracted match, Chromium151.0.7922.34, synthetic provider0live. It was not independently downloaded.
Default local R1 receipt1834654/2c1f5765... remains separate. Do not transfer hashes across configurations or candidates.
For R2 publish exact current default package receipt and runtime parity. New applicable remote CI remains architect gate; lack of executor auth is not PASS.

PUBLICATION / TERMINAL
Normal FF same branch, reuse draft PR9; no merge.
Terminal exact task/start/final/tree/parents; code/test/evidence commits; changed paths; per-case Q1–Q7 outcomes including failures; gates and provenance; package receipt; production byte-identity.
Stop after terminal for architect review. If a prescribed case reveals a production defect, report exact failure, fixture preconditions and observed state; leave it FAIL.
No C2.2-B, offline Work/profile execution, server/Health, dependency upgrade, site/SEO/domain migration, deployment or release.
