TASK ID: SA-I1-SRV5-R2-20260916-01
STAGE: I1-SRV.5 — close the mandatory integration gate by fixing one test fixture's isolation.
ROLE: One implementation executor. Apply this exact architect-designed correction, run the prescribed regressions and gates, publish, then stop for review. No independent architecture, investigations, extra agents or parallel tasks.

REPOSITORY / BRANCH / EXACT START
Repository: MaksimUnimax/Seller_Agents.
Continue feature/server-i1-srv5-acceptance-2026-09-16, existing draft PR8.
Start head: 832b135f129154dae2a1ce726528fdb353e92292.
Start tree: 53238b426990508d20cf277b8ffb9797861d2f54.
Start parent: 39478f0b4e28dd875111ce28670396531a8efc4d.
Canonical main: 5d7c8853cc69dd95bc6e713cac3fb2aa0a63383c.
Fetch/read remote refs first; verify start and main exactly. Do not overwrite unexpected advancement, force-push, create another PR or alter PR7/main/Health branch. The prior R1 task has completed.

PROVEN PROBLEM / ARCHITECT DECISION
The R1 signing correction and new Playwright regression match the design and remain unchanged.
The R1 local pnpm test:integration FAILED: 1507 passed, 20 skipped, health-persistence beforeAll primary-key collision. This is not PASS, and "unrelated" is not a waiver for the mandatory gate.
Architect source inspection established:
- packages/server/db/src/adapter-registry.integration.test.ts uses adapter A = 00000000-0000-4000-8000-000000000001, inserts it as chatgpt, and afterAll only closes the pool.
- packages/server/db/src/health-persistence.integration.test.ts uses exactly that UUID as IDS.adapter, inserts it as fixture-ai, but beforeAll only calls runtime.ready() before INSERT. It performs neither schema reset nor migration; runtime.ready() is only SELECT 1.
- tests/integration/server/vitest.config.ts sets fileParallelism:false. Sequential execution prevents concurrency, but does not isolate residual rows or guarantee an accommodating file order.
- Existing adjacent DB integration suites reset public/drizzle and run the canonical migrations before seeding.
Apply that same isolation boundary to the health-persistence test suite. Do not change its IDs, assertions, production health implementation, constraints, migration definitions or the order of the complete test suite.
This is the current I1-SRV.5 acceptance-gate repair. It does not accept or implement Health/P8.4 B5; that separate branch's h3-health-persistence failure remains open.

ALLOWLIST
- packages/server/db/src/health-persistence.integration.test.ts
- docs/server/evidence/i1-srv5-r2-2026-09-16/README.md (new)
- docs/server/I1_SRV_5_REFERENCE_ACCEPTANCE_2026-09-16.md
- docs/server/I1_SERVER_EXTENSION_SYNC_2026-09-16.md
- docs/server/evidence/i1-srv5-r1-2026-09-16/README.md: factual SHA/history clarification only.
- docs/STATUS.md and docs/ROADMAP.md: factual SHA/current gate clarification only.
No other code changes. In particular do not change Playwright config/reference test, adapter-registry test, shared fixtures, Vitest configuration/sequencing, workflows, root dependencies, lockfile, production DB repositories, migrations, extension or Health feature branch. Do not edit architect STATE.

EXACT IMPLEMENTATION
In packages/server/db/src/health-persistence.integration.test.ts:
1. Import runMigrations from ./migrations.js.
2. Inside the existing beforeAll, immediately after await runtime.ready() and before any fixture INSERT:
   - await runtime.query with DROP SCHEMA IF EXISTS public CASCADE;
   - await runtime.query with DROP SCHEMA IF EXISTS drizzle CASCADE;
   - await runtime.query with CREATE SCHEMA public;
   - await runMigrations({ connectionString }).
3. Keep all existing fixture inserts, UUIDs, suite definitions, tests, assertions and afterAll pool closure unchanged. Do not reset between tests: this suite intentionally checks shared history, immutability and rollback across its own tests.
4. No ON CONFLICT, ignored exceptions, random IDs, renamed fixtures, retries, skips, conditionally disabled tests, or forced global execution order. Those hide the fixture dependency rather than fix isolation.
Use the existing task-owned disposable PostgreSQL only. Before any destructive test command verify DATABASE_URL points to that newly created loopback test database, not a shared or production database. No shared Docker pruning or data/process cleanup.

DETERMINISTIC RED / GREEN
Use Node24 and pnpm10.34.5 with frozen lockfile.
On the unchanged start source and one task-owned disposable DB:
A. Run pnpm exec vitest run --config tests/integration/server/vitest.config.ts packages/server/db/src/adapter-registry.integration.test.ts; require PASS.
B. In a separate Vitest invocation on the SAME database, without cleanup/reset between A and B, run packages/server/db/src/health-persistence.integration.test.ts with the same config.
Expected RED: beforeAll INSERT fails PostgreSQL23505 on ai_adapters primary key for the shared UUID; 20 health tests do not execute. Capture sanitized constraint/key/setup location and exit status. Do not call this an expected PASS of the suite.
The separate invocations force the relevant order without changing Vitest configuration.
After applying the exact correction:
C. Repeat A then B on a task-owned DB; both suites must PASS, all 20 health-persistence tests must execute.
D. Run the corrected health-persistence suite once more on that same DB without outside cleanup; it must PASS again. This checks that its own persisted immutable health rows do not break the next invocation.
These are concrete changed-risk checks, not retries-until-green.
If RED differs from the prescribed cause, report the actual sanitized failure and stop changing code; do not invent a new repair.

REQUIRED ACCEPTANCE
After the focused proof, run the full server acceptance cycle once on a separately fresh task-owned DB, in the workflow order:
pnpm lint; pnpm format:check; pnpm typecheck; the existing Playwright config regression; pnpm test; pnpm test:integration; pnpm db:migrate; pnpm openapi:check; pnpm bridge:guard; pnpm build; pnpm test:e2e; pnpm docs:check.
Full integration must exit0 with all required suites/tests executed, including all 20 health-persistence tests. On unchanged suite membership expect 39 files / 1527 tests, no new skips.
Full E2E must preserve both I1 reference tests and the existing 88-test set.
Use the fixed Playwright configuration unchanged; retain no private keys, tokens, raw envelopes, browser traces/screenshots/video in artifacts.
Do not run extra extension/Health feature acceptance outside the existing applicable workflow gates.
If environment resources fail, record the exact local failure; publish only the allowed correction and retain remote gates. Never mark failed/skipped/not-run as PASS.

DOCUMENTATION / SHA CORRECTION
The prior task again retained a 39-character main SHA. Obtain the actual full value via Git and verify it has length40 and equals 5d7c8853cc69dd95bc6e713cac3fb2aa0a63383c.
Correct every standalone occurrence of the truncated value in the allowlisted status/roadmap/SRV5/R1 evidence files. Verify no such 39-character standalone value remains there. Use the full value in terminal report.
Preserve original R1 local integration FAIL and initial disk-exhaustion BLOCKED as historical outcomes. Append the new deterministic RED/GREEN and full clean-DB gate results separately.
Remove the duplicated "refresh rotation continuity..." fragment in the synchronization handoff while updating that same verification paragraph.
Describe the actual current candidate and gate outcomes; I1-SRV.5 remains IMPLEMENTED_CANDIDATE / ARCHITECT_REVIEW_PENDING until architect verdict.
Do not claim full I1/C2, installed extension, live providers, email delivery, preprod or release.
C1 at56c81a3521c02502b65fd713aec890e5a30f038d remains accepted separately; PR7 unmerged. Health/P8.4 B5 remains NOT ACCEPTED; B6–B8 remain queued.

PUBLICATION / TERMINAL
Commit only allowlisted changes, recheck refs and push fast-forward to the same branch. Reuse draft PR8; no merge.
Record exact head/tree/parent and canonical base; distinguish code commit from later evidence commit and PR virtual merge SHA.
Required remote Server CI and Documentation CI stay gates. If authenticated visibility is unavailable, report that once and known workflow links; architect reads actual jobs. Do not invent run IDs or rerun workflows merely to wait.
Terminal: task ID; exact refs; changed paths; RED constraint/exit evidence; GREEN ordered pair and repeated suite results; full integration and E2E outcomes; remaining failures; evidence path; PR8.
Stop after terminal report for architect review. No next task or C2/Health launch.
