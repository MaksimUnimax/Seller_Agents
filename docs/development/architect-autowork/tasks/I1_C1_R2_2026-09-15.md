TASK ID: SA-I1-C1-R2-20260915-01
ROADMAP: Seller Agents / D2-I1 / I1-C1 / R2 correction of G1-G6.
ROLE: You are the single coding executor. Implement the architect's concrete decisions below, run the prescribed checks, publish one reviewable candidate and stop for architect review. Do not research/design a different solution, spawn agents, start another roadmap step, or accept your own result.

REPOSITORY AND START
Repository: MaksimUnimax/Seller_Agents.
Work branch: feature/extension-i1-client-2026-09-15.
Required starting published head: 9e80e8ad531f079b38bf03b9e29f171c87c477c0.
Historical line base: bc0cd0088ca50ba06021ea602a46bdd90de91378.
Current canonical main verified by architect: 5d7c8853cc69dd95bc6e713cac3fb2aa0a63383c.
Existing draft PR: https://github.com/MaksimUnimax/Seller_Agents/pull/7.
Fetch current remote refs first. Preserve unrelated work. Use an isolated checkout/worktree if the shared checkout is occupied. Do not reset/clean someone else's work, force-push, merge main into this branch, or touch the Health branch. If the required feature head changed or another existing executor job occupies this work, return the exact discrepancy without starting overlapping implementation. Do not rerun I1-C1-R1.

READ FOR IMPLEMENTATION
Read current AGENTS.md and the named files below in full. The owner's current unified-architect/single-executor direction supersedes historical Stream A/Stream B ownership boundaries and grants this client correction only.
The handoff branch docs/architect-autowork-handoff-2026-09-15 contains docs/development/architect-autowork/STATE.md and references/I1_C1_R1_REVIEW.md, I1_C1_R1_reproduce.mjs, I1_C1_R1_reproduction-results.json. Review-source snapshot ec1481dca4a2f5debfbacd2891decf8f17f7f160 is immutable provenance, not a feature base.
Also read docs/development/client-i1/README.md and docs/migration/evidence/extension-i1-client-2026-09-15/README.md.
Server reference only: docs/server/I1_SRV_4_EXTENSION_HANDOFF_2026-09-15.md from current main, packages/shared/src/index.ts and packages/server/compatibility/src/index.ts. Do not change these server contracts.
The previous independent review already established five failing observations and six successful controls. Its assertions intentionally detect defects; do not preserve the bad assertions as acceptance criteria.

EDIT SCOPE
Primary implementation: packages/control-client/src/client.js; apps/extension/src/application/runtime.js.
Focused tests: tests/regression/extension-core/client-i1/client-lifecycle.mjs, client-races.mjs; add client-r2.mjs there for the new acceptance scenarios.
Test support: tests/regression/extension-core/worker-harness.mjs only for reusable signing/deferred-response/storage-failure controls and deterministic readiness; no production stubs.
Native harnesses: tests/regression/extension-core/browser_application.py and tests/regression/extension-core/client-i1/installed_local_integration.py.
Gate wiring: tooling/checks/extension_i1.py; .github/workflows/extension-i1-ci.yml or .github/workflows/extension-ci.yml only if required to run/preserve these gates.
Documentation: docs/development/client-i1/README.md and a new r2/ subdirectory under docs/migration/evidence/extension-i1-client-2026-09-15/.
Read the following consumers to implement the explicit G3 closure without changing their unrelated behavior: apps/extension/src/background/context/runtime-context.js, executeManualCommand.js, attemptManualBatchDelivery.js, commitManualBatchDeliveryInsert.js, manualRecoveryForContent.js; apps/extension/src/application/delivery.js; apps/extension/application-patches.json; apps/extension/composition.json.
The central G3 routing correction is in runtime.js; do not rewrite every consumer. If a necessary edit outside this scope or a false premise is discovered, report the exact file/function and blocking fact to the architect instead of inventing a new architecture.

G1 — GENERATION-OWNED FLIGHTS
Cause: ensurePolling assigns flight.finally(...) to pollingFlight but cleanup compares with the different original flight. The settled owner never releases. Global unqualified flight reuse also lets cancelled/stale work block a new attempt.
Implement explicit operation-owner records for polling, activation and refresh, carrying the captured generation and relevant attemptId/deviceId/sessionId; refresh additionally retains its rotation ownership. Keep the same stored promise/owner identity in the equality check used by finally.
Join only a flight owned by the current logical context. Detach obsolete owners on reset/cancel/context invalidation; an old HTTP request may finish later, but a new generation must not wait for it. Old finally must never clear a newer owner. Do not invalidate a current pending/start merely to obtain singleflight.
Keep all durable mutations serialized. Use the full isCurrent(context) predicate before and after an awaited updater, not a weaker subset. Fence portal opening, exchange results, errors, persistence and cleanup. Preserve per-attempt start/exchange idempotency and bounded pending polling.
Tests: two completed logins with reset between them in the SAME worker, two real exchanges; cancelled held start then new start before releasing the old HTTP; cancelled held exchange then new attempt; release old success/error/finally while the new attempt is pending and after it authenticates. No extra portal, duplicate poll owner, old pending resurrection, or overwritten new authority.

G2 — ONE FORCED ROTATION AFTER BOOTSTRAP 401
Separate normal refresh freshness from an explicit forced refresh. bootstrap captures its original generation/device/session before awaiting refresh/request and never rebinds an old operation to a newer login.
On the first bootstrap 401: validate that captured context is still current; force the HTTP refresh even when the rejected access token's local expiry is in the future; retry bootstrap once with the rotated access token. Preserve current-context singleflight and durable rotation idempotency.
Pass the rejected access token internally to the forced-refresh decision. If another current-context rotation already replaced that exact rejected token, reuse the new credentials rather than rotate them again. Never join a refresh owned by an old session/generation.
Persist the rotation record before HTTP; retain its same refresh token/idempotency key after an ambiguous transport failure so the prescribed later retry replays the same rotation. A logical bootstrap recovery allows at most one rotation and one bootstrap retry, never recursive retry.
Final bootstrap 401 or current AUTH_REFRESH_INVALID clears current auth through the fail-closed invalidation path. Late old-session success/failure cannot rotate, clear, or retry with a newly logged-in account.
Tests must assert ordered routes and bearer changes: bootstrap(old)->refresh->bootstrap(new) succeeds; final second 401 denies and clears; concurrent 401s share one applicable rotation; lost refresh response retries the same persisted key; delayed old refresh/401 cannot harm account B in the SAME worker. Do not replace this last proof with two independently seeded workers.

G3 — KNOWN DENIAL MUST CLOSE WORK, DISPATCH AND DELIVERY
Cause: verifyV2 failure and signed browser MAINTENANCE throw before replacing old positive authority. ensureForIdentity then returns that authority.
Add one context-fenced restrictive authority transition. Use it for HTTP 403, final 401/auth invalid, malformed/oversized successful bootstrap responses, verification/unknown-key/schema errors, expired/incompatible signed policy, and an invalid/non-operational requested profile. Keep error classes distinguishable. A pre-response transport failure and audited transient 503 are not a terminal denial; do not implement full C2 offline policy here.
For a current known denial, synchronously replace in-memory authority with null and advance generation inside the serialized mutation before awaiting persistence or cleanup. Retain credentials for a recoverable non-auth policy/verifier denial; clear credentials/pending/rotation for terminal auth denial. Detach obsolete operation owners. Notify the existing runtime invalidation callback even if durable writing fails. Preserve a bounded lastError. A later successful current online bootstrap may recover; ensureForIdentity must never reuse the rejected positive snapshot.
Persist this restrictive state. If writing it fails, attempt removal of seller_agents_control_auth_v2 as the durable fail-closed fallback; after successful removal converge memory to signed-out as well. Never restore old authority on an exception. If both write and removal fail, current memory must remain denied and the result must explicitly expose a bounded persistence failure; do not claim that unchanged disk is safe across restart. Do not wipe stores, unrelated accounts, or all browser storage.
Runtime decision: saEnabled currently mixes application-mode selection with currentAccount availability. Change it to mean that the composed Seller Agents application has completed saReady (saCatalogEnabled), independent of login. It must not switch the composed carrier back to the old standalone route after auth is lost. Frozen donor/baseline code stays unchanged.
Keep account presence and current operational Work permission as separate checks. Add a local-only Work-authority assertion at saPendingGuard and saGuard; recheck permission/generation when saReadContext computes active, so cached guards close across awaits. In saHandleMessage require current Work permission for command admission, Work start acknowledgements and quota resume; keep auth/login, harmless status, and Finish/cleanup usable while denied. Existing delivery assertion/send-commit handling must remain active when signed out.
saAssertDeliveryOwner must deny missing/obsolete composed execution context and missing Work permission, not return success because saEnabled/currentAccount is false. The existing file-port read/mutate/chunk and recovery callers must therefore close too. Report public Work/button state as inactive while denied; cleanup callback is not the security gate.
These routing changes must cover ordinary text insertion/send, binary descriptor/chunk/recovery, queued commands and delayed provider results using the existing guards. Do not add control-plane HTTP on these local checks.
Tests: signed MAINTENANCE and invalid signature after previously valid active Ozon/WB Work; HTTP 403 and invalid profile/minimum-version denial; no new provider dispatch, insertion/send grant, binary read or replay after denial. Exercise held provider/delivery operations and cleanup failure so a successful cleanup is not the only protection. Signed-out composed runtime with seeded legacy global credentials/binding must not execute through the standalone fallback. Known denial write failure plus successful auth-key removal must remain denied after restart with seedAuthority:false. If both storage operations fail, assert current denial and explicit persistence failure. Late denial from old account cannot close account B. Two compatible same-context bootstraps must preserve active Work and issue no invalidation callback.

G4 — CONTRACTUAL VERSION COMPARISON
Replace the broken split(/[.+-]/,1) parser.
Use distinct browser and extension comparison functions. Extension input must match the full SemVerV1Schema grammar and 64-character bound from packages/shared/src/index.ts. Compare all major/minor/patch components, then SemVer prerelease identifiers; release is greater than its prerelease, numeric prerelease identifiers precede nonnumeric, compare numeric identifiers numerically and others by ASCII, shorter equal-prefix prerelease list precedes longer. Ignore build metadata for precedence. Split at the first hyphen and preserve the rest of the prerelease. Use decimal-string length/lexical comparison or equivalent exact integer handling, not floating-point rounding. Malformed input denies.
Browser versions follow compareChromiumVersionV1: 1-4 decimal components, no leading zero except zero itself, each <=2147483647, zero-pad to four, compare all four. Invalid input denies. Retain existing family mapping.
Use these comparators in validProfileShape and enforce non-null signed top-level extension minimum as well as profile/browser minima. Do not alter server/shared code or import Node-only modules into the browser.
Tests through signed profiles include 0.2.4 versus 0.99.0, equal versions, lower patch/minor, major transition, release versus prerelease, alpha.2 versus alpha.10, hyphenated prerelease, ignored build metadata, malformed input, and same-major browser minimum including fourth component. Recompute profile fingerprint and signature in fixtures.

G5 — REAL INSTALLED LOCAL AUTH HARNESS
In installed_local_integration.py set ROOT to Path(__file__).resolve().parents[4]. Before services start, require the exact root package.json, tooling/build/extension_composed.py, client-i1/api-harness.ts, client-i1/make-browser-config.mjs, and fixtures/application-chat.html to exist.
After #auth-reset click, wait for the popup's #confirmation, click its REAL #confirm and wait for signed-out account state and visible #auth-start. The old assertion requiring #auth-reset to remain visible is wrong: render hides it when neither authenticated nor pending. Native browser dialog.accept does not handle this custom confirmation.
Run actual API/portal/PostgreSQL device start, pending/approve/exchange and browser signature verification; two distinct accounts in the same worker without storage injection or a browser restart. Keep portal logout between accounts and confirm that B cannot see A's store. Record distinct authorization/exchange completion and same-worker continuity using safe test-only booleans.
Keep generated private signing material in the API process, public packaged trust from its export, and fixed development OTP explicitly labelled as not real email. Do not weaken API/auth/DB production code to pass.

G6 — DETERMINISTIC NATIVE FIXTURE RESTART
In browser_application.py use this exact setup:
1. Launch the first persistent Chromium context with the temporary profile directory and chosen runtime/options. Obtain this extension's worker and await completion of SellerAgentsControlClient.status()/normal init while storage is empty; assert unauthenticated. This deliberately makes the formerly racy ordering deterministic.
2. Seed the signed fixture authority once using the existing ephemeral fixture key and await storage.set. Set an ephemeral test-only JS sentinel on that worker; do not mutate production auth memory or call a reinitialization backdoor.
3. Close that whole persistent context and await closure. Relaunch Chromium with the SAME temporary profile directory, SAME runtime path, SAME executable/options and unchanged packaged trust. Do not regenerate or rewrite package config/key, clear storage, or seed again.
4. Obtain the new worker, assert the extension URL/identity is unchanged and the old JS sentinel is absent. Await normal restore/saReady and assert expected authenticated account and canWork:true before starting popup scenarios.
5. Install routes/fetch fixtures and popup active-tab port on the new context/worker, and run the existing Work/catalog/text/no-replay/Hide/Show/File/IDB/attachment/Finish/layout scenario there. Reassign context/worker variables and close the current context reliably on every failure.
Run this independently on generated source runtime and extracted ZIP. This is a native synthetic application fixture, distinct from G5's real local auth flow. No production reload-authority hook, timing sleep as the synchronization mechanism, or skipping the account wait.

VALIDATION AND PUBLICATION
Use Node 24 and pnpm 10.34.5, the repository's frozen dependency installation and pinned Python browser requirements. No unrelated dependency upgrades.
Add client-r2.mjs to BOTH source and extracted routes in tooling/checks/extension_i1.py. Use actual composed runtime modules and controls; do not test copied implementations or replace client/verifier methods with stubs. Add onStorageRemove failure injection only in the test harness if required.
First demonstrate the new focused acceptance assertions fail on the starting candidate for their relevant G1-G4 conditions. Then implement and run them green. Preserve all six successful controls from the independent R1 review. Do not rerun the entire historical review or require its deliberately bad assertions to stay green.
Run:
- python tooling/checks/extension_i1.py --output <new task-specific output>
- python tooling/checks/extension_core.py --output <new task-specific output>
- browser_verifier.py against the produced package as configured in extension-i1-ci.yml.
- Native browser_application.py on source and extracted package with the single per-build ephemeral public trust/private fixture input, exactly as extension-ci.yml prescribes, now using the restart setup.
- installed_local_integration.py with PRODUCT_CONTROL_PLANE_E2E=1 and an isolated disposable loopback PostgreSQL database.
- pnpm docs:check; pnpm bridge:guard; pnpm openapi:check.
Preserve the full existing Extension CI baseline matrix, common-core and native jobs plus both Extension I1 jobs and Documentation CI. Let applicable remote CI finish on the final published candidate. Do not repeat unrelated server suites locally solely to inflate coverage.
Reuse only this task's disposable resources. No shared Docker pruning, other-process kills or Business Bridge restart. If a named gate fails beyond the specified diagnosis, return its exact step/log and source fact; no arbitrary fixture weakening.
Keep development version 0.2.4; this is a new candidate commit, not an installed release. Preserve deterministic packaging and complete source-to-ZIP input/file/hash parity. Keep the immutable R1 evidence. Write new r2/ requirement mapping, commands/outcomes, package receipt, failure evidence where applicable and bounded limitations; update the current client README factually.
Push fast-forward to the existing feature branch after rechecking its remote head. Keep PR #7 draft and unmerged. Record final commit/head, source tree and whether CI actually checked a pure feature SHA or PR virtual merge; provide associated head plus actual checkout SHA and main parent. New code commits need the affected gates on their new SHA. If gh/Actions access fails, report the exact endpoint/command/error; do not claim GitHub is universally unavailable, and do not loop on the same access failure.
Do not overwrite the architect-owned handoff STATE branch. Architect updates its cursor and reviews the result.

NON-GOALS / PRESERVED INVARIANTS
No server implementation/shared schema/OpenAPI/migration change, no Health/P8 or feature/server-health-h3-p8-4 edit, no I1-SRV.5, full C2/offline implementation, D3/store sync, S1.2/email/preprod, deployment, browser-store release, monetization or live marketplace/AI calls.
Preserve mature Ozon common lifecycle, sequential explicit read-only packets, account/store isolation, pinned generation, unknown-outcome no replay, text/binary delivery ownership, one-hour buffer, packaged Ed25519 trust and local-only marketplace credentials. Ordinary data-plane dispatch/delivery performs zero mandatory control-plane HTTP.
No secrets, tokens/device codes, private keys, browser storage/profile, raw seller/ChatGPT content in Git or CI artifacts. Fixture keys remain temporary. Do not publish raw auth HTTP traces: record order and same/changed token booleans instead.

TERMINAL REPORT AND STOP
Return task ID; branch and starting/final full SHA/tree; G1-G6 -> exact changed functions -> meaningful test/outcome; retained controls; local gate outcomes; run/job URLs and actual checkout SHA; package filename/hash and byte-readback result; remaining failures/blockers and explicit proof scope.
Report IMPLEMENTED_CANDIDATE / READY_FOR_ARCHITECT_REVIEW only when the prescribed checks actually pass; otherwise report REWORK_REQUIRED or BLOCKED with exact facts. C1 acceptance belongs to the architect. Do not claim full I1/beta/installed live acceptance. Stop after the terminal report, with no next task.
