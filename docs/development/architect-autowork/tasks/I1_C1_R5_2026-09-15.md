TASK ID: SA-I1-C1-R5-20260915-01
STAGE: Seller Agents / D2-I1 / I1-C1. Correct the diagnosed popup blocker and finish remaining R4 acceptance assertions.
ROLE: One implementation executor. Implement the decisions below, run checks, publish, stop for architect review. No agents or independent architecture/research.

REPOSITORY / START
MaksimUnimax/Seller_Agents; branch feature/extension-i1-client-2026-09-15; PR#7 draft/unmerged.
Required base/head 60c3a34bdc1b6a5563fad59e8ac38c78b76e32d3; tree 243d944b3834f4c88c694f4c3f668ddc80bb7bbf.
Observed main 5d7c8853cc69dd95bc6e713cac3fb2aa0a63383c.
Fetch/check remote before work and publication. Unexpected advancement, conflicting local work or a previous active executor => report exact condition and stop. No reset, force-push, duplicate submission or parallel roadmap task. R4 terminal has been reviewed, not accepted as a whole.

R5-1 — POPUP STATE MUST NOT REQUIRE AN AI TAB (original G5)
Proven cause:
- R4 push run34975780030/job104403007844 checked out60c3a34 and failed waiting for #auth-code:64 observations of an empty hidden paragraph. PR run34975784338/job104403022717 failed identically on merge111598ce78535a3922ca427665aff3cd24805e06, parents main5d7c8853 and60c3a34.
- OTP request/verify and activation preview/account-option waits had already passed. This CI failure is not PostgreSQL initialization.
- apps/extension/src/application/popup.js queries the active tab. installed_local_integration.py opens popup.html as its own active tab, without an AI-tab override.
- runtime.js saPopupState first awaits tabIdentity(normalizeTabId(tabId)). The frozen worker's tabIdentity requires an AI content receiver. Missing/unsupported/extension tabs reject before auth status is read; popup action's refresh catches the error and never renders the pending code.
- Independent extracted-function probe reproduces zero auth-state reads for missing/unsupported tabs. Native browser_application.py masks this condition by overriding chrome.tabs.query(active) to the synthetic AI tab.

Decision and exact production change:
Modify only apps/extension/src/application/runtime.js, function saPopupState, for this production correction.
1. Initialize a neutral local identity with ai_id:null, origin:null, conversation_id:null, status:"unavailable", source:"none", chat_path:"".
2. Attempt normalizeTabId and tabIdentity in a narrow try/catch covering ONLY identity acquisition. On failure keep the neutral identity and no usable tab context. Do not catch auth, catalog, storage or other downstream failures.
3. Derive conversation_key only from a successfully validated AI identity. With no usable identity return conversation_key:null, pending Work:null, work:null, operation:null and the existing saPublicContext(null) inactive context.
4. Always obtain the real SellerAgentsControlClient.status and its account; list the account-scoped catalog only if authenticated, exactly as today. Auth pending/code must reach the existing popup renderer without any AI identity.
5. Preserve valid supported-tab behavior, including pending Start on a supported page without conversation_id.
Do not modify global tabIdentity/normalizeIdentity, saPopupSender, Start/dispatch/delivery guards or saEnabled. A successful read-only popup state must never grant Work or invent a RESOLVED AI. Existing popup rendering disables Start for identity.ai_id=null.

Tests:
- Add actual composed-worker cases in tests/regression/extension-core/application.mjs: own popup sender + missing tab, tab0, unknown positive tab/no content identity. SA_POPUP_STATE must return ok:true for signed-out, actual pending activation, and authenticated account-only state; pending auth code is present only in pending state; account/catalog preserved, neutral identity, no Work context. Use real client/signing fixtures, not status/authority=true stubs.
- Preserve supported-AI positive state and existing sender-denial cases. SA_WORK_START with missing/unsupported tab still fails and causes zero provider dispatch.
- Add a native popup-as-own-tab assertion to browser_application.py BEFORE the existing AI-tab-controlled popup scenario: no tabs.query override on this new page; restored fixture account/catalog renders, Start disabled. Close that page, retain all existing AI Work/delivery/restart checks.
- Run installed_local_integration.py unchanged in meaning: fresh profile, real local API/portal/PostgreSQL, existing-account OTP A -> signed account bootstrap -> catalog save -> portal logout204 -> real local-reset confirmation -> OTP B -> isolated catalog. No tab-query override, storage seeding, API-only substitute or browser restart between A/B.
- Keep exact success counts201 start,200 exchange/bootstrap, actual same-worker sentinel, distinct accounts/device-session tuples/authorization IDs, UNCONFIGURED Work:false, and CLOSED beta unchanged.
- In installed_local_integration.py add bounded failure diagnostics before closing context: stage, pending/authenticated/workAllowed booleans, code-present boolean and an allowlisted public error code when available. Catch diagnostic-read failure separately. Never dump DOM/status strings, pending objects, URLs, IDs, codes, tokens, cookies, screenshots or raw response bodies. This preserves evidence if a later assertion fails; do not increase waits to hide the root.

R5-2 — FINISH DELIVERING-OWNER DENIAL PROOFS (original G3)
R4's held-provider Ozon/WB tests are retained and accepted for their tail/no-advertisement scope. Their insertion check runs after owner.status=failed with a fallback delivery ID. It does not satisfy the required delivering-owner/file/recovery controls.
Files: tests/regression/extension-core/client-i1/client-r4.mjs or new client-r5.mjs; reuse application.mjs setup/start/execute/collected/deliver and APP-07 fakeIDB/binary patterns. worker-harness.mjs only for necessary fixture controls; tooling/checks/extension_i1.py wires new tests on BOTH source and extracted routes.
- For Ozon and WB create a real SA store, complete SA_WORK_START acknowledgement/identity, execute and await an actual delivering owner with nonempty outgoing result and real delivery_id.
- Use separate fixtures for denial before insert and denial after successful insert but before send. Trigger explicit bootstrap403; keep Work/binding/manual cleanup-write failure injection active through the denied checks, so stale durable records remain. Assert preconditions instead of forcing owner.status or inventing IDs.
- Call OZ_BATCH_DELIVERY_INSERT_COMMIT, OZ_WORK_DELIVERY_ASSERT and OZ_WORK_SEND_COMMIT with the real sender/owner/delivery tuple at the appropriate phase. No insert/click grant after denial; no new delivery advertisement or replay.
- For a real WB binary delivering owner, first demonstrate artifact META/CHUNK success and byte equality via APP-07's attachment port fixture. After denial, OZ_ATTACHMENT_RECOVERY_GET, OZ_ATTACHMENT_COMMIT, OZ_ATTACHMENT_ARTIFACT_META, OZ_ATTACHMENT_ARTIFACT_CHUNK and OZ_ATTACHMENT_SEND_COMMIT must expose no recovery payload, descriptors, bytes/chunks or attach/click grant. Cover denial before first attachment commit in a separate fixture. Assert no artifact read for denied requests where the owner guard rejects before IDB access.
- Exercise OZ_CONTENT_READY/OZ_CONTENT_SYNC recovery after denial with the actual identity; no replayable text/attachment payload or new delivery messages. Error/empty recovery is acceptable; do not require every status response to be an error.
- Seed fixture legacy credentials at OzonRuntime.STORAGE_KEYS.SELLER_CLIENT_ID/SELLER_API_KEY and PERSONAL_DATA_ENABLED in these negative fixtures; composed denial still holds and legacy autorun remains disabled.
- SA_AUTH_STATE and SA_POPUP_STATE remain available. Then restore cleanup-write availability and verify explicit OZ_WORK_FINISH is callable without reauthentication.
- Count explicit control bootstrap separately: ordinary denied guards add ZERO control-plane requests and ZERO new provider calls. Retain a valid-authority positive delivery control.
The existing production guards/application-patches implement this decision; do not change them speculatively. If a prescribed new assertion reveals another failure, report exact assertion, runtime function and bounded trace for architect decision; do not weaken expectations or invent a repair.

R5-3 — TWO MISSING SEMVER RELATIONS (original G4)
In client-r4.mjs's signed real-client matrix, retain existing hyphenated identifier cases but rename misleading numeric-before-text labels.
Add 0.2.4-alpha.2 versus minimum0.2.4-alpha.beta => denied; reverse => allowed.
Add 0.2.4-alpha versus minimum0.2.4-alpha.1 => denied; reverse => allowed.
Vary packaged client and signed minimum as existing matrix does; recompute profile/signature. No production comparator/schema change.
Oversized200/201/206 denial and prerelease+build are independently confirmed corrected in R4; preserve them, all existing owner-race tests, common-core provider splits/zero failure counts/cd4bce38 and 0.2.3 sender adaptation.

CHECKS / EVIDENCE / PUBLICATION
Keep development0.2.4 and pinned dependencies. Run the existing workflow commands with fresh R5 output directories:
- tooling/checks/extension_i1.py and extension_core.py, source/extracted;
- browser_verifier.py and browser_application.py source/extracted using the established fixture trust/restart setup;
- installed_local_integration.py on a disposable loopback PostgreSQL;
- pnpm docs:check, pnpm bridge:guard, pnpm openapi:check.
Show the popup regression RED on60c3a34 and GREEN after the change. No unrelated full-server reruns. Local PostgreSQL unavailable => record exact blocker, do not alter shared containers or prune disks; applicable remote installed gate remains mandatory.
Preserve R1-R4 evidence; write r5/README.md, assertion mapping R5-1..3 to original G1-G6, outcomes, limitations and deterministic ZIP receipt: full SHA256/size, repeat archive, source/extracted parity, byte readback. Scope claims to demonstrated behavior.
Recheck remote; commit/push fast-forward to the existing branch, keep PR#7 draft/unmerged. Do not edit architect handoff STATE. Allow normal CI on the new candidate; record applicable Extension CI, both I1 jobs and Documentation CI. Record actual checkout SHA and merge parents, not just workflow metadata. Missing gh/API access is a precise observation, not proof that CI is unavailable to the architect. Do not rerun old R3 job34969687336/104382641739 or duplicate this task.
NON-GOALS: server/shared schema/migrations/portal changes, beta admission policy, I1-SRV.5/C2, Health/P8, BFF idempotency and server SemVer backlog, D3/S1.2/email/preprod, live providers, deployment/release, dependencies or cosmetic refactoring. Preserve account/store/generation isolation, explicit sequential read-only work, no replay, one-hour buffers and zero mandatory control HTTP in data-plane guards.
TERMINAL REPORT: task ID; start/final full head/tree; R5-1..3 assertions and retained controls; local/remote status and actual job/checkout evidence; ZIP receipt; exact remaining blockers. READY_FOR_ARCHITECT_REVIEW only if required gates pass, otherwise precise BLOCKED or REWORK_REQUIRED. Stop for architect review; no next task.
