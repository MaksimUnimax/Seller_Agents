TASK ID: SA-I1-C2-1-20260916-01
STAGE: C2.1 — durable cache context and effective-time boundary, prerequisite for offline grace.
ROLE: One executor. Implement this exact design; no independent architecture, additional agents or parallel tasks.

REPOSITORY / BRANCH / BASE
Repository MaksimUnimax/Seller_Agents.
Continue integration/i1-c1-srv5-2026-09-16 and reuse draft PR9.
Exact start: 1ff322b3dd2b68c4f02e5390850cd2f1b9548186.
Start tree: f944feef7fb5cf7631df51d20fc70b2692ee907f.
Start parent: b8cd19ccfe9b7a1b92b9fa3376f94dccceb2c3ed.
Canonical main: 5d7c8853cc69dd95bc6e713cac3fb2aa0a63383c.
The synchronization task is complete and accepted in its bounded combined-source/package/native/installed-local scope. Read fresh remote refs and verify the exact start before changing anything. Do not overwrite unexpected advancement. PR7/8, accepted source branches, main and Health branch remain untouched.

PROVEN GAP / CHOSEN BOUNDARY
Architect inspected packages/control-client/src/client.js and the accepted I1-SRV.4 handoff. Current authorityBaseValid uses Date.now directly; restoreOnce binds only device/session/generation and does not record origin/version/browser/trust context.
Independent source VM with the exact Git blobs, real Ed25519 verifier and synthetic storage/clock established: valid Work=true; clock beyond expiry gives false; rolling it back gives true again. A fresh worker with changed packaged API origin or browser version also restores Work=true.
These are explicitly pending C2 criteria, not a regression introduced by the synchronization merge.
Implement durable context binding and a nondecreasing effective-time floor in the existing browser control client. Keep the current FRESH-only Work policy in this task. Full offline-grace eligibility, signed profile consumption and joint offline command-result acceptance remain C2.2+; do not declare C2 complete or change server contracts.

ALLOWLIST
- packages/control-client/src/client.js
- tests/regression/extension-core/worker-harness.mjs
- tests/regression/extension-core/browser_application.py
- tests/regression/extension-core/client-i1/client-cache-time.mjs (new)
- tooling/checks/extension_i1.py
- docs/development/client-i1/README.md
- docs/development/client-i1/C2_CACHE_TIME_2026-09-16.md (new)
- docs/migration/evidence/extension-i1-c2-1-2026-09-16/README.md (new)
- docs/migration/evidence/extension-i1-c2-1-2026-09-16/results.json (new)
- docs/server/evidence/i1-sync-2026-09-16/runtime-blobs.json: correct the count label only as specified below.
No other edits. Crypto verifier, packaged config, composition/version0.2.4, provider adapters, imported runtime, all old assertions, workflows, dependencies, server, contracts, migrations and architect STATE remain unchanged.

DESIGN 1 — EXPLICIT PERSISTED OWNERSHIP
Continue using seller_agents_control_auth_v2; do not add a second auth stack or arbitrary storage/message API.
Add authority.cacheBinding with strict shape:
cacheVersion="control_cache_binding_v1";
controlApiOrigin; portalOrigin; contractVersion; extensionVersion;
browser={family,version}, obtained with existing browserFamily/browserVersion;
detectedAi=null for account-only authority, otherwise the exact normalized family/surface/variant of the verified resolved profile;
trustBundleSha256=lowercase SHA-256 of UTF-8 verifier.canonicalJson(config.trustBundle).
Store this binding only after online verifyV2 and existing compatibility/profile/fingerprint validation succeed. Keep exact signed envelope/payload bytes unchanged and existing device/session/generation ownership checks.
Add state.cacheClock with strict shape:
cacheVersion="control_cache_clock_v1";
owner={controlApiOrigin,portalOrigin,contractVersion,deviceId,sessionId};
trustedServerTimeMs; effectiveTimeMs.
Both times must be finite, nonnegative, safe integral millisecond values within the valid Date range; effectiveTimeMs >= trustedServerTimeMs >= the cached signed serverTime.
The clock owner is the auth session, so a new profile/bootstrap in the same session cannot reset either floor. Nonterminal authority invalidation retains this clock; a genuinely new device/session initializes a new owner. Explicit local reset/terminal credential removal may clear it.
The binding and clock are local metadata, not newly signed server fields and not hardware anti-rollback guarantees.

DESIGN 2 — RESTORE AND CONTEXT MISMATCH
Refactor restoreOnce to separate strict cryptographic/static-profile validation from time eligibility.
Verify the saved envelope again with the current packaged ring; compare decoded payload with stored payload exactly; revalidate the existing account/AI/profile compatibility and fingerprint checks. Validate cacheBinding, cacheClock and ownership.
Require exact packaged origins, contract, extension version, browser family/version, trust-bundle digest and detected-AI/profile binding.
Do not infer a missing legacy binding from the new package. A legacy/malformed record without demonstrable origin/session ownership must fail closed and require fresh authorization. Preserve local marketplace/store data.
An origin mismatch must clear credentials before any request to the new origin; never send old tokens to the replacement origin.
For a demonstrably same-origin/session record with a version/browser/trust mismatch, discard cached authority, preserve structurally valid credentials and the existing session time floor, and require a new online verified bootstrap. Do not silently rewrite the old binding to current values.
Malformed/unsafe clock metadata cannot authorize Work. Do not reconstruct a lower floor from Date.now or signed expiry.
Use the existing restrictive invalidation approach: deny in memory before storage awaits, durable denied-state replacement, then removal fallback if writing fails. Preserve honest failure when both write and removal fail; do not promise restart protection after total storage failure.
A cryptographically valid but expired same-context record can be retained as non-authorizing cache with its floor; do not grant stale Work in this step. Expiry is not permission to reset the time metadata.

DESIGN 3 — EFFECTIVE TIME AND COMMIT BARRIER
Inside client.js add explicit helpers for current cache context, strict metadata validation, effective-time computation and a serialized cache authorization checkpoint.
Production clock sources are Date.now() and performance.now(); no shipped test hook or runtime-configurable clock. Missing/nonfinite/decreasing monotonic time fails closed instead of falling back to wall time.
Maintain a runtime anchor {effectiveNowMs,monotonicNowMs} and runtime effective high-watermark for the current clock owner.
Compute effectiveNow as max(wallNow, trustedServerTimeMs, durable effectiveTimeMs, runtime high-watermark, anchorEffective + elapsedMonotonic), rounding fractional milliseconds upward. Reject unsafe values or a backwards monotonic observation. Wall-clock rollback alone cannot reduce effectiveNow.
On first restore, initialize the anchor from validated persisted floors and current wall time. Across restart, retain the durable floor; do not pretend to measure elapsed powered-off time or resist deliberate user editing of storage.
Before returning any positive cached Work authorization, persist the increased effective floor through the existing mutationQueue. Only after successful storage and a final generation/device/session/authority identity check may the call return ALLOW/true/payload.
Advance the in-memory floor before awaiting persistence. A failed write cannot grant Work. A subsequent call may recover only after successfully persisting the nondecreased floor; no rollback to the old floor.
Persist an observation at or beyond expiresAt even when denying Work, and likewise at/beyond offlineGraceUntil. This prevents a restarted client from reclaiming an observed expired interval after wall-clock rollback.
Use half-open FRESH eligibility: effectiveNow < expiresAt. At equality and beyond, Work=false. Do not modify expiresAt/offlineGraceUntil or create a new TTL.
Keep authorityBaseValid/static profile checks pure with an explicit effective-time input; do not hide asynchronous storage inside a synchronous predicate. Separate the time-independent account/profile validator so an expired signature-valid record can be retained without allowing Work.
The checkpoint helper assumes restored state and does not call init. Public methods await init before calling it; restoreOnce calls that internal helper directly after validation, so initialization cannot await itself. The checkpoint owns exactly one queueMutation call. Online commit computes the same pure clock/context decision inside its existing commitIfCurrent rather than nesting a second queueMutation. Capture/recheck the owner and exact authority identity at entry and after awaits. PublicStatus consumes the resulting decision; it must not recursively call status/canWork.
Route API canWork, status.workAllowed, ensureForIdentity cached fast path and getAuthority's returned workAllowed through the same awaited checkpoint. publicStatus must not independently decide Work with raw Date.now. getAuthority must not expose workAllowed=true after a negative checkpoint.
hasAuthority/currentAccount may still represent verified account identity, but neither is a Work permission. Existing application guards continue requiring canWork.
A time-only checkpoint must not increment auth generation or interrupt valid Work. When time first denies previously usable authority, notify the existing authorityChanged cleanup after the serialized mutation has released; do not await reentrant application cleanup inside the storage queue.
Retain all generation/session guards around crypto, storage and HTTP awaits. A held checkpoint from A must not publish authority or a floor into session B.

DESIGN 4 — ONLINE BOOTSTRAP
For a verified new online snapshot, require the signed serverTime not to regress below the session's trustedServerTimeMs, and require the candidate to be fresh at the computed effective time.
Commit credentials/authority/cacheBinding/cacheClock consistently in the existing serialized path. Preserve the larger prior effective floor on every same-session refresh/bootstrap.
Only a successfully verified, compatible, fresh online bootstrap can replace rejected cache context.
Keep existing forced refresh after401, singleflight, bearer-change handling and same-idempotency lost-response retry unchanged.
No new polling/timers/HTTP request per ordinary command: canWork/status/checkpoint are local. ensureForIdentity may use the existing online bootstrap path when fresh matching authority is unavailable.
No offline fallback is enabled by this task.

HARNESS ADAPTATION — NO ASSERTION REMOVAL
worker-harness.mjs: add test-only injectable wall/monotonic clocks in its VM sandbox, with real defaults. Seed signed fixture records with the exact new binding and clock metadata, derived from the actual fixtureConfig/userAgent/credentials/payload. Use the injected clock when producing fixture times. Do not bypass production verification or seed arbitrary workAllowed overrides.
browser_application.py seed_authority: add the same metadata derived from the actual packaged config, actual navigator user agent, fixture device/session and signed serverTime; compute the trust digest with WebCrypto. Keep the real persistent-profile close/reopen, native Work, text/binary delivery and all existing assertions.
Do not change installed_local_integration.py: it must obtain the new metadata from actual online bootstrap.
Wire client-cache-time.mjs into tooling/checks/extension_i1.py for BOTH source and extracted runtimes, retaining every existing test.

REQUIRED RED / GREEN MATRIX
Use one focused new suite, deterministic clock/storage barriers, no sleeps-until-pass:
1. On the exact base, prove the existing expiry→wall rollback regression and changed-origin/browser restore acceptance. Record RED without claiming an installed-browser result.
2. Fresh same-context signed snapshot permits Work; exact expiresAt and later deny. Roll back wall time in the same worker: still denied.
3. Persist a fresh checkpoint, advance monotonic while wall rolls back: effective time advances. Restart with the same backing and rolled-back wall: floor is preserved.
4. Observe at/beyond expiry and grace, then restart: no reclaimed authorization. Verify signed payload/envelope and deadlines remain byte-identical.
5. Missing/NaN/decreasing monotonic clock and malformed/inconsistent persisted time metadata fail closed.
6. Hold/reject storage during a positive checkpoint: no authorization before successful persistence; write failure denies. Subsequent successful persistence can recover only with a nondecreased floor. Exercise restart after durable-denial write failure plus successful removal; record total-storage-failure limitation separately.
7. Table-driven fresh-worker changes: API origin, portal origin, contract version, extension version, browser family, browser version, trust-ring content, requested AI, device/session/generation. Cached Work denied in each case. Origin changes send zero credential-bearing requests. Same-context positive control succeeds.
8. Legacy missing binding/clock requires fresh authorization, with catalog/store data preserved. A same-origin version mismatch can obtain new authority only via actual signed online bootstrap.
9. Same-session new bootstrap cannot lower signed server time or effective floor. Later fresh compatible snapshot can renew authority; expired/invalid/incompatible snapshot cannot.
10. Hold verification/checkpoint from account A across a completed reset/new B activation; release A: no change to B authority/floor/account and no positive stale A result.
11. Start ordinary composed Work under fresh authority and verify repeated dispatch/delivery guards cause zero control-plane HTTP calls. After expiry, provider dispatch, delivery and attachment/recovery paths stay denied through existing guards. Keep positive controls proving these paths worked before expiry.
12. Account-only UNCONFIGURED remains authenticated but Work=false; all previous C1 races, denial, native and installed-local gates remain valid.
Show the focused RED on base, GREEN on candidate and meaningful source/extracted assertions. No V1 fallback or weakening existing tests.

VALIDATION / EVIDENCE
Run extension_i1.py and extension_core.py once on candidate, browser verifier and native application source/extracted routes, the unchanged installed-local API/portal/PostgreSQL harness, pnpm docs:check, pnpm bridge:guard and pnpm openapi:check.
Use only task-owned disposable DB/ports/browser profiles. Preserve all applicable remote PR9 workflows; do not edit filters or rerun jobs merely to wait. Unchanged server implementation needs no extra local full-server replay; applicable Server CI remains required.
Record package SHA/size, deterministic repeat, source/extracted parity and byte readbacks. Distinguish source VM, native synthetic and installed-local auth evidence; zero live provider calls.
In sync runtime-blobs.json, correct only the misleading compared_non_documentation_paths=990 label:990 is the total inventory excluding the five pre-existing allowlisted documentation paths, not the count outside docs/. Preserve the historical check and add the architect-verified actual non-doc count759 with zero mismatches at sync head1ff322b3. Do not recompute that historical receipt against the new C2 tree.
Keep C2.1 candidate status pending architect review. Explain that fresh-cache context/time is this step; offline grace, signed profile consumption and full joint C2 remain open. Preserve Health B5/B6–B8, S1.2/D3, full I1/D2 and release boundaries.

PUBLICATION / TERMINAL
Commit only allowlisted changes, push fast-forward to the existing integration branch, reuse draft PR9. No new PR, main merge, source-branch update, Health task, deploy or release.
Report task ID; exact start/final/tree/parent; changed paths; RED/GREEN matrix; all gate results with failed/skipped/not-run distinctions; package/evidence paths; actual remote run/job/checkout facts when observable. If executor lacks authenticated CI visibility, state UNKNOWN once; architect checks the connector.
Stop after terminal report for architect review. Do not launch the next step yourself.
