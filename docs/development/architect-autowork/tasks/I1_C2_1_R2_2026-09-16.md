TASK ID: SA-I1-C2-1-R2-20260916-01
STAGE: C2.1 — preserve valid activation across worker restart and complete outstanding acceptance.
One executor. Implement the concrete correction and tests below; no investigation/architecture delegation or parallel work.

REPO / BRANCH / BASE
MaksimUnimax/Seller_Agents; integration/i1-c1-srv5-2026-09-16; existing draft PR9.
Start13068c24354e8ec61bab6cf05598a5bb6ced7b65; tree035cbd5ee60919597ae29f56aa1ed4240950fb06; parent/code17d54dc3a8577ae6d46736608c0bbed909b70d1a.
Canonical main5d7c8853cc69dd95bc6e713cac3fb2aa0a63383c.
Verify current refs; do not overwrite unexpected advancement. No repeat of R1. The previous report used the old task ID; it corresponds to R1 by exact base, changes and evidence.

ARCHITECT VERDICT / PROOF
REWORK_REQUIRED. Independent probes on exact client blob a6a2e7d18376f01fcfd73d0ca2452f2485723b42 confirm that the earlier origin leak, monotonic regression, lost elapsed time and held-write expiry bug are corrected. Preserve those implementations.
New regression: create activation through actual startActivation with a synthetic201 response; persist the pending attempt with current authContext and hold its token exchange. Recreate the worker with unchanged config/backing. Before restart: phase=pending, authContext present, credentials=null, one exchange. After restart: pending=null, exchange count still1, lastError=STORED_CREDENTIALS_INVALID.
Cause: restoreOnce treats every saved record lacking valid credentials as terminal corruption. Credentials=null is normal before exchange.
validAuthContext also checks only contractVersion type, not equality with config.contractVersion; complete that prescribed check while fixing pending restoration.
The current “held-write expiry” regression releases the write and awaits true BEFORE advancing to expiry. Its comment/receipt claim a same-call expiry veto which it does not test. Required storage-removal, exact-owner and composed-expiry cases from R1 remain incomplete.
Architect verified I1 CI35057935403 jobs104671924481(installed) and104671924614(client) SUCCESS on checkout3e69df315214546b32b09c411fcdadf754ebdcdc, whose tree equals the candidate. CI success does not close the regression or missing assertions.

ALLOWLIST
packages/control-client/src/client.js
tests/regression/extension-core/client-i1/client-cache-time.mjs
tests/regression/extension-core/worker-harness.mjs — only necessary deterministic fixture/latch support
docs/development/client-i1/C2_CACHE_TIME_2026-09-16.md
docs/migration/evidence/extension-i1-c2-1-2026-09-16/r1/README.md — factual review addendum only
docs/migration/evidence/extension-i1-c2-1-2026-09-16/r2/README.md (new)
docs/migration/evidence/extension-i1-c2-1-2026-09-16/r2/results.json (new)
No other changes. Preserve original/R1 results.json and failure history.

FIX — RESTORE BY AUTH STATE
In restoreOnce distinguish:
1. No credentials (null/absent): normal signed-out or activation state. Do not call invalidateKnown solely for this condition. No authority/rotation/cacheClock may authorize without credentials; discard orphan credential-dependent fields if present, but preserve a separately valid pending/starting attempt.
2. Non-null malformed credentials: terminal denial using existing in-memory-first write/removal fallback.
3. Valid credentials: retain R1 ownership/clock validation independently of authority; mismatches remain terminal. Keep same-origin authority replacement and floors unchanged.
Validate pending independently before publicPending, portal opening or polling:
- validAuthContext requires exact current API origin, portal origin AND contractVersion, plus existing strict shape/version;
- phase=pending requires validPending, valid future expiry and the same authContext;
- phase=starting requires a nonempty string attemptId, valid startIdempotencyKey and exact authContext; no deviceCode/exchange key is required before receiving the start response;
- unknown/malformed/legacy/mismatched attempt is discarded durably with existing write/remove fallback and no old secret/request reuse.
For valid restored pending keep generation, attemptId, authorizationId, deviceCode, exchangeIdempotencyKey and expiry unchanged. After initialized=true resume through the existing generation-owned ensurePolling exactly once.
For valid restored starting keep attemptId/startIdempotencyKey; do not poll without deviceCode. The next startActivation retries with those same keys; do not allocate a new attempt merely because the worker restarted.
Do not renew pending expiry. A normal signed-out restore must not increment generation or report STORED_CREDENTIALS_INVALID just because credentials are null.
Keep init/queue ownership safe: do not await public init-dependent polling from within restoreOnce before initialized. Keep old worker callbacks fenced; no weakening origin ownership to restore activation.

TESTS — EXACT CASES, BOTH SOURCE AND EXTRACTED
Extend the already wired client-cache-time.mjs. Keep existing assertions. Use injected wall/monotonic clocks and deterministic latches. Do not silently seed a new authority after removing the auth key: restart with seedAuthority:false.

T1 valid pending restart, RED on exact13068c2 and GREEN after fix:
Start from signed-out; call real startActivation; return201 pending, hold exchange. Close worker and recreate with the same backing/config/current wall. Assert pending/generation/all attempt keys/expiry unchanged, exchange resumes exactly once with the same key/code, and no new device-start request occurs. Complete activated exchange and signed bootstrap; assert authenticated account and new session-owned clock. Do not log secrets.
T2 valid starting restart:
Hold the start response after phase=starting is durably stored; recreate worker; startActivation reuses start key/attempt. Complete the idempotent response and exchange. Do not release an HTTP promise in a closed-worker VM as though a terminated worker can still execute callbacks; same-worker stale responses are covered in T6.
T3 negative pending controls:
Individually change API origin, portal origin, contract, remove authContext, corrupt starting/pending shape, or expire the attempt. Assert no old deviceCode/idempotency key leaves via exchange or portal; new start uses fresh keys. Include same-context positive control and normal signed-out restart with no spurious credential error.
T4 actual completion veto:
Hold a positive checkpoint write; assert the public promise remains unresolved. WHILE HELD move clocks to exact expiresAt, then release. That same call must return false and durable denial floor must be >=expiry. Restart with rolled-back wall and assert false. Separate still-fresh held-write control returns true. Add equivalent status/getAuthority denial assertions. Correct the misleading R1 test comment/receipt claim with a factual addendum.
T5 storage failures:
With seeded valid authority, force checkpoint set failure and successful removal; false immediately, key absent, restart seedAuthority:false remains denied. Separately fail set AND remove; false in memory, record restart limitation without claiming durable protection; recover only after successful persistence of nondecreased floor.
T6 ownership/renewal:
Same-session later signed fresh bootstrap preserves or raises both floors; lower signed serverTime/expired/incompatible responses cannot grant Work. Force terminal refresh invalidation with an old high floor, activate different device/session, then signed fresh bootstrap: new session does not inherit the old floor.
Retain held A response across completed reset/B activation and add B envelope/clock byte comparisons. Hold checkpoint storage, queue reset/B activation, prove serialization; after B commits no old decision or cleanup mutates B. Test replacement by a different signed snapshot in the same session: a decision for the old envelope cannot authorize the new one. Record actual ordering, do not require B to commit through the locked mutation queue.
T7 composed expiry — mandatory unfinished R1 criterion:
Read client-r4.mjs and client-r5.mjs for established startStore/collect/binaryCommand/fakeIDB fixture patterns; copy necessary helpers into the focused suite without changing old files or assertions.
For Ozon and WB start actual composed Work via SA_STORE_SAVE, SA_WORK_START and OZ_WORK_PENDING_IDENTITY. Execute the established seller_info command with synthetic provider response; prove real provider dispatch and a delivering owner. Fresh repeated local guards produce zero control-plane HTTP.
For text delivery prove OZ_BATCH_DELIVERY_INSERT_COMMIT succeeds before expiry; after exact expiry require denial from OZ_BATCH_DELIVERY_INSERT_COMMIT, OZ_WORK_DELIVERY_ASSERT and OZ_WORK_SEND_COMMIT for the captured real owner/delivery IDs.
For WB binary response, before expiry use OZ_ATTACHMENT_COMMIT, OZ_ATTACHMENT_ARTIFACT_META and OZ_ATTACHMENT_ARTIFACT_CHUNK; capture the ACTUAL original_provider_file artifact_key and compare bytes. After expiry reuse that same real key/owner for attachment commit/meta/chunk/recovery/send-commit: no bytes/descriptors/recovery and no authorization. A fabricated missing artifact key is not proof.
Use the R5 test-only cleanup-write rejection pattern if necessary to keep an old owner available for negative guard tests; do not reject AUTH clock writes. Wait for the explicit cleanup-attempt latch before measuring post-denial IDB reads. Guard denial must precede artifact reads.
OZ_CONTENT_READY/OZ_CONTENT_SYNC must not recover outgoing content after expiry. A new denied command must not invoke the provider; no additional delivery advertisement, provider replay or control-plane HTTP from these local guards. Preserve fresh positive controls and account-only Work=false.

Do not skip T5–T7 or substitute old unrelated C1 tests. Report a named outcome per actual case; process count98 is not a case count. If an unexpected failure cannot be corrected by the specified restore fix/test fixture setup, report exact facts for architect decision instead of inventing a runtime redesign or declaring PASS.

VALIDATION / RECEIPTS
Run focused source/extracted tests and candidate extension_i1.py, extension_core.py, browser verifier and unchanged native application source/extracted. Run installed_local_integration.py with task-owned disposable PostgreSQL as in R1; no shared containers/data cleanup. Run docs:check, bridge:guard, openapi:check.
Keep applicable PR9 workflows enabled; no workflow reruns merely to wait, filter changes or redundant local full-server replay.
New ZIP: full SHA256/size, repeat archive, source/extracted inventory and byte parity. Separate VM/native/installed-local/live scopes; zero live provider calls.
R2 evidence uses exact task ID SA-I1-C2-1-R2-20260916-01. R1 addendum identifies the original ID mislabel and the unexecuted/misdescribed cases without erasing its historical receipt. Do not count D twice when it is assertions inside A. Document which RED is independently architect-proven and which was executed locally.
This fixes current C2.1 acceptance; do not start offline grace, signed-profile consumption, C2.2, Health, merge, deployment or release.

PUBLICATION / TERMINAL
Commit allowlisted files only; fast-forward existing integration branch, reuse draft PR9. Do not touch main/PR7/PR8/source branches/Health.
Terminal report: exact task/start/final/tree/parents and code/evidence commits; changed paths; T1–T7 named results including every failed/skipped/not-run; gate results; package receipt; evidence paths; observable remote head/CI run/job/checkout facts. No invented remote PASS.
Stop after terminal for architect review. Architect takes the next step without another owner command.

