TASK ID: SA-I1-C2-2A-R1-20260916-01
STAGE: C2.2-A correction and completion of the already assigned acceptance.
One executor implements the following design. No independent architecture or parallel task.

REPOSITORY / REFS
MaksimUnimax/Seller_Agents.
Branch integration/i1-c1-srv5-2026-09-16; reuse draft PR9.
Exact start ec9c23da45091255e0ba53914b3682539ef0b797.
Tree eeeb24dfea47f714b38d6997550073e468aad58c.
Parent a553d1f80769528544ee1c90d1305edb51d9d71f.
Current main at review e5f302d2d0334a106fa046b8dee4e8df6e38e583.
Main contains owner-authorized Octoport/site/ingress changes. Preserve them; do not merge/reset main into this task, modify the migration, or relax exact origin binding. Verify refs before work and before normal fast-forward publication.

VERDICT / PROVEN DEFECTS
C2.2-A REWORK_REQUIRED. C2.1 acceptance remains intact.
Independent probes ran exact published client blob026740435bdab9c07b826d231874b75aa6e6ece8 with real fixture Ed25519 and unchanged production client:
P1: access token expired; only /v1/auth/refresh returned503 with error.code BOOTSTRAP_UNAVAILABLE. bootstrapWithPolicy returned CACHE/FRESH without any /v1/bootstrap request. The HTTP fallback predicate lacks endpoint provenance.
P2: at offlineGraceUntil, keep access fresh, hold the second AUTH write (the actual denial write), then begin a later raw bootstrap and release the successful write. Old policy returned AUTH_DENIAL_PERSISTENCE_FAILED, performed one remove, and deleted AUTH although neither set failed. persistCacheDenial catches its own CACHE_ACQUISITION_OBSOLETED exception as a storage failure.
The full A–H declaration is unsupported by the published test file: several assigned classification, completion/restart, both-fail storage, context and reset/crypto/storage race cases are absent.
Native and installed-local CURRENT CI passed independently; do not use these successes to waive the proven defects.

ALLOWLIST
packages/control-client/src/client.js
tests/regression/extension-core/client-i1/client-offline-policy.mjs
tests/regression/extension-core/worker-harness.mjs — deterministic test barriers only if needed
docs/development/client-i1/C2_OFFLINE_POLICY_2026-09-16.md
docs/migration/evidence/extension-i1-c2-2a-2026-09-16/README.md — factual addendum only
docs/migration/evidence/extension-i1-c2-2a-2026-09-16/r1/README.md — new
docs/migration/evidence/extension-i1-c2-2a-2026-09-16/r1/results.json — new
No changes to old result JSON, checker registration, server, contracts, application dispatch, profile execution, site/ingress or other paths.

IMPLEMENTATION
1. Bind eligible HTTP errors to the actual request endpoint.
Keep existing private transport provenance. Add a private WeakMap for HTTP-error provenance.
In request(), register the endpoint on the exact failure object created for a received non-OK HTTP response, before throwing it. Body/size/parser/local exceptions do not receive synthetic HTTP provenance.
Replace the policy503 predicate with all of:
private HTTP provenance is exactly endpointFor("/v1/bootstrap");
failure.status===503;
failure.code==="BOOTSTRAP_UNAVAILABLE".
No decision from status/code alone or from absence of transport provenance.
Refresh503, including one with BOOTSTRAP_UNAVAILABLE, must propagate as an ineligible failure. Preserve preflight refresh no-response fallback, bounded401 refresh and durable terminal invalidation.

2. Separate storage failure from obsolescence.
In persistCacheDenial, narrow try/catch to await persist(next) itself. Only an actual rejected set enters AUTH-removal fallback. After a successful set, set runtimeFloorNeedsPersistence=false, then perform policyCurrent check OUTSIDE that catch and throw CACHE_ACQUISITION_OBSOLETED if false.
Apply the same narrow try/catch structure to the increased-floor write inside cachedBootstrapCheckpoint; remove reliance on checking exception.code inside a catch that contains non-storage work.
Obsolescence after a successful write must not remove AUTH, report storage failure, mark a new owner's authority denied, or roll back the persisted floor.
Keep genuine write failure behavior: denied call, memory floor retained, removal attempted; both set/remove failures honestly reported. Preserve the existing mutation queue, no nested queues or second storage authority.

3. Complete the prescribed public-return fence.
bootstrapWithPolicy currently returns await cachedBootstrapCheckpoint(...) directly. Await into a result, recheck policyCurrent(capture) after the queue promise resumes, and only then synchronously return the result. No further await between final check and return.
For ONLINE policy results, record the exact committed signed-authority identity from the online commit in the private attempt object. After awaiting bootstrapOnline, require current sequence/context AND exact current committed identity before returning ONLINE. Retain raw bootstrap's payload return shape.
This is the originally required public-consumer ownership check, not a new offline Work grant.
Do not set authority.workAllowed=true or runtimeLastCheckpointAllowed=true on the cached path, and do not wire this API into Work consumers.

REGRESSIONS / ACCEPTANCE COMPLETION
Use real signatures, public privileged client APIs and deterministic test-only barriers. Source and extracted routes are both required. Retain the passing cases.

R1-1 exact-start RED/candidate GREEN for P1:
matching signed cache; expired access; refresh returns503 BOOTSTRAP_UNAVAILABLE.
Expect rejection with that503, zero bootstrap requests and zero CACHE result.
Positive paired case: fresh access; bootstrap itself returns the same503/code; expect CACHE.
Also inject a local failure carrying the same status/code outside request's HTTP-error construction: never eligible.

R1-2 exact-start RED/candidate GREEN for P2:
create valid signed authority, keep access fresh through grace, advance to exact grace.
Hold the actual denial AUTH write after any increased-floor write; assert which write is held.
Begin a later raw bootstrap while the old queue transaction is held; keep its HTTP response unresolved.
Release successful denial write.
Expect old error CACHE_ACQUISITION_OBSOLETED, removeAttempts=0, AUTH remains present with nondecreasing expired floor; no AUTH_DENIAL_PERSISTENCE_FAILED.
Complete newer valid signed bootstrap afterward and prove its envelope/session/floor remain owned by the newer result.
Do not accidentally exercise a refresh-intent write or the first positive-floor write instead of the denial write.

R1-3 finish original A/B/C:
The exact-base adapter may capture the actual old online rejection, but the RED assertion must require the intended CACHE outcome; do not early-return from a LEGACY_ONLINE_ONLY branch and count it PASS. Never require a newly introduced error code on the old raw TypeError.
Add missing oversized200/503, unknown key, bad signature, signed incompatible browser/extension/profile, signed server-time regression and local-storage-error controls. Preserve wrong/malformed503,429/500/502/504, body-read-after-Response and malformed200.
Keep successful preflight transport fallback with rotation intent, denied bootstrap401→forced-refresh transport, and preflight/forced refresh401/403 terminal paths. No activation/exchange cache path.
For each row assert the actual response/failure source and no cached result; generic assert.rejects alone must not hide fixture/setup failure.

R1-4 finish original D:
Add expiresAt-1 FRESH and grace-1 STALE beside existing equality/after controls.
Hold a real fallback checkpoint write before grace, move clocks to grace while held, release: same call must deny and persist the expired floor.
Use resolved-AI stale cache to assert canWork=false, status.workAllowed=false, getAuthority.workAllowed=false after successful CACHE acquisition; account-only denial is not that proof.
Advance monotonic while wall rolls back, persist consumed grace, recreate worker with seedAuthority:false and rolled-back wall: cannot reclaim consumed time. Test both a still-eligible floor and the expired floor.
Never rewrite signed times as the clock advances.

R1-5 finish original E:
Exercise failed set/successful remove AND failed set/failed remove.
Assert no result on failure, exact attempts, safe error, in-memory floor and honest restart limitation.
Restore storage and re-evaluate normally.
Keep explicit catalog marker and real fixture IDB records unchanged; “surface not involved” is not an assertion.

R1-6 finish original F:
The existing account-only cases do not cover the assigned context dimensions.
Exercise policy acquisition with changed API/portal origin, browser/version, extension version, contract, packaged trust ring, requested AI, device/session/account and differently signed envelope.
Use packagedConfig/userAgent and real signed fixtures, valid restore controls, and existing harness clock options. Distinguish a restore-level denial from an in-flight policy identity mismatch in outcomes.
No cache must preserve the original online failure. Account-only cache must return configuration only for account-only context.
Retain the existing C2.1 context tests; do not count their fresh-only API coverage as new fallback coverage.

R1-7 finish original G/public return:
Add held policy network→localReset→real B activation/exchange/signed bootstrap; release old A after B and assert no old result or B mutation.
Add held real cache verification and held AUTH checkpoint transactions with later raw invocation, same-session differently signed replacement and403. Keep published passing later-raw/later403 network cases.
Queue-locked storage prevents B commit until release: assert that ordering rather than demanding impossible concurrent publication.
Add a deterministic boundary between checkpoint completion and policy public consumer, proving the final fence rejects obsolete acquisition. Test-only barriers may wrap actual verification/storage scheduling; do not mutate production closure state or bypass signature verification.
Record B generation/account/envelope/clock before releasing obsolete work and compare afterward where that ordering is possible.

R1-8 retain original H:
ONLINE renewal returns the newly verified payload and replaces cache.
Work guards remain fresh-only and add zero control-plane requests.
No provider replay or live calls.

EVIDENCE
Replace broad inferred A–H claims with an explicit case ledger: test label, assertion/scenario, source/extracted outcome, and missing/failed/skipped entries. All prescribed cases must exist before claiming the group PASS.
Preserve initial results.json as historical evidence. Append README correction that initial A–H coverage was incomplete and that the current receipt existed in terminal while the initial evidence file still said UNKNOWN.
Publish current default-development and ephemeral-key native receipts separately; full hash/size, repeat archive and byte parity. No historical package hashes as current evidence.

Existing current-task remote facts:
I1run35066457056: client104697816433 and installed104697816751 SUCCESS.
Extension PRrun35066456987: native104697809040 SUCCESS source/extracted Chromium151.0.7922.34,0live calls; core104697809262,WB-nodes104697809203,Ozon104697809452 SUCCESS.
Docs35066457014/job104697729787 SUCCESS.
At architect snapshot Server35066457022/job104697802488 and WB-browser104697809313 remained IN_PROGRESS; no all-green claim. Additional pushrun35066452300 exists; do not rerun/cancel it merely to wait.
Actual PR checkoutbc3da6a062b1bdcd49badb3879ac2adc03f40a97/treecec0617f7188d19d3bb718affa12972e5aea3470 includes current main e5f302... plus candidate. It is NOT tree-identical to candidate: additional files are authorized Octoport/site/ingress documents/assets/workflow. Control client/runtime changes are the candidate's.
Executor local UNKNOWN/not-run history remains distinct from authenticated remote PASS.
No need to await old CI to implement these already proven corrections; no bypass of new candidate gates.

VALIDATION / PUBLICATION
Focused exact-base RED for both proven defects, candidate GREEN on both routes and the completed case ledger.
Existing I1/core, browser verifier, unchanged native source/extracted, installed-local API/portal/PostgreSQL, docs:check, bridge:guard, openapi:check.
Use the repository's frozen supported Node/pnpm dependencies; prepare the task-owned workspace rather than declaring missing pnpm/node_modules the final validation strategy. If an actual resource failure prevents a run, report it truthfully and retain the applicable remote gate.
Task-owned PostgreSQL only. Preserve per-run matching ephemeral private/public config. No unrelated full-server local replay.
Normal fast-forward integration branch; PR9 remains draft, no main merge.
Terminal: exact task/start/final/tree/parents, code/evidence commits, changed paths, R1-1–8 outcomes, all gates with scopes, separate receipts, actual remote facts or UNKNOWN.
Stop after terminal for architect review.

NON-GOALS
No C2.2-B/Work grant, profile execution, feature/capability mapping, scheduler, provider replay, server/contracts, dependency upgrades, arbitrary refactoring, migration/site/ingress, Health, deployment or release. C2.1 remains accepted; full C2/I1 remains open.