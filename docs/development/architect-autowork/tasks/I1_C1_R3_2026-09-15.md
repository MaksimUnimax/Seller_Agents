TASK ID: SA-I1-C1-R3-20260915-01
STAGE: Seller Agents / D2-I1 / I1-C1, remaining G1-G5 correction. G6 native fixture restart is retained.
ROLE: Single implementation executor. Implement the architect's specified solution and tests, publish one candidate, return facts and stop. No agents, architecture delegation, new roadmap work or self-acceptance.

REPOSITORY / START
MaksimUnimax/Seller_Agents; feature/extension-i1-client-2026-09-15; draft PR #7 remains unmerged.
Required starting base/head: 0b2a1776ff484d2410b3a43d338ec5548e73f019, tree 3a83601109f7cb643e06d923b1da36691f9b0014.
Main observed: 5d7c8853cc69dd95bc6e713cac3fb2aa0a63383c. Historical line base bc0cd0088ca50ba06021ea602a46bdd90de91378 is not the implementation start.
Fetch/check the exact remote feature head before work and before fast-forward publication. If unexpectedly advanced or an earlier executor job is still active, stop with facts; do not reset, duplicate or force push. Preserve existing unrelated work.

PROVEN R2 FINDINGS
1. Exact-source independent VM probe: bootstrap HTTP 200 body >1 MiB throws CONTROL_RESPONSE_TOO_LARGE but authenticated and workAllowed both remain true. request throws before verifier; bootstrap catch invalidates only 401/403.
2. Exact-source independent VM probe with credentials and no authority: correctly signed ACTIVE account/device, supported compatibility, ai={status:UNCONFIGURED}, accessBasis=NONE is rejected BOOTSTRAP_PROFILE_INCOMPATIBLE. BootstrapService.issueV2 starts with UNCONFIGURED when request omits detectedAi; ensurePolling calls bootstrap without detectedAi. Initial account login cannot complete. This is separate from the CI failure before approval.
3. R2 client-r2.mjs G1 and G2 return bootstrap 403 and assert signed-out; they prove neither two successful logins nor successful 401 recovery. Missing signed-version matrix, held same-worker owner races and composed denial/restart controls remain acceptance gaps.
4. Exact feature CI installed job 104368454586, run 34965374842: PostgreSQL healthy; installed_local_integration.py line 91 fails re.search(...).group(0) because #auth-code has no match. The harness does not await code rendering and checks portal URL before its asynchronous redirect. Local disk exhaustion does not explain this remote failure.
5. Exact feature CI common-core job 104368455663, run 34965374971: core-source-full-worker-green fails OZ_SAVE_GLOBAL_SETTINGS with LEGACY_ACTION_DISABLED. tooling/checks/extension_import.py generates run_composed_full_worker_gate.mjs from frozen run_full_worker_batch_gate.mjs and adapts only the sender; its setup remains legacy save/bind/resume/manual. Restore neither legacy fallback nor acceptance of these mutations.

A — CLIENT AUTHORITY AND RESPONSE CLASSIFICATION
File packages/control-client/src/client.js, request/readLimitedBody/bootstrap/restore validation.
Preserve size limit, verifier, pinned trust and strict schemas. Carry HTTP status/response classification when readLimitedBody throws. An oversized 2xx bootstrap is a known invalid authority response: invalidateKnown for the originating context before rethrow, preserving the storage failure semantics. A final 401 or any 403 must invalidate even when its body exceeds the limit. Do not classify a 503/transport failure as a signed policy denial; no offline redesign.
Separate verified account-only bootstrap from operational Work:
- After signature/schema, active account/device, unexpired snapshot, supported browser, allowed extension status and actual top-level extension minimum checks, permit ai.status UNCONFIGURED only when no detectedAi was requested.
- Store the verified envelope/account as authority with workAllowed:false and requestedAi:null. Login/catalog/status may authenticate; Work stays false. No profile fabrication, entitlement grant or implicit profile fallback.
- If replacing an operational authority with account-only authority, advance generation and invoke existing authority-change cleanup so old Work/delivery owners cannot remain current.
- A requested AI returning UNCONFIGURED is denied. RESOLVED still requires the exact local family/surface, shape, minima and matching signed fingerprint. Invalid signature/schema/profile, MAINTENANCE, unsupported or incompatible requested profile remain fail-closed.
- Use the same account-only versus operational validation on normal storage restore; a legitimate account-only record restores authenticated with Work false. Invalid stored record never regains Work.
Do not modify BootstrapService, API contracts or fake a RESOLVED profile in api-harness.ts to hide the login defect.

B — FINISH CONTEXT OWNERSHIP
File packages/control-client/src/client.js: bootstrap, refresh, startActivation, operation-owner preparation.
Capture bootstrap context immediately after init/credentials check, before any refresh await; validate options.context at entry. Pass this context into initial refresh. Recheck after every await before reading credentials or sending a bootstrap request. Never recapture a newer account for an older operation.
In refresh, validate expected context before fresh-token/bearer-changed fast returns, before joining a flight, and again inside queueMutation before preparing/persisting rotation. Capture entry context for non-option calls too. Keep generation/session ownership even while preparation awaits storage; an owner must not use an unqualified null context to join unrelated generations.
The credentials-without-authority startActivation retry branch must capture its context and use commitIfCurrent for bootstrap error recording; its current unconditional commit can write an old error into newer state.
Preserve owner-identity finally checks, detached old HTTP on reset/cancel, per-attempt idempotency, durable lost-refresh-response retry, one current-session refresh singleflight, at most one bootstrap retry, and no stale portal/write/cleanup effect.
Make only changes required for these named paths and the deterministic tests below.

C — MEANINGFUL R2/R3 CLIENT TESTS
Files tests/regression/extension-core/client-i1/client-r2.mjs, new client-r3.mjs, worker-harness.mjs and tooling/checks/extension_i1.py.
Use actual source and extracted modules. Fixture-only signer/helper may be shared; no production methods/comparators replaced. Wire new tests on BOTH routes.
Replace G1/G2 failure-only success surrogates with:
- Two COMPLETE successful device start/exchange/signed UNCONFIGURED account bootstraps for distinct accounts/device sessions in one worker, reset between, both authenticated; account IDs differ; pending ends; portal/exchange counts exact. Then a valid detected-AI signed profile enables Work only for the current account.
- Locally fresh access token -> bootstrap401 -> exactly one successful rotation -> bootstrap200 signed valid profile: call resolves, correct account, Work true, durable new refresh token and changed bearer. Keep final401/403 denial separately.
- Held old start and held old exchange, cancel/reset, begin B before releasing A; release old success and old failure cases; no stale portal, polling suppression or state/error overwrite. Two same-worker concurrent refresh callers share rotation; lost-response retry reuses its idempotency key. Hold old refresh/bootstrap, reset and authenticate B, release old success/failure; B survives. No two-worker substitute.
Add fail-closed matrix: bad signature, unknown key/schema, malformed/oversized 2xx, 403 and final401 (including oversized bodies), signed maintenance and incompatible requested profile. Begin with valid positive Work. Assert denied memory, generation invalidation, no new control HTTP in ordinary data-plane guards. Retain a valid-compatible positive control and transient503 classification control.
Using real composed worker, active Ozon and WB owners and held provider/delivery, trigger denial with cleanup/storage failure: no new dispatch/send/file/chunk/replay after denial; stale positive local Work records and seeded legacy credentials cannot bypass. Auth/status/Finish remain callable. Existing set-fails/remove-succeeds test must actually close worker and create another with same backing and seedAuthority:false, proving signed-out restore; both fail proves memory-only denial and AUTH_DENIAL_PERSISTENCE_FAILED, not restart safety.
Add signed profile/top-level version cases for client0.2.4 vs minimum0.99.0, equal/lower patch/minor, major transition, release/prerelease, alpha.2/alpha.10, hyphenated prerelease, build metadata, malformed version, exact large decimal component comparison; Chromium zero-padding/fourth component/leading-zero/out-of-range. Change only packaged fixture config when varying client version, recompute content fingerprint and signature. Existing comparator is retained unless a case proves a defect.
Demonstrate the two independent defect assertions RED on the starting head and GREEN after fixes; do not label all new cases RED if they already pass. Keep the six positive R1 controls and R2 G6 controls.

D — COMMON-CORE FIXTURE ADAPTATION, NOT GATE REMOVAL
Files tooling/checks/extension_import.py, new tests/regression/extension-core/full-worker-composed.mjs, test-only worker-harness.mjs if needed.
For expected_version0.2.4 full-worker GREEN, run the new composed test on the same actual runtime path. Keep frozen imported tests and all donor RED routes byte-identical; retain old routes for earlier versions.
Port the ten candidate cases and their behavioral assertions from tests/regression/imported/ozon-v0.1.22/validation/swagger-read-surface-live-repair-2026-09-13/run_full_worker_batch_gate.mjs. Replace ONLY fixture construction/setup with makeWorker, its signed fixture authority, privileged SA_STORE_SAVE (seller and performance fixture credentials, personalDataEnabled:true), and its existing real SA_WORK_START/ack/identity handshake via worker.start. request already records fixtureStoreId on SA_STORE_SAVE; do not call legacy save/bind/resume/manual.
Keep fakeFetch provider responses, exact nine report aliases and tenth seller operation, independent semantic fingerprint cd4bce38, admission, exactly-once planning/request/result counts, no uncaught failure, external_request_executed, logical/physical identity and one Performance auth request in each fresh worker. Preserve attachment-port/storage-wake loaded checks via test-only listener-count evidence from makeWorker, if needed. Close each worker.
Retain exact assertion inventory in adaptation evidence. Do not drop cases, return PASS after rejecting legacy setup, stub authorization true, change production guard or rewrite the frozen donor.

E — INSTALLED LOCAL AUTH SYNCHRONIZATION / EVIDENCE
File tests/regression/extension-core/client-i1/installed_local_integration.py.
Keep corrected ROOT checks and real #confirmation/#confirm flow.
After auth-start opens portal, explicitly wait until popup #auth-code matches its full valid code pattern; bounded timeout must report safe UI state, not call group on None. Never log user/device code.
For this clean profile and the verified logout between two accounts, await the actual /login?returnTo= redirect and visible email input before OTP; do not branch on an immediate URL snapshot. Await activation URL, loaded device preview and active account option before approve. Assert exact successful approval status text; merely finding role=status also accepts an error.
Observe successful start/exchange/bootstrap and distinct authorization/account/session identities in memory; output only counts/equality booleans, no token/code/raw bodies. Set a random JS worker sentinel once and compare after both successful logins (do not reseed); assert worker object/identity continuity. same_worker and distinct_accounts must be measured, not literal true.
Require A authenticated/catalog usable while Work is false for UNCONFIGURED; B authenticated and unable to see A store. Verify portal logout HTTP success. No browser restart or extension storage injection between accounts. G6 native synthetic restart stays separate.
Keep API-owned ephemeral private signing key/public packaged trust and fixed development OTP explicitly not real email/live provider proof. Do not add server beta/profile behavior to make this account-only test pass.
On timeout save bounded stage/error and safe status/counts; no raw auth traces or screenshots containing codes. Use CI disposable PostgreSQL if local task DB initialization is unavailable; report local BLOCKED separately from remote gate outcome. No shared pruning, unrelated kills or Bridge restart.

VALIDATION / PUBLICATION / STOP
Node24, existing pinned pnpm/Python requirements, frozen dependencies; version0.2.4 unchanged.
Run extension_i1.py (source/extracted), extension_core.py (source/extracted), browser_verifier.py, browser_application.py on source/extracted with existing trust setup, installed_local_integration.py with isolated loopback PostgreSQL, pnpm docs:check, pnpm bridge:guard, pnpm openapi:check. Use fresh task output paths and exact workflow arguments already in .github/workflows/extension-ci.yml and extension-i1-ci.yml.
Preserve full Extension CI matrix, both I1 jobs, Documentation CI. Publish candidate fast-forward once local prescribed checks complete or a genuine environment-only blocker is recorded; let applicable remote checks run on published head. A named new implementation failure outside this design must be returned with exact log and source location, not solved by arbitrary weakening.
Write new r3/README.md, requirement-mapping, commands/outcomes, limitations/failure evidence and deterministic ZIP receipt/readback. R2 has no tracked r2/README.md despite terminal link: preserve historical r2 files and reference the actual directory; do not claim missing historical evidence exists. Correct current client README's blanket 'R2 closes' wording to reviewed scope.
Keep PR#7 draft/unmerged; no merge/rebase/main change. Do not edit architect-owned handoff STATE branch.
NON-GOALS: server/shared schema/OpenAPI/migrations, Health/P8, I1-SRV.5, C2/offline, D3, S1.2/email/preprod, release/deployment, dependency upgrades, unrelated refactor, real marketplace/AI calls. Preserve account/store/generation isolation, no replay, explicit sequential read-only execution, one-hour buffers and zero mandatory control HTTP in ordinary dispatch/delivery.
Terminal report: task, start/final full head/tree, G1-G6 criterion/function/test/result with exact scope, local/remote outcomes and job URLs plus checkout SHA, ZIP hash/size/byte/parity, remaining blockers. If gh/API access fails, state precise failure once; architect has independent GitHub access. READY_FOR_ARCHITECT_REVIEW only if required checks pass, otherwise BLOCKED/REWORK_REQUIRED. Stop after report; no next task or self-acceptance.
