TASK ID: SA-I1-SRV5-R1-20260916-01
STAGE: I1-SRV.5 — correct the per-run Playwright signing fixture and complete reference acceptance.
ROLE: One implementation executor. Apply the architect's exact correction, verify, publish, and return terminal report. Do not investigate a new direction, change production behavior, or start another task.

REPO / BRANCH / BASE
MaksimUnimax/Seller_Agents.
Continue feature/server-i1-srv5-acceptance-2026-09-16 from b0d93e36b2c7f37a4758ea4b33dbfa9cf2c688bb, tree 9ebcf18e4d0c24d7058a8b730a7e18a74ff950b6.
Canonical main is 5d7c8853cc69dd95bc6e713cac3fb2aa0a63383c (40 characters; previous report/docs omitted the last c).
Architect has created draft PR #8: https://github.com/MaksimUnimax/Seller_Agents/pull/8. Reuse it; never create a duplicate. PR7 and Health branch remain untouched.
Read current remote refs before editing. If the task branch moved unexpectedly, report the actual head and do not reset, force-push, or start another execution. Use the completed task's own checkout or an isolated checkout; no shared-container/DB/process intervention.

PROVEN PROBLEM / DECISION
The new reference test reads CONFIG_SIGNING_PUBLIC_KEY_RING_JSON. tests/e2e/server/playwright.config.ts generates fresh K1/K2 key pairs and overwrites that variable at every module evaluation. Playwright 1.62.1 sets TEST_WORKER_INDEX before the worker deserializes/reloads the config. Consequently the worker replaces the runner public keys, while the disposable API signs with the runner private keys.
The architect reproduced this with the same Playwright version and the exact config key-generation section: worker public ring equals runner ring = false on the candidate; true after the correction below. This also corrects the architect's earlier assumption that the current environment variable was already stable.
Remote push Server CI35043545567/job104628447057 independently confirms FAILURE: the new lifecycle test at line86 expects firstVerification.ok true but receives false; 86 other E2E tests and the integration/migration/build gates pass. The final job log and checkout b0d93e3 were read by the architect.
Fix the test fixture ownership. Do not change the cryptographic verifier, server keys, API contracts, production code, or the reference test's signature expectations. Do not switch to trusting response keys or dynamically fetching runtime trust.

ALLOWED FILES
- tests/e2e/server/playwright.config.ts
- tests/e2e/server/i1-reference-acceptance.spec.ts
- docs/server/I1_SRV_5_REFERENCE_ACCEPTANCE_2026-09-16.md
- docs/server/I1_SERVER_EXTENSION_SYNC_2026-09-16.md
- Focused factual SHA/status corrections in docs/STATUS.md and docs/ROADMAP.md.
- New docs/server/evidence/i1-srv5-r1-2026-09-16/README.md for sanitized commands/outcomes.
No shared fixture changes, dependency/lockfile updates, workflow changes, migration, production, extension, Health, or architect cursor changes.

EXACT CONFIG CORRECTION
In tests/e2e/server/playwright.config.ts:
1. Distinguish a Playwright worker by process.env.TEST_WORKER_INDEX !== undefined, including the string "0".
2. Generate the existing two fresh Ed25519 pairs only when TEST_WORKER_INDEX is undefined. Keep the same e2e-config-k1/e2e-config-k2 IDs. In a worker use an empty pair list: no private-key generation.
3. Construct e2eConfigSigningRingJson only for a nonempty runner pair list; otherwise undefined. Preserve the existing version/key/private PEM format.
4. Assign CONFIG_SIGNING_PUBLIC_KEY_RING_JSON only from the runner's nonempty list. A worker must retain its inherited public JSON byte-for-byte. If no public ring is available after this step, throw a clear setup error; do not generate replacement trust in a worker.
5. In the API webServer.env add CONFIG_SIGNING_KEY_RING_JSON only when e2eConfigSigningRingJson exists, using conditional object spread. Do not assign private signing material to process.env, global public metadata, any file, or a test artifact.
6. Keep all existing cwd, commands, servers, readiness URLs, disposable interlocks, workers/retries, timeout and trace/video/screenshot settings unchanged. No reuseExistingServer bypass.
This is a test-environment fix, not a change to packaged production trust or runtime key distribution.

EXACT REGRESSION AND EXISTING SCENARIO
In i1-reference-acceptance.spec.ts add one short Playwright test named:
"I1-SRV.5 worker preserves runner public trust without private signing material".
Use existing beforeEach reset and existing sql helper (import it; do not edit support/fixtures.ts).
- Require TEST_WORKER_INDEX to be defined.
- Require process.env.CONFIG_SIGNING_KEY_RING_JSON to be absent, using a boolean assertion that cannot print its value.
- Build packagedK1TrustMap() from the inherited public JSON.
- Read only the disposable database's public metadata: SELECT public_key_spki_der FROM signing_keys WHERE key_id=$1, with e2e-config-k1.
- Require exactly one row; export the selected trusted KeyObject as DER/SPKI and compare byte equality to that public metadata. Assert only the equality boolean, not raw keys.
The database comparison is a fixture consistency assertion; it must not replace or populate the trust map used by the lifecycle scenario.
Run this regression on the unmodified b0d93e3 config first: it must fail at key equality. With the corrected config, it must pass.
Retain the original lifecycle test and all its assertions: normal portal activation, one V2 seed, env-provided trust, exact signed account identity, tamper/unknown-key negatives, injected account/device negatives, refresh-token change and same session/device/account, portal revoke, bootstrap401, terminal refresh failure and no restored credentials. Never require access JWT bytes to change within the same timestamp unit.
Keep secret/raw-envelope logging, traces, screenshots and video disabled.

DOCUMENTATION
Correct the incomplete base SHA in the allowed docs to 5d7c8853cc69dd95bc6e713cac3fb2aa0a63383c.
Preserve prior local disk-exhaustion outcomes as historical BLOCKED, not retroactive PASS. Record the discovered runner/worker key mismatch, architect-selected repair, red/green regression, actual lifecycle and CI outcomes.
PR8 now exists; replace the current "PR not created" blocker with an explicitly historical record and the current draft PR URL.
Distinguish reviewed b0d93e3 from the new published head in the terminal report; do not invent a self-referential final commit SHA inside its own commit.
Before successful execution say the scenario "covers/asserts", not "proves". Keep I1-SRV.5 IMPLEMENTED_CANDIDATE / ARCHITECT_REVIEW_PENDING until architect acceptance.
C1 acceptance remains separately at 56c81a3521c02502b65fd713aec890e5a30f038d, PR7 unmerged. V1 simulated offline policy, V2 server/reference evidence, installed extension, live/email/preprod and release remain separate.
C2 and Health queue are unchanged.

VERIFICATION
Node24 / pnpm10.34.5, frozen install.
1. Run the focused new consistency test RED on the unchanged candidate config, then GREEN after correction using the normal Playwright config and a task-owned disposable PostgreSQL/API/portal.
2. Run i1-reference-acceptance.spec.ts including the full V2 lifecycle; both tests must pass. Run the existing playwright-config-regression.test.ts to retain cwd/command guarantees.
3. Run lint/typecheck/docs checks and one applicable server acceptance cycle after the change: format, unit, integration, migration, OpenAPI, bridge, build, full E2E. Required remote Server CI and Documentation CI remain gates on the published candidate. Do not rerun extension or Health suites outside the existing applicable workflows.
4. If the local host is still full, record that exact local blocker once and publish the bounded fix for normal remote CI. Do not prune shared Docker resources, delete shared data, lower assertions, or substitute a mocked API for the lifecycle. Architect can inspect remote CI through the connector.
5. Do not rerun old CI just to wait. Base-candidate CI and new-candidate CI are distinct; retain actual outcomes with their checkout SHAs, including PR virtual merge refs.

PUBLICATION / TERMINAL
Commit only allowed changes and fast-forward push this existing branch. Reuse draft PR8; no merge, no new PR, no changes to PR7 or main.
If gh/authenticated API remains unavailable, state it once; do not spend the task repeating unauthenticated requests. Provide final commit/tree/parent/base, actual remote ref, changed-file list, focused regression RED/GREEN or precise local BLOCKED, lifecycle outcome, other gate outcomes, evidence path and known CI URLs.
Stop after terminal report for architect review. No independent next task.
