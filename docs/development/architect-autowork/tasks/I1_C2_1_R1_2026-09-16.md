TASK ID: SA-I1-C2-1-R1-20260916-01
STAGE: C2.1 correction — origin ownership and durable effective time. C2.1 is REWORK_REQUIRED; C2.2 is not started.
ROLE: One executor implementing this exact architect decision. No independent architecture, extra agents or parallel task.

REPO / BRANCH / BASE
MaksimUnimax/Seller_Agents; integration/i1-c1-srv5-2026-09-16; reuse draft PR9.
Exact start 981d7e422d45234674aeda84c762245be55ff493, tree 4711cea969329f5552fe07e7f99af122fda60dc9, parent 38471d667936f6f80a3f4aa682057309745ac080.
Canonical main 5d7c8853cc69dd95bc6e713cac3fb2aa0a63383c.
Verify fresh refs before edits; never overwrite unexpected advancement. Do not repeat C2.1 or synchronization. PR7/8, source branches, main and Health branch stay untouched.

PROVEN DEFECTS
Architect's independent VM executed the exact candidate client blob 2fdb51545a4e64822be4e68efbc7d4393fefba3a with real Ed25519 verification and synthetic clocks/storage/network:
1. Restore same-origin extension-version mismatch discards authority and retains credentials. Restart that resulting record with changed API origin: bootstrap sends Authorization to the replacement origin. restoreOnce checks ownership only inside state.authority && state.credentials.
The same probe also restored a structurally valid pending activation at the replacement origin and observed deviceCode sent to /v1/device-authorizations/token; pending attempts currently lack origin provenance.
2. Monotonic readings 1000 → 2000 → 1500 still allow Work: effectiveTime compares against the initial anchor, not the preceding reading.
3. Observe a +60000ms wall jump, roll wall back, advance monotonic by1000ms: effective floor advances0ms. The initial anchor does not carry forward the larger observed floor.
4. Hold a positive checkpoint storage write, move clocks to expiry, release: that call returns true; the next call returns false. Eligibility is not rechecked after storage.
The submitted focused suite misses these cases. Its “legacy” case deletes the complete auth record; that does not test legacy credentials lacking metadata. The held-write case does not assert the promise remains unresolved before release, and no new composed-expiry dispatch/delivery/attachment/recovery matrix exists.
Architect verified installed-local CI35055691061/job104665287264 SUCCESS on PR checkout13cf170bd3f0d6cd884e48f1133ef161f6b5361a; its tree equals the candidate. Executor's local SKIPPED remains historical. Green CI does not close these defects.

ALLOWLIST
packages/control-client/src/client.js
tests/regression/extension-core/worker-harness.mjs
tests/regression/extension-core/client-i1/client-cache-time.mjs
docs/development/client-i1/README.md
docs/development/client-i1/C2_CACHE_TIME_2026-09-16.md
docs/migration/evidence/extension-i1-c2-1-2026-09-16/README.md
docs/migration/evidence/extension-i1-c2-1-2026-09-16/r1/README.md (new)
docs/migration/evidence/extension-i1-c2-1-2026-09-16/r1/results.json (new)
Existing checker already runs the focused file source/extracted. Preserve prior results.json and all historical failure/skip records. No other edits.

R1-1 — OWNERSHIP BEFORE ANY SECRET-BEARING REQUEST
In restoreOnce validate credentials/cacheClock independently of whether authority exists. Before bootstrap or refresh can use credentials, require strict clock shape, valid numeric floors, matching device/session, and exact controlApiOrigin/portalOrigin. Check the same invariant on entry to bootstrap and refresh, before accessFresh/forced-refresh shortcuts or HTTP.
A missing/malformed clock, unknown origin, origin mismatch or device/session mismatch clears auth credentials, rotation, pending and authority in memory before storage; persist denied state then removal fallback. Require a new activation; preserve marketplace/catalog/IDB data. Never infer legacy origins from current config. Validators must return false for malformed URL inputs rather than escaping into an unrelated recovery path.
A structurally valid same-origin/session clock survives authority=null and same-origin extension/browser/trust/contract replacement. Retain both floors; discard incompatible cached authority and require verified fresh online bootstrap. An old contract value in a valid same-origin/session clock is only provenance for preserving floors, not cached Work eligibility. A successful online bootstrap replaces the clock owner contract with the current contract while preserving larger session floors.
At successful device exchange in ensurePolling, initialize the genuinely new session clock atomically with credentials, pending=null and authority=null: current origins/contract/device/session, trustedServerTimeMs=0, effectiveTimeMs=validated current wall milliseconds. This is a local transport-ownership record, not signed authority. Only verified bootstrap supplies trusted server time and cacheBinding. Never carry the old session's clock into new credentials. Clear the clock/runtime owner on terminal credential removal and localReset; retain it on nonterminal authority invalidation and same-session refresh.
Bind persisted activation attempts too: pending.authContext has exact keys contextVersion="control_auth_context_v1", controlApiOrigin, portalOrigin, contractVersion. Create it before the start request and retain it when replacing the starting attempt with the pending response. Restore/poll/openPortal must validate that context before sending deviceCode or opening its approval URL. Missing/mismatched legacy attempt context requires fresh activation; never resume its code or idempotency keys at a different origin. This is the same origin-leak correction, not a new auth protocol.
Keep all existing generation/attempt ownership, refresh singleflight, 401 retry and lost-response idempotency behavior.

R1-2 — ROLLING MONOTONIC ANCHOR
Replace the initial-anchor-only calculation in effectiveTime with an owner-scoped rolling anchor and last monotonic observation.
For each successful observation require finite nonnegative monotonic time >= the preceding accepted monotonic value. Compute ceil(max(wall, trusted floor, durable floor, runtime high-watermark, previousEffective + monotonic - previousMonotonic)); reject unsafe/date-overflow values. Then advance the runtime anchor to this effective value and this monotonic reading. Equal monotonic values are legal. A decrease must deny even if it remains above the first reading; do not reset the anchor on an invalid observation. Recovery requires a valid reading at least as large as the last accepted one and successful required persistence.
Preserve larger floors across same-session online bootstrap, refresh, authority replacement and restart. Reset runtime anchoring only for genuinely new owner/local reset/terminal removal. No production clock injection, wall-only fallback, rewritten signature/deadline or powered-off elapsed-time claim.

R1-3 — STORAGE COMPLETION VETO AND EXACT OWNER
cacheAuthorizationCheckpoint retains exactly one queueMutation and no init recursion.
Capture generation, device/session and exact signed authority identity: envelope key/payload/signature plus binding, account and requested AI. The current tuple alone is insufficient to distinguish two same-session snapshots. Recheck after awaits and again at the public API boundary; a decision must not be applied to a replacement authority.
Inside the queue observe effective time, advance in-memory floor before persistence, and persist the checkpoint before positive authorization. After storage resolves, sample valid clocks again and recheck the exact authority owner and half-open expiry.
If the post-write observation is expired, unsafe or stale, return no positive permission. For observed expiry/grace, commit the nondecreased denial floor and workAllowed=false before releasing the queue, with existing write/remove fallback. Clock-invalid observations deny; do not manufacture a numeric floor. Any required storage failure denies.
Use a bounded algorithm: one authorization checkpoint write and, only if a post-write veto needs it, one denial write/fallback. Do not loop writing until continuously advancing performance.now becomes equal to stored time.
For a still-fresh post-write observation, the grant's durable checkpoint is the successfully written pre-write floor; the completion sample is a veto and updates the runtime rolling anchor/high-watermark for the next checkpoint. Do not label an unpersisted completion sample as durable. This explicitly avoids an impossible “persist every final clock read” loop.
Return decision identity with the result. canWork, status.workAllowed, getAuthority.workAllowed and ensureForIdentity's cached payload must consume it only for the matching current authority. Default publicStatus without a checkpoint decision must not invent a positive cached Work permission.
Time-only changes do not increment generation. Dispatch first-denial cleanup after queue release, with captured authority/generation, and discard stale notifications; never await reentrant cleanup inside this checkpoint's storage transaction.
Keep failed-write recovery monotonic, including removal success and total-storage-failure distinction.

R1-4 — MEANINGFUL SOURCE/EXTRACTED REGRESSIONS
Extend client-cache-time.mjs, preserving all existing assertions:
A. Reproduce the four architect cases as RED against exact981d7e4; GREEN after correction. Use deterministic latches for storage, not a sleep as proof. Assert positive promise unresolved while write held; crossing exact expiry gives false on that same released call.
B. Authority=null records from actual same-origin version mismatch and actual nonterminal invalidation: restart with API-origin or portal-origin change; bootstrap, forced refresh and startActivation never transmit old bearer/refresh/deviceCode secrets. Record only counts/booleans, never token values. Same-origin record must obtain a valid signed online replacement with floors retained.
C. Keep a real legacy record's credentials and signed authority but remove binding/clock; require fresh authorization and retained catalog data. Also test authority=null with missing/malformed/owner-mismatched clock. Test pending start/poll provenance and a new activation's initial clock followed by real signed bootstrap.
D. Monotonic1000→2000→1500 denies; equal readings legal. Wall-forward→wall-back with monotonic advancement increases effective time by at least elapsed time; verify exact-expiry denial and restart floor. Cover NaN, malformed/inconsistent persisted times and unsafe arithmetic.
E. Storage write failure/removal success then restart denies; both fail remains denied in memory and reports the restart limitation. A subsequent successful write recovers only with nondecreased floor.
F. Same-session later signed snapshot renews authority without lowering either floor; lower signed serverTime, expired and incompatible snapshots cannot. A genuinely different activated device/session does not inherit the old session's future floor.
G. Hold A crypto response across completed reset/B activation, release A: B account/envelope/floors unchanged. For a held checkpoint write, queue reset/new activation and assert mutation serialization; do not require B to commit through a locked queue. After B commits, no old checkpoint result/notification may authorize or mutate B. Include replacement by another same-session signed snapshot in exact-decision identity checks.
H. Exercise existing composed Ozon/WB routes with positive controls for provider dispatch, prepared result delivery, real attachment key and recovery. Repeated guards while fresh emit zero control-plane HTTP. Advance to expiry: the same dispatch/delivery/attachment/recovery routes deny, with zero provider invocation/delivery caused by those attempts. Reuse the established route helpers and fixture patterns in client-r4.mjs/client-r5.mjs; read those named files for implementation, do not weaken their original controls.
I. Account-only stays authenticated/Work=false; old C1 races and native/installed-local scenarios stay intact.
Do not mark a group PASS unless its assertions actually run. Report exact case names/counts.

VALIDATION / EVIDENCE
Run the focused RED/GREEN and the existing extension_i1.py and extension_core.py source/extracted checks; browser verifier and unchanged native browser_application.py source/extracted; docs:check, bridge:guard, openapi:check.
Run unchanged installed_local_integration.py with a task-owned disposable PostgreSQL if available. Use the established task DB setup; no shared-container cleanup, no arbitrary disk purge. If the prerequisite is unavailable, retain explicit local BLOCKED/SKIPPED; architect checks the mandatory remote installed job.
No redundant local full-server replay for unchanged server code. Keep all applicable PR9 workflows enabled. Do not rerun workflows to wait or edit filters.
Publish a fresh reproducible development ZIP receipt with full SHA256, size, repeat and source/extracted byte readbacks. Separate source VM, native synthetic, installed-local and live evidence; no live provider calls.
Document the completion-veto/durable-checkpoint distinction and auth-without-authority ownership explicitly. Preserve prior C2.1 candidate evidence and mark its review REWORK_REQUIRED with the proven causes. New R1 remains pending architect review.
Offline grace/profile consumption/joint offline acceptance remain C2.2+; Health B5/B6–B8, S1.2/D3, full I1/D2 and beta remain open.

PUBLICATION / TERMINAL
Only allowlisted paths; normal commits and fast-forward push to the existing integration branch; reuse draft PR9. No merge, release, deploy, new PR or next task.
Terminal: task ID; exact start/final/tree/parents; code/evidence commits; changed paths; named RED/GREEN cases; all gate outcomes including skipped/failed; package receipt and evidence paths; remote refs and actual observable CI checkout/run/job facts. Missing executor CI credentials is UNKNOWN, never PASS.
Stop after terminal for architect review; architect resumes the cycle immediately.

