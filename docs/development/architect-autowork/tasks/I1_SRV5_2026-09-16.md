TASK ID: SA-I1-SRV5-20260916-01
STAGE: I1-SRV.5 — bounded server reference-client acceptance and synchronization handoff.
ROLE: One Codex implementation executor. Implement this exact test/documentation decision, execute gates, publish and stop for architect review. No independent research, architecture, agents or parallel task.

REPO / BASE / BRANCH
Repository MaksimUnimax/Seller_Agents.
Create feature/server-i1-srv5-acceptance-2026-09-16 from verified main5d7c8853cc69dd95bc6e713cac3fb2aa0a63383c in an isolated checkout.
First fetch main and verify the exact base. If the branch already exists, inspect its task identity/head; do not overwrite or duplicate an existing execution. Unexpected main advancement or conflicting active work => report exact condition and stop without resets/force.
Accepted client C1 is56c81a3521c02502b65fd713aec890e5a30f038d on feature/extension-i1-client-2026-09-15; PR7 remains draft/unmerged. Do not merge/cherry-pick or modify it.
Server I1-SRV.0–4 were accepted on main5d7c8853 (PR6); stale pending wording does not reopen implementation.

PROBLEM / ARCHITECT DECISION
Server authority is implemented and accepted, and client C1 now has passing installed-local A/B auth. The remaining server step is a consolidated reference acceptance/handoff. Existing activation.spec.ts covers activation/refresh/revoke and bootstrap.spec.ts covers signed V2 identity, but no single named I1-SRV.5 scenario joins activation -> V2 account bootstrap -> rotation -> V2 identity continuity -> revoke.
Add this bounded test and an explicit evidence matrix. Reuse all production auth/bootstrap/trust code and existing fixtures. Do not introduce a second auth stack or retrofit V2/offline into SimulatedExtensionClient: its existing policy path is V1, and this distinction must remain explicit.

READ NAMED AUTHORITIES
AGENTS.md; docs/STATUS.md; docs/ROADMAP.md; docs/decisions/DECISIONS.md; docs/server/I1_SRV_0_CONTRACT_DEPENDENCY_AUDIT_2026-09-14.md; I1_SRV_2_DEVICE_AUTH_INTEGRATION_ACCEPTANCE_2026-09-15.md; I1_SRV_3_BOOTSTRAP_TRUST_HANDOFF_ACCEPTANCE_2026-09-15.md; I1_SRV_4_ERROR_REVOCATION_OFFLINE_ACCEPTANCE_2026-09-15.md; I1_SRV_4_EXTENSION_HANDOFF_2026-09-15.md.
Implementation references: tests/e2e/server/activation.spec.ts, bootstrap.spec.ts, support/fixtures.ts, playwright.config.ts; packages/server/simulated-extension-client/src/index.ts and offline-policy.test.ts/policy.test.ts; .github/workflows/server-ci.yml.
Current session instructions supersede historical separate-stream restrictions.

ALLOWED CHANGES
- New tests/e2e/server/i1-reference-acceptance.spec.ts.
- New docs/server/I1_SRV_5_REFERENCE_ACCEPTANCE_2026-09-16.md.
- New docs/server/I1_SERVER_EXTENSION_SYNC_2026-09-16.md.
- Focused status/link updates in docs/STATUS.md, docs/ROADMAP.md, docs/README.md and the two I1-SRV.4 documents above.
No production, shared fixture, workflow, dependency, migration, contract or extension code changes. If the prescribed test exposes a real defect, return the exact failing assertion and bounded trace; do not design a fix or weaken expectations.

EXACT NEW TEST
Use normal Playwright test with reset() before each test and existing isolated PostgreSQL/API/portal harness.
1. Use activateExtensionClient(page) from support/fixtures.ts. This performs real development OTP/portal approval and device exchange; never write client credentials or insert device/session/token rows yourself.
2. Call seedBootstrapConfig({contractVersion:"control_plane_v2"}) once. Use the same V2 HTTP request shape as bootstrap.spec.ts: contractVersion control_plane_v2, extensionVersion1.2.3, chrome123, actual deviceId, lastConfigVersion:null, existing detectedAi fixture.
3. Read credentials(extension) only as a test observation; take a copied snapshot before rotation. POST to apiOrigin/v1/bootstrap using its bearer. Require200, SignedBootstrapEnvelopeV2Schema.parse, verifyBootstrapEnvelopeV2 success and account.id equal to accountId(), account.status ACTIVE and exact V2 version tags.
4. Supply the trusted e2e-config-k1 public key from CONFIG_SIGNING_PUBLIC_KEY_RING_JSON created by playwright.config.ts, using node:createPublicKey with SPKI DER. Validate the selected entry exists. Build the trust map before consuming the response; never trust a key from the response or add a runtime download. Never persist private keys/credentials.
5. Retain the first envelope and tamper ONLY account.id in decoded payload, leaving signature unchanged; verification must fail. The same untouched envelope with an empty trusted map must fail UNKNOWN_SIGNING_KEY. POST a bootstrap body with injected accountId =>400; POST a different random device UUID under the real bearer =>403 DEVICE_MISMATCH. These negative calls must not change the real device/session.
6. Call extension.refresh with a unique stable test idempotency key once; require true. Read the new credentials: same deviceId/sessionId and a different refresh token. Do not require different access-token bytes when issue times fall within the same JWT timestamp unit. A second normal V2 bootstrap with the rotated bearer must verify and carry the SAME account.id. Do not assert that the old unexpired access token is revoked by rotation.
7. Navigate the existing portal to /devices, click its sole Revoke button, and await the E2E Chrome REVOKED state as in activation.spec.ts. With the copied rotated bearer, require bootstrap401 UNAUTHORIZED. Calling extension.refresh with a new idempotency key must return false. A repeated direct bootstrap remains401; no successful bootstrap or new credentials after revoke.
8. Use expect comparisons without printing tokens, cookies, auth codes, raw envelopes or browser traces. Test artifacts contain scenario labels/outcomes only. This test is server/reference + browser portal acceptance, NOT installed extension, live marketplace, email delivery or offline V2 acceptance.

ACCEPTANCE MATRIX / DOCUMENTATION
Create a compact criterion -> exact existing/new test file and test name -> actual execution result -> evidence level matrix:
- integrated V2 lifecycle: the new scenario;
- pending/deny/device-limit/rotation/revoke: existing activation.spec.ts, index.test.ts and p2-3/p2-4/p2-5 integration suites;
- V1 compatibility and V2 signed identity/tamper: bootstrap.spec.ts and existing contract/remote-config tests;
- exact packaged trust/unknown key/rotation: existing bootstrap.spec.ts and I1-SRV.3 trust tests;
- signed compatibility denial, eligible transport/503 fallback, half-open expiry/grace, persisted time floor/restart, terminal invalidation/storage failure: existing policy.test.ts and offline-policy.test.ts plus bootstrap.spec.ts. Label the simulated policy path V1; direct V2 verifier tests do not prove V2 cached browser behavior.
Do not create copies of already passing tests or inflate process counts as proof.
Record S1.1 and I1-SRV.0–4 as historically accepted with main5d7c8853/PR6 and existing evidence; preserve original candidate history.
Record C1 accepted separately on56c81a3: source/package/native and installed-local API/portal/PostgreSQL, development OTP, synthetic provider/AI, zero live calls; PR7 unmerged. R5 final Extension CI34980773590/34980778417 and I1 CI34980773436/34980778468 plus Documentation34980778452 succeeded. Do not call the whole I1/D2 or beta complete.
I1-SRV.5 stays IMPLEMENTED_CANDIDATE / ARCHITECT_REVIEW_PENDING until architect verdict.
The synchronization handoff must distinguish implemented server contract, C1 verified scope, C2/offline/profile integration still pending, and S1.2/D3/Health boundaries. No new roadmap order or additional implementation.

VERIFICATION
Node24 / pnpm10.34.5, existing frozen lockfile.
First run the new focused test using the existing Playwright config and PRODUCT_CONTROL_PLANE_E2E=1 on a fresh disposable PostgreSQL with DATABASE_URL set.
Then run the one applicable server acceptance cycle: pnpm lint, pnpm format:check, pnpm typecheck, pnpm test, pnpm test:integration, pnpm db:migrate, pnpm openapi:check, pnpm bridge:guard, pnpm build, pnpm test:e2e, pnpm docs:check. The full server workflow is the required gate for this server acceptance candidate; do not rerun extension/Health/provider suites outside it.
Keep traces/video/screenshots off as configured. No shared-container pruning, shared DB reset, unrelated process kill or port-stealing. If canonical test ports are occupied or isolated PostgreSQL unavailable, report exact blocker and retain mandatory remote Server CI.
No policy/storage/contract bypass to make tests green.

PUBLICATION / TERMINAL
Recheck origin/main and target branch. Commit only the allowed files, push fast-forward, create one draft PR to main; no merge. Do not modify architect handoff STATE or PR7.
Record actual commit/tree, parent/base, applied checks and remote Server CI/Documentation CI URLs/status/checkout SHA; distinguish pure push from PR merge. If gh/API unavailable, state the exact access failure once; architect can inspect CI independently. Never invent IDs or equate pending/unknown with PASS.
Terminal: task ID; full base/final head/tree; test matrix and gate outcomes; sanitized evidence paths; draft PR; exact blockers. Stop for architect review, no next task. No automatic S1.2/D3/C2/Health launch.
