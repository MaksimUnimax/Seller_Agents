TASK ID: SA-I1-C2-1-R3-20260916-01
STAGE: C2.1 — durable invalid-attempt cleanup and completion of acceptance.
One executor. Implement the specified correction; no independent architecture or parallel task.

REPO / REFS
MaksimUnimax/Seller_Agents.
Branch integration/i1-c1-srv5-2026-09-16; reuse draft PR9.
Exact start06e0eb79badc5e65ea972035a2968f72fdaae6a7.
Tree e090b748a6a1aa8d35869f215c7846984e4f73f3.
Parent ead575816dde5df2613a61cb8aa599e216c8066a.
Main at review5d7c8853cc69dd95bc6e713cac3fb2aa0a63383c.
Verify current refs; no force push or overwrite of other work. Owner is concurrently migrating агентселлер.ру to октопорт.ру: preserve those authorized changes, do not revert them or relax exact origin/auth checks. This task does not perform the domain migration.

PROVEN RESULT / REMAINING DEFECT
R2 restore correction is independently confirmed: valid pending survives worker recreation, resumes a second exchange, and retains its context without STORED_CREDENTIALS_INVALID.
REWORK_REQUIRED because the invalid-pending branch in restoreOnce still uses queueMutation+commit. commit persists before setting state and has no removal fallback.
Independent probe on exact client blob cb3632414fbbce7aca678bc91a40d1120865cc41: create actual pending activation, advance to expiry, recreate worker while AUTH storage.set fails and storage.remove is available. Result: restore rejects PROBE_AUTH_SET_FAILED, one set attempt, zero remove attempts, old pending remains durable. This is the unimplemented in-memory-first/removal-fallback requirement, not a new scope item.
Current-head native CI35060667796/job104680101447 PASSED both source/extracted routes. Installed CI35060667782/job104680101417 PASSED; client104680101221 also SUCCESS. Checkout c8652fb156b4a14e67ecbf9de8c016176163e1bb has candidate-identical tree.
Preserve executor local native FAIL as history. Its local cause is unknown from the published evidence; do not guess, weaken assertions or modify native code to hide it.
R2 T6 still lacks queued-checkpoint/reset and same-session snapshot-replacement assertions. T7 does not issue a new denied command and does not assert manual_recovery absence. Complete these exact outstanding assertions; do not reimplement passing R1/R2 behavior.

ALLOWLIST
packages/control-client/src/client.js
tests/regression/extension-core/client-i1/client-cache-time.mjs
tests/regression/extension-core/worker-harness.mjs — only deterministic test barriers if needed
docs/migration/evidence/extension-i1-c2-1-2026-09-16/r2/README.md — factual review addendum
docs/migration/evidence/extension-i1-c2-1-2026-09-16/r3/README.md (new)
docs/migration/evidence/extension-i1-c2-1-2026-09-16/r3/results.json (new)
Preserve previous result files. No other edits.

IMPLEMENTATION — NARROW TERMINAL DISCARD
In restoreOnce replace the invalid restored pending/starting queueMutation+commit branch with the existing terminal invalidateKnown(contextForState(), error("ACTIVATION_CONTEXT_MISMATCH"), true) path.
An invalid persisted activation context is terminal for that malformed auth record: clear pending, rotation, credentials, authority and cacheClock in memory before storage; reset runtime clock/owners; preserve marketplace/catalog/IDB data.
Reuse the existing single denied-state write followed by AUTH-key removal on set failure. If both fail, retain denied memory, expose AUTH_DENIAL_PERSISTENCE_FAILED and the truthful restart limitation. Initialization must still complete denied, not reject a raw storage exception.
Do not nest queueMutation. If cleanup notification is present during restore, do not make initialization await an init-dependent callback; retain the existing advisory notification behavior without introducing a second auth stack.
Valid pending/starting and normal signed-out records must remain on their R2 paths: no reset, no new keys, no generation bump simply because credentials are absent.
Keep exact origin/portal/contract checks, rolling effective time, completion veto and exact authority identity unchanged.

REQUIRED REGRESSIONS
R3-A: exact-base RED, candidate GREEN for the independently proven failed-set path.
Create pending via real startActivation fixture; close the old worker without resolving its old exchange. Expire the attempt; reopen with seedAuthority:false and fail only AUTH set. Count remove calls and allow remove.
Expect restore/status resolve signed-out, pending=null, Work=false; one failed denied-state write followed by one successful AUTH removal; storage key absent; no old exchange/portal/request.
Reopen again with rolled-back wall and seedAuthority:false: the deleted attempt cannot return.
Restore writes, call startActivation: a fresh attempt and new idempotency keys; no old deviceCode. Preserve a catalog marker.
R3-B: fail both set and remove; initialization resolves denied with AUTH_DENIAL_PERSISTENCE_FAILED, no old request and no claim of durable protection. Restore storage availability and start a fresh attempt without recycling old keys. Include invalid-origin and malformed-starting records through this same cleanup branch; retain valid pending/starting positive controls.

R3-C: complete the omitted T6 queue/identity cases.
With fresh A, hold an actual checkpoint AUTH write. Queue localReset and B activation. Assert the reset cannot publish through the held storage transaction. Release the latch, complete B exchange and signed bootstrap, record B generation/account/envelope/clock. No old result or late cleanup may alter or authorize B. Assert recorded ordering; do not demand impossible B completion while the mutation queue is locked.
Separately replace authority with a DIFFERENT signed envelope in the SAME device/session/requested-AI/account context. Use real signature verification and deterministic storage/crypto-completion barriers, test-only in the harness. Arrange the replacement commit between the old checkpoint computation and its public consumer; assert the old decision is denied or belongs only to its pre-replacement ordering, never reused for the new envelope. B/current status/getAuthority must reflect the new envelope. Do not mutate production closure state or bypass verification.
Preserve and reuse the completed held-A HTTP→reset/B and floor-renewal tests rather than counting them as these missing cases. Case names must identify which ordering was actually exercised.

R3-D: complete the existing T7 expiry fixture.
After the existing exact-expiry denial and real-owner/real-artifact controls, issue a fresh OZ_EXECUTE_COMMAND for each marketplace with a new manual_request_id and the captured Work session/conversation.
Assert not accepted, provider-call count unchanged, no new delivery advertisement and no extra control-plane HTTP from this denied guard.
For OZ_CONTENT_READY and OZ_CONTENT_SYNC assert manual_recovery is null/undefined as well as recovery/outgoing_text absence.
Retain all positive text/binary bytes controls and actual artifact_key use. Do not replace them with fabricated keys or direct canWork-only assertions.

VALIDATION / FACTUAL EVIDENCE
Run focused source/extracted RED/GREEN, then the existing extension_i1.py and extension_core.py gates, browser verifier, unchanged native source/extracted and installed-local harness on task-owned PostgreSQL, docs:check, bridge:guard, openapi:check.
For native runs use exactly the existing CI construction: one fresh per-run private key and matching packaged public config, build the runtime with that config, and run source/extracted with that same key. Read .github/workflows/extension-ci.yml and tests/regression/extension-core/client-i1/make-browser-config.mjs for those exact existing commands. Do not mix keys/configs from different builds; this is a reproducible run instruction, not a claimed diagnosis of the earlier local FAIL.
If native still fails, record a secret-free diagnostic at the failed assertion in evidence using a temporary diagnostic copy: status/lastError, envelope keyId and public trust fingerprints, runtime/source hashes and exact command paths. Never record private keys, tokens, deviceCode or whole auth storage. Do not commit a modified native harness or relabel FAIL.
Keep required remote workflows; do not rerun just to wait or edit filters. No redundant local full-server replay for unchanged server.
Publish new ZIP full SHA256/size, repeat and source/extracted byte parity. Distinguish source VM, native synthetic, installed-local and live; no live provider calls.
R2 addendum: current-head native CI PASS is separate from local historical FAIL; old35057935403 was R1 evidence, not R2 CI. Preserve R2 published results as history and record final refs/current CI facts separately. No stale “not pushed” wording as current state.
Every newly claimed case must have actual assertions; if a prescribed case fails, report exact facts, not an invented runtime redesign.

NON-GOALS / PUBLICATION
No offline grace/C2.2/profile consumption/Health/server/contracts/dependency/refactor/domain migration/merge/deploy/release work.
Fast-forward the existing integration branch; reuse draft PR9. Main, PR7/8 and accepted source branches remain untouched.
Terminal: exact task ID/start/final/tree/parents; code/evidence commits; allowlisted changed paths; R3-A–D outcomes and retained T1–T7 status with missing/failed/skipped distinctions; all gates; package/evidence paths; actual remote run/job/checkout facts. Use UNKNOWN where unavailable.
Stop for architect review after terminal; architect continues the cycle.

