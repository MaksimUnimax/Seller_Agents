# D2.4 — пауза по команде владельца

Историческая точка паузы по команде владельца. Затем владелец сообщил: «Лимиты обновлены, продолжай работу». Пауза снята; дальнейшее состояние и приёмка фиксируются в актуальной квитанции D2.4. Сведения ниже относятся к моменту паузы.

## Репозиторий и сохранённое состояние

- Актуальный репозиторий: MaksimUnimax/Seller_Agents. Blood & Sand — только исторические donors.
- Workspace: `/workspace/scratch/a73487e33082/Seller_Agents`.
- Ветка кандидата: `feature/extension-stores-d2-4-2026-09-14`.
- Удалённый code candidate: `7f2488c3658913355f0b96407c37f411a2e3af81`, tree `d611c15e34a1af7a0b9af72e3d37a6186e479112`.
- Его локальный commit `32e8420` имеет тот же tree; локальные и connector commit SHA различаются. Не считать их общей Git-историей.
- Проверенный main перед паузой: `de41f33646d5dd61bcae66624225e16538fd8f3a`, tree `8d41cd3c376885db6f2bccdfa51c529ad114ee2b`. D2.4 в main НЕ опубликован.
- Перед паузой осталась одна незакоммиченная локальная правка `tests/regression/extension-core/browser_application.py`: замена двух popup.wait_for_function на ожидание текста через locator. Её точный diff сохранён рядом в `D2_4_PENDING_FIXTURE.patch`. Если workspace потерян, применить patch поверх code candidate; если файл уже исправлен, повторно не применять.

## Что сделано

Development 0.2.3: постоянные IDs и несколько магазинов каждой площадки; общий popup, карточки Seller/Performance/WB, явные проверки ключей, подтверждение смены/удаления; Work Start с закреплённым магазином; WB подключён к настоящему content admission, той же очереди и зрелой Ozon доставке. Hide сохраняет разрешённый пакет/доставку, Finish закрывает последующие действия. Добавлены durable send commit и no replay по Work/message/block; локальные ссылки Ozon закреплены за магазином. WB binary идёт через общий IDB/chunk/File/attachment порт с оригинальным безопасным именем. TTL не более часа; expiry read gate и housekeeping. Подробности: [EXTENSION_APPLICATION](../EXTENSION_APPLICATION.md).

Сервер, shared wire contracts, frozen donors и live API не менялись. Ozon — эталон общей механики; старые WB popup/worker/Work/delivery не копировались. Реальная авторизация/аккаунт сервиса — следующий I1; сейчас UI явно local development. Backup/relay/logout/редкая синхронизация не реализованы. WB INSTALLED FAIL и R1–R8 BLOCKED сохраняются.

## Доказательства на момент паузы

- Последний полный local source + extracted ZIP: `build/d24-gate5/summary.json`, **PASS, 109 gate processes**, 9 новых application-групп, прежние 17 WB adapter/12 Ozon context/6 worker/core и исходные gates. Квитанция: [LOCAL_RESULTS](../../migration/evidence/extension-application-d2-4-2026-09-14/LOCAL_RESULTS.json).
- Package: `SELLER_AGENTS_D2_4_v0.2.3_DEVELOPMENT.zip`, **1 725 341 bytes**, SHA256 `77f31e916446d9e41332356a3ad92faf888d7d170d5fe22588bf10233e48bb10`.
- 84 production inputs, 38 runtime files; deterministic repeat ZIP и source/extracted MATCH.
- Candidate Extension CI: **34845082473**. Core/source/package, Ozon baseline, WB Node baseline SUCCESS. WB browser baseline был IN_PROGRESS при последнем чтении; итог после паузы проверить заново.
- Новый native Chromium application job **103978952267 FAILED** из-за fixture `popup.wait_for_function`, который использует eval и блокируется CSP расширения. **CSP не ослаблять.** Локальная незавершённая правка заменяет эти два ожидания locator-based polling. Runtime этой правкой не меняется. Продолжение native сценария ещё не проверено; могут обнаружиться следующие реальные или fixture-дефекты.
- Core artifact **10348245283**, outer SHA256 `87712ee5a13f285c269c0c8b3758c696ae8a775eafedaad20b5a5d89854288ae`, скачан `build/d24-core-first.zip`; composition receipt и внутренний ZIP побайтово совпали с local gate5.
- Browser failure artifact **10347916190**, outer SHA256 `776064761018a7957a8e00bc230a87e70f5e6f0b4106d89ecec927108444113f`, скачан `build/d24-browser-first.zip`; screenshot/result извлечены в `build/d24-browser-first/`. Ещё не просмотрены визуально.
- Recursive remote tree кандидата прочитан полностью: 1136 entries, truncated=false, SHA совпал с local tree. Отдельная итоговая file/input readback receipt ещё не оформлена.
- Повторный CI прежнего main D2.3 **34841155889 SUCCESS** — проверено перед паузой.
- Последние docs-check / diff-check / Python compile / JS syntax PASS. Docs STATUS/ROADMAP/AGENTS пока содержат D2.4 CANDIDATE, приёмки нет.

## Ограничение локальной среды

Локальный Chrome 152 завершается до запуска расширения: `socket(): Operation not permitted` в process_singleton_posix.cc. Не обходить ограничение и не просить лишнее подтверждение. Browser fixture работает в штатном CI, с реальными worker/content/IDB/File, синтетическим ИИ и fetch. Popup открыт extension-страницей; active-tab query контролируется fixture. Это не live/installed certification целевого ИИ/браузера.

## Точный следующий шаг после новой команды

1. Прочитать этот checkpoint, AGENTS, STATUS и EXTENSION_APPLICATION. Проверить реальные main и candidate branch HEAD; сохранить параллельные изменения сервера.
2. Проверить завершившиеся jobs CI 34845082473. Убедиться, что locator-правка находится в рабочем browser_application.py; применить соседний patch только при необходимости.
3. Просмотреть failure screenshot/result. Опубликовать исправленный тест в candidate branch обычным fast-forward и запустить предусмотренный CI. Не обновлять main при красном native application job.
4. Довести browser fixture source и extracted routes: общий popup → реальный Start → baseline старой истории → WB HELP/API → текстовый Send/no replay → Hide/Show → WB binary через native IDB/File/attachment/send → Finish → 320/380px и увеличенная типографика. Сохранить фактические screenshots и результаты; не выдавать за живой ИИ.
5. При ревизии обратить внимание на `publicQuotaWait`: исходный helper добавляет численные Ozon fields даже WB wait; UI их пока не показывает. Для WB публичное состояние должно содержать только наблюдённый Retry-After, без выдуманной Ozon-квоты. Эта возможная корректировка ещё не сделана и не проверена.
6. После окончательной правки выполнить достаточные source/ZIP gates, полный remote CI; скачать точный финальный artifact, сверить ZIP и все inputs/tree, оформить итоговую квитанцию и актуализировать STATUS/ROADMAP/README/AGENTS.
7. Только после PASS и повторного чтения main выполнить non-force FF публикацию. Обычные docs-only финальные сведения не считать новой installed приёмкой. Затем короткий отчёт владельцу с удалённым SHA и пределами. Следующий продуктовый участок — ранний I1 с реальной серверной авторизацией.

## Доступ и публикация

HTTPS git fetch здесь не имеет credentials; работа шла GitHub connector tools github_fetch/create_tree/create_commit/create_branch/update_ref. Не выводить секреты и signed URLs. Для remote commit base_tree/parent брать из реально прочитанных remote refs, не из отличающейся локальной истории; returned tree должен совпасть с git write-tree. Никакого force, PR comments или сообщений людям.

Пауза не является разрешением закрыть D2.4, снять WB FAIL, выполнить I1/live calls/production или запустить дополнительных агентов.
