# I1-C1 / C2 client authority

Status: `C1 ACCEPTED`; `C2.1 ACCEPTED`; `C2.2-A ACCEPTED / REMOTE VERIFIED`; `C2.2-B ACCEPTED / REMOTE VERIFIED`; `C2.2-C ACCEPTED / REMOTE VERIFIED`; `C2.2-D1 ACCEPTED / REMOTE VERIFIED`; `C2.2-D2 ACCEPTED / REMOTE VERIFIED`. These bounded acceptances do not close full I1, C2, D3, Health/P8, S1.2, release or deployment.

Base: `bc0cd0088ca50ba06021ea602a46bdd90de91378`. Accepted C1 head: `56c81a3521c02502b65fd713aec890e5a30f038d` on `feature/extension-i1-client-2026-09-15`. C2.1 is accepted on candidate `4acc5fb3336e3e38fa30d6a7ec16c80730bcd7e1`. C2.2-A is accepted on exact tested implementation head `5d93bccac163c8728a8d7a3b1d4b9f23ea16b680`, tree `531033ac08f001dd55427c4ae969a6c4b5d6b861`; current acceptance evidence is in `docs/migration/evidence/extension-i1-c2-2a-2026-09-16/r4/`. C2.2-B is accepted on exact tested implementation head `8a5d9e6ca611d69512ad28fc00684c63bdc87324`, tree `1fb11a510d6dd314a0647c5348e56d1a26bc007e`; evidence is in `docs/migration/evidence/extension-i1-c2-2b-2026-09-17/r1/`. C2.2-C is accepted on exact tested implementation head `a1d0a9dc83daf80536f58bf56d498206da8a1eb3`, tree `1f7a54c0ac0c1c95b680071f7036f084715bac4c`; evidence is in `docs/migration/evidence/extension-i1-c2-2c-2026-09-17/r1/`. C2.2-D1 is accepted on exact tested head `fdccf06fbdb3d67beafd0ca1759db74018f6a475` and merged by PR #20 at `66140093650dae057b4bb9017d49f48faeb7c5db`. C2.2-D2 is accepted on exact tested head `860b4799eb5bf627e14bfbc7bcfff53a241a2800`, tree `305d326add8f04a9bb28dbbaee99abefbfdbe466`, and merged by PR #21 at `a2c9cdbd48914f6abf1e40221de4b64f1e5e76a7`; its receipt is `docs/migration/evidence/extension-i1-c2-2d2-2026-09-17/r1/README.md`.

R2 was reviewed for the earlier flight-owner, forced-bootstrap-refresh, fail-closed authority, composed signed-out routing, and contractual version-comparison scope. R3 corrects the remaining client authority/response classification and context-ownership paths, and adds meaningful same-worker account, rotation, denial, version, and composed-worker coverage. Polling, activation and refresh owners carry generation and logical identity; restrictive denial replaces in-memory authority before persistence/cleanup and uses removal as a durable fallback. Work permission is checked separately from composed application readiness. Pending transport/status/rate-limit failures remain retryable until the attempt deadline, while terminal denial is not restored as authority after restart.

Bootstrap V2 remains WebCrypto Ed25519 with packaged trust, exact signed bytes and strict envelope/payload checks. Work requires a current account/device/session generation, compatible browser/extension, supported local AI mapping, signed policy and a validated profile fingerprint; unsupported AI/profile fallback is denied. The browser verifier is included in the extracted package and is tested against source/package fixtures, including payload and identifier boundaries.

Account authority and local Work context carry auth generation. Compatible same-session bootstrap does not globally finish Work; authority changes and late callbacks are fenced. Ozon/WB queues, delivery, recovery, attachment/IDB and ordinary local marketplace dispatch remain covered by the preserved composed route. Popup account reset is visible in authenticated and pending states, with pending cancellation available.

C2.1 adds only durable `authority.cacheBinding` context and `state.cacheClock` metadata to `seller_agents_control_auth_v2`. Restore re-verifies the unchanged signed envelope and payload, requires exact packaged origin/contract/version/browser/trust/AI context, and preserves structurally valid credentials for same-origin package mismatch. A nondecreasing effective-time floor combines wall and monotonic time and is persisted through the existing mutation queue before fresh cached Work is allowed; freshness remains half-open (`effectiveNow < expiresAt`). Missing or unsafe metadata fails closed.

C2.2-A adds only online-first verified cached-bootstrap **configuration acquisition** for audited transport/503 conditions. The cached envelope is reverified with packaged trust and exact context binding, and ownership/obsolescence fences prevent stale completion. `bootstrapWithPolicy()` is not wired into Work dispatch. Offline Work, signed profile/capability execution, joint offline command-result behavior and scheduler integration remain outside C2.2-A.

C2.2-B adds only `getVerifiedBootstrapMetadata(options)`, a detached frozen projection over the already accepted C2.2-A path. It exposes signed top-level `features`, signed top-level `entitlements`, configuration identity and the already validated signed AI state/profile, but it always publishes `executionAuthority: false` and synthesizes neither `capabilities` nor `workAllowed`. The authority audit found no independent packaged local capability registry or packaged AI-profile fingerprint authority, so remote signed metadata is not treated as executable authority. No application Work/provider/scheduler path was changed.

C2.2-C adds only an independent immutable package-local capability-presence authority. `SellerAgentsPackagedCapabilities` records that the current deterministic package contains the reviewed Ozon, Wildberries, ChatGPT web and Alice web adapter components. The manifest and every row carry `executionAuthority: false`; `signedPermissionBindings` is empty; the global authority slot is non-writable/non-configurable. The local ids are deliberately a separate namespace from server `features`/`entitlements`, so equal-looking signed keys do not imply a permission binding. `canWork()`, application Work and provider execution remain unchanged.

C2.2-D1 adds the canonical server-owned permission vocabulary only. It defines exactly `source.ozon`, `source.wildberries`, `ai.chatgpt` and `ai.alice` as BOOLEAN/CAPABILITY permissions, each explicitly non-executing. It does not seed PostgreSQL, alter plans, emit Bootstrap permission values or change Work authority.

C2.2-D2 adds the explicit read-only binding/intersection authority only. `SellerAgentsCapabilityIntersection` maps the four accepted packaged ids to the four D1 signed entitlement keys. A row is satisfied only when the exact local capability exists and the exact signed entitlement is literal boolean `true`; missing/false denies, signed features never substitute, unknown keys never infer a binding, and non-BOOLEAN reviewed values fail the whole projection closed. Manifest, result, arrays and rows are immutable and every surface remains `executionAuthority: false`. No executable control-client method or Work/provider path was patched.

A server dependency was exposed by D2 acceptance: current beta access is only `BETA | NONE` and defines no per-capability permission set, while current Bootstrap signs commercial entitlements only for independently eligible commercial access. A beta-only snapshot therefore currently has `entitlements: {}` and D2 correctly denies all four capability rows. Do not bypass this by treating beta status, `features`, provider-specific entitlement names or local capability ids as permission. A separate server-owned capability-permission policy/emission step is required before execution wiring.

## Verification

The established verification surface includes the full I1 composition checker, shared core checker, focused C2 suites, source/extracted package parity, native Chromium fixture, installed local API/portal/PostgreSQL acceptance, documentation/OpenAPI/bridge guards, Server CI and Extension CI. C2.2-A architect acceptance verified all seven exact-head workflow runs successful with zero failed workflows on tested tree `531033ac08f001dd55427c4ae969a6c4b5d6b861`; focused policy source/extracted are `37/37 PASS` each and Extension I1 is `100/100 PASS`.

C2.2-B exact implementation tree `1fb11a510d6dd314a0647c5348e56d1a26bc007e` adds a focused signed-metadata gate with `5/5 PASS` on source and `5/5 PASS` on extracted package; the full Extension I1 checker is `102/102 PASS`. Extension I1 run `35187262735`, Documentation run `35187264986`, Extension CI PR run `35187264983`, and Extension CI push run `35187262733` are SUCCESS. Browser verification and installed-local API/portal/PostgreSQL acceptance are PASS; Extension CI includes the preserved Ozon/WB and WB-browser baselines. Server CI was not artificially re-triggered because the exact C2.2-B diff changes no server/contracts/schema/migration/OpenAPI/server-doc path; accepted base server authority remains unchanged.

C2.2-C exact implementation tree `1f7a54c0ac0c1c95b680071f7036f084715bac4c` adds a focused packaged-capability gate with `5/5 PASS` on source and `5/5 PASS` on extracted package; the full Extension I1 checker is `104/104 PASS`. Extension I1 run `35190621200`, Documentation run `35190625142`, Extension CI push run `35190621221`, and Extension CI PR run `35190625256` are SUCCESS. Browser verification and installed-local API/portal/PostgreSQL acceptance are PASS; both Extension CI runs include the preserved Ozon/WB and WB-browser baselines. The deterministic package is `1,839,269` bytes, SHA-256 `61e992cc28323c68bb2f448d88bb2d9ecc7ce2e0f1c9ced5de82b4ac2d123057`, with runtime/extracted/ZIP `39/39/39` and zero byte mismatches. Server CI was not artificially re-triggered because the exact diff changes no server/contracts/schema/migration/OpenAPI/server-doc path; accepted server authority remains unchanged.

C2.2-D1 changed exactly four server-owned vocabulary/documentation files. Full Server CI and Documentation CI passed. The first Server CI attempt exposed a stale-state `ADMIN_STATE_STALE` race in an unchanged admin E2E; the identical head passed the complete Server CI on rerun without a code patch, so no D1 behavior was changed to mask the flake. PR #20 then merged with expected-head protection.

C2.2-D2 exact implementation tree `305d326add8f04a9bb28dbbaee99abefbfdbe466` has a focused six-case intersection gate on both source and extracted package, including deep immutability of the computed result. Extension I1-C1 run `35207407249`, Documentation CI run `35207411644`, Extension CI push `35207407386`, and Extension CI PR `35207411652` are SUCCESS. The full I1 checker reports `106` gate processes PASS, browser verifier PASS on Chromium `151.0.7922.34`, installed-local API/portal/PostgreSQL PASS, and preserved Ozon/WB/WB-browser baselines PASS. Exact-head artifacts are recorded in the D2 receipt. The merge commit `a2c9cdbd48914f6abf1e40221de4b64f1e5e76a7` preserves the exact tested tree.

The native Chromium fixture uses a per-run ephemeral signing key: only its public trust bundle enters the local package, while the private key stays temporary. Installed-local development uses fixture OTP `424242`; it is not real email-delivery or preprod/production evidence.

## Evidence and handoff

C2.1 evidence is in `docs/migration/evidence/extension-i1-c2-1-2026-09-16/`; its design notes are in `C2_CACHE_TIME_2026-09-16.md`. C2.2-A design/acceptance is in `C2_OFFLINE_POLICY_2026-09-16.md` and `docs/migration/evidence/extension-i1-c2-2a-2026-09-16/r4/`. C2.2-B design/acceptance is in `C2_SIGNED_METADATA_2026-09-17.md` and `docs/migration/evidence/extension-i1-c2-2b-2026-09-17/r1/`. C2.2-C design/acceptance is in `C2_PACKAGED_CAPABILITIES_2026-09-17.md` and `docs/migration/evidence/extension-i1-c2-2c-2026-09-17/r1/`. C2.2-D1 design is in `C2_SERVER_PERMISSION_VOCABULARY_2026-09-17.md`. C2.2-D2 design/acceptance is in `C2_CAPABILITY_INTERSECTION_2026-09-17.md` and `docs/migration/evidence/extension-i1-c2-2d2-2026-09-17/r1/README.md`.

The next capability-related step is **not** direct offline Work or provider dispatch. It must first establish an explicit server-owned permission policy/emission path for the four reviewed D1 keys. The current beta-access authority does not define capability grants and beta-only Bootstrap currently signs an empty entitlement map. The next bounded server step must therefore define exactly which reviewed permissions are valid for each authorized access basis and emit them through the already signed entitlement map without inference. `NONE` and malformed/unknown policy must remain fail-closed; commercial plan entitlements remain independently authoritative; beta/commercial overlap must be explicitly resolved. Only after that server result is separately accepted may a later step consider D2 as part of executable Work authority while preserving account/session generation, compatibility, AI profile, Health, freshness and state-machine gates. Health, S1.2/D3, full I1/D2 and release/deployment remain separate.
